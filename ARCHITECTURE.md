# Architecture: Nebula

## Overview

Nebula is a multi-platform Swift foundation/architecture library distributed as an SPM package — the sibling of Cosmos (the SwiftUI design system). It is **not** a UI library. It begins with four `Sendable` value-type configuration structs and a `Sendable` error envelope, plus extensions on Foundation value types:

- `NebulaLogConfiguration` — logging behavior: level, category, subsystem, min level, `@Sendable` handler.
- `NebulaErrorConfiguration` — error reporting: isEnabled, category, `@Sendable` handler.
- `NebulaStandards` — formatting policy: locale, timeZone, calendar, FormatStyle accessors.
- `NebulaMeasureConfiguration` — measurement: clock, signposter, enabled, handler.
- `NebulaError` — `Error`/`LocalizedError`/`CustomNSError`/`Sendable`/`Hashable` envelope with lossy mapping from `NSError`/`DecodingError`/`URLError`/`CocoaError`/`any Error`.

Every contract is a `Sendable` struct with a `@Sendable` handler and fluent `.with*` builders — mirroring `CosmosConfiguration`/`CosmosLogConfiguration`/`CosmosErrorConfiguration` minus the SwiftUI `@Entry`/`@Observable` plumbing. Apps inject explicitly (process-wide `Mutex<Nebula*Config>` accessor or explicit-parameter DI); there is no SwiftUI environment.

## Goals

1. **Apple-native primitives only:** `os.Logger`/`OSSignposter`, `FormatStyle` family, `Measurement`, `Regex`, `AttributedString`, `Duration`/`Clock`, `Mutex`/`Atomic`. No `swift-log`, no legacy `Formatter` subclasses in the public surface.
2. **Concurrency safety:** Swift 6 strict mode; every public type `Sendable`; zero concurrency warnings; `Mutex`/`Atomic` from `Synchronization`; no `NSLock`/`DispatchQueue`/`nonisolated(unsafe)`.
3. **One import:** a single `Nebula` target exposes logging, errors, extensions, formatting, and measurement through one `import Nebula`.
4. **Lossy-but-Sendable error mapping:** `any Error` is not `Sendable` — mapping inits consume the existential at construction time and keep only Sendable fragments; `NebulaError` is a concrete `Sendable: Error` for typed-throws consumers.
5. **Apple-aligned:** modern APIs over legacy; `@available` gates include all 5 platforms incl. `visionOS 26`; DocC documentation.
6. **Testability:** value types and plain contracts are easy to unit-test off the main actor.

## Non-goals

- Backwards compatibility with pre-v26 Apple platforms (Nebula major == OS major; baseline Nebula 26).
- UIKit support or any explicit UIKit dependency.
- SwiftUI environment integration or any `@Entry`/`@Observable`/`@Environment` surface (apps inject explicitly).
- UI/presentation layer (Nebula reports, it does not present — mirrors Cosmos).
- `RecoverableError` adoption (AppKit-oriented, escaping closure, not multiplatform-Sendable-friendly).
- Back-deployment via `FoundationPreview` in v1 (Apple-platform-only at `.v26`).
- Runtime theming engine (static configurations only).

## Stack

- Swift 6.3+ toolchain (dual Xcode 26.4+ / Xcode 27 build), language mode v6, Xcode 26.4+
- Foundation + os + Synchronization + _Concurrency + CryptoKit (only behind `NebulaHashAlgorithm`)
- Swift Testing
- DocC

## Structure

```
Nebula/
├── Sources/Nebula/          # Single SPM target
│   ├── Nebula.swift         # `Nebula` enum + `NebulaVersion` (Nebula N == OS N baseline)
│   ├── Nebula.docc/         # DocC catalog (auto-discovered)
│   ├── Logging/             # os.Logger façade + signposter + memory handler
│   ├── Errors/              # NebulaError envelope + Box + configuration + lossy mapping
│   ├── Extensions/          # DateTime/ String/ Number/ Primitive/ Collection/ Codable/ DataURL
│   ├── Standardize/         # NebulaStandards + NebulaStandardsConfig (FormatStyle façades)
│   ├── Measure/             # NebulaMeasureConfiguration + NebulaMeasureResult + NebulaMeasureConfig
│   ├── Environment/         # NebulaEnvironment + NebulaEnvironmentConfiguration + NebulaEnvironmentConfig (Info.plist-keyed environment reader)
│   └── Architecture/        # Clean Architecture toolkit — seams only (domain/ports/use case/repository/gateway/validation/registry/testing/async) + Presentation/ (Foundation-only nav model) + Preferences/ (UserDefaults façade + port) + Network/ (Endpoint/Client/Request/Response + per-endpoint cache + local HTTP server + interceptors/auth) + Pinning/ (SSL/TLS public-key pinning — Sendable policy + pure evaluator + URLSessionDelegate façade + session builder) + Streaming/ (SSE — pure WHATWG parser + event stream + config + open-struct error; WebSocket — port + message bridge + URLSessionWebSocketTask façade + combined delegate + session builder + open-struct error) + Bodies/ (multipart/form-data — pure Data composer + temp-file writer + open-struct error; download — URLSession.download façade + progress stream + resume + move-to-destination + per-call config + open-struct error) + Keychain/ (Security.framework SecItem* façade + secure-store port) + FeatureFlags/ (feature-flag port + in-memory façade + remote port + composite) + Notifications/ (UNUserNotificationCenter port + delegate-adapter façade + @Sendable-handler config + value types) + Permissions/ (NebulaPermissionStatus value enum) + BackgroundTasks/ (BGTaskScheduler port + Sendable-handle façade + @Sendable-handler config + value types; macOS/watchOS-unavailable) + Metrics/ (metrics port + event/value/kind types + Mutex event batcher + @Sendable-handler config + local façade) + Analytics/ (analytics port + event + @Sendable-handler config + local façade; reuses the event batcher) + CloudKit/ (CloudKit sync port + CKSyncEngine wrapper + Sendable Equatable config + stateless-per-call CloudKit boundary — CloudKit-backed preferences + CloudKit-backed remote feature flags; no CKDatabase stored, Sendable derived with no @unchecked) + PerformanceSink (routes Measure results into Metrics)
└── Tests/
    └── NebulaTests/          # Swift Testing — mirrors source layout

Meridian/                      # Sibling SPM package (SwiftUI) — depends on Nebula via ../
├── Sources/Meridian/         # @Observable Router + MeridianNavigationStack + DocC
├── Sources/MeridianExample/  # @main App — compile gate / living docs (NOT shipped)
└── Tests/MeridianTests/      # Swift Testing — Router / DeepLink / Destination

Aurora/                        # Sibling SPM package (SwiftData) — depends on Nebula via ../
├── Sources/Aurora/           # @ModelActor AuroraRepository + AuroraEntityMapping + DocC
├── Sources/AuroraExample/    # @main CLI — compile gate / living docs (NOT shipped)
└── Tests/AuroraTests/        # Swift Testing — repository CRUD / port conformance / Sendable
```

## Configuration contracts

All four contracts are `Sendable` `struct` value types. Mutations happen by replacement via fluent `.with*` builders returning a copy (mirror Cosmos):

```swift
let log = NebulaLogConfiguration.default
    .withSubsystem("com.example.app")
    .withMinLevel(.info)
    .withHandler { event in /* @Sendable */ }

NebulaErrorConfig.set(
    NebulaErrorConfiguration.default
        .withCategory("App")
        .withHandler { event in /* @Sendable */ }
)

let standards = NebulaStandards.default
    .withLocale(Locale(identifier: "en_US_POSIX"))
    .withTimeZone(.gmt)
```

There is no `@Entry` environment value and no `@Observable` runtime theme. Apps either set a process-wide default via `Nebula*Config.set(_:)` (Mutex-backed) or pass a configuration explicitly as a parameter. Both paths are supported — global Mutex default for ergonomics, explicit parameter for testability.

## Logging

`Sources/Nebula/Logging/` is a thin Sendable façade over the native `os.Logger` / `OSSignposter` unified-logging stack (no `swift-log`, no third-party deps).

| Type | Responsibility |
|---|---|
| `NebulaLogLevel` | enum Int Sendable mapping 1:1 to `os.OSLogType` (debug/info/notice/error/fault; `warning` alias = error — verified `Logger.warning` → `.error`). |
| `NebulaLogCategory` | Sendable `ExpressibleByStringLiteral` struct with presets (`.networking`/`.persistence`/`.formatting`/`.measure`/`.concurrency`/`.general`). |
| `NebulaLogger` | Sendable struct façade over `os.Logger` (`@unchecked Sendable`, iOS 14+); `init(subsystem:category:)`, `isEnabled(_:)`, `debug`/`info`/`notice`/`warning`/`error`/`fault`/`log(level:_:)` (String convenience path — builds the `OSLogMessage` literal at the call site so it compiles under `oslog.log_with_level`; defaults `.public`, NO per-argument redaction). Exposes `public var osLogger: os.Logger` as the redaction-preserving path (the `OSLogMessage` literal must appear directly at an `os.Logger` call site — `os.Logger` cannot be wrapped, compile-verified). `var signposter: NebulaSignposter`. |
| `NebulaLogEvent` | Sendable struct (category/level/message/date) for the handler fan-out path. |
| `NebulaLogConfiguration` | Sendable struct + `@Sendable` handler + `.with*` builders + `static let default`. `logger()` builds a `NebulaLogger`; `log(_:_:)` is the String convenience path (defaults `.public`, loses per-argument redaction — document loudly). |
| `NebulaSignposter` | Sendable struct over `os.OSSignposter` (iOS 15+); exposes `public var osSignposter` for `emitEvent`/`beginInterval`/`endInterval`/`withIntervalSignpost` (the `StaticString`/`SignpostMetadata` literals must appear at the `os.OSSignposter` call site — cannot be wrapped, compile-verified); keeps Nebula-typed `makeSignpostID`/ID/state wrappers (no-literal ops). |
| `NebulaSignpostID`/`NebulaSignpostIntervalState`/`NebulaSignpostMetadata` | Sendable wrappers over the os signpost types (metadata is a `typealias = os.SignpostMetadata`). |
| `NebulaMemoryLogHandler` | `final class @unchecked Sendable` in-memory ring buffer for tests/preview; `Mutex<T>`-backed (effective floor iOS 18/macOS 15/visionOS 2 — below `.v26`). The ONLY `@unchecked Sendable` in the codebase — a reference type, not a value type, so it does not violate the "derive Sendable, never `@unchecked` on a Nebula-defined value type" rule. |

**Deferred**: `NebulaLogStoreExporter`/`NebulaLogStoreEntry` (macOS-only `os.OSLogStore` exporter — `#if os(macOS)` + explicit per-platform unavailable; visionOS availability uncertain → conservatively gate unavailable) is tracked in `ROADMAP.md` → "Later". `OSMetricOperation` (OS 26 signpost-metrics) is `internal` in the os overlay — cannot be re-exported; if metric APIs are needed, Nebula defines its own public operation enum gated `@available(iOS 26, *)` (deferred to Nebula 27).

## Errors

`Sources/Nebula/Errors/` is a Sendable value-typed envelope plus a configuration contract mirroring `CosmosErrorConfiguration`.

| Type | Responsibility |
|---|---|
| `NebulaError` | `Error`/`LocalizedError`/`CustomNSError`/`Sendable`/`Hashable` envelope: `code`/`kind`/`message`/`failureReason`/`recoverySuggestions[]`/`helpAnchor`/`metadata`/`context`/`date`/`underlying: Box?`. |
| `NebulaError.Code` | `Sendable`, `Hashable` struct `(domain: String, code: Int)` — stable identity across NSError/Swift. |
| `NebulaError.Kind` | `String, Sendable, CaseIterable` enum (network/decoding/encoding/cocoa/file/validation/serialization/unknown). |
| `NebulaError.Context` | `Sendable`, `Hashable` struct (codingPath `[String]`, debugDescription, source). |
| `NebulaError.Box` | `final class: Sendable, Hashable` — reference box holding a nested `NebulaError` (required because a Swift `struct` cannot recursively contain itself; `underlying: NebulaError?` does NOT compile). Derived `Sendable` on a final class with a Sendable `let`; no `@unchecked`. |
| `NebulaErrorConfiguration` | `Sendable` ONLY (NOT `Equatable` — the `@Sendable` handler closure is not `Equatable`, mirroring `CosmosErrorConfiguration`). Fluent `.with*` builders; `static let default`. |
| `NebulaErrorEvent` | `Sendable`, `Equatable` struct (category, error, date) — Equatable valid (all fields Equatable). |
| `NebulaErrorConfig` | Process-wide `Mutex<NebulaErrorConfiguration>` accessor (`get()`/`set(_:)`). |
| `NebulaDecodingError`/`NebulaEncodingError` | Sendable mirrors of `DecodingError`/`EncodingError` (Kind, codingPath `[String]`, debugDescription, underlyingErrorDescription). |

**Lossy mapping inits** (`NebulaError+Mapping.swift`): `init(_ nsError: NSError)`, `init(decodingError:)`, `init(urlError:)`, `init(cocoaError:)`, `init(error: any Error)` — the underlying `any Error` is consumed at construction time and NOT retained (not `Sendable`); only Sendable fragments survive. `NebulaError.wrap(_:)` wraps any throwing closure into `Result<T, NebulaError>`.

**Typed-throws policy**: untyped `throws` is the default for all public Nebula APIs (SE-0413 evolution safety); `NebulaError` is exposed as an opt-in concrete `Failure` for `throws(NebulaError)` / `Result<T, NebulaError>`. `RecoverableError` is deliberately NOT adopted (AppKit-oriented, escaping closure, not multiplatform-Sendable-friendly); `recoverySuggestions[]` is surfaced via `LocalizedError.recoverySuggestion`.

## Extensions

`Sources/Nebula/Extensions/` hosts value-type extensions on Foundation primitives, grouped by concern. All derive `Sendable`; no `@unchecked` on Nebula types; no legacy `Formatter` subclasses.

| Folder | Coverage |
|---|---|
| `DateTime/` | `Date` arithmetic/predicates (startOfDay/endOfDay/startOfWeek/…, DST-safe via `calendar.dateInterval(of:for:)`), `DateComponents` builders, `DateInterval.init(start:duration: Swift.Duration)` (distinct from Foundation's TimeInterval overload), `NebulaDateFormat`/`NebulaDurationFormat` façades. No `Calendar.gregorian` static (does not exist — use `Calendar(identifier: .gregorian)`); `RelativeFormatStyle` via init (no fluent chain, no `.offset`/`.timer`). ISO/stable presets pinned to `Locale(identifier: "en_US_POSIX")` + `.gmt`. (No `NebulaClock` — deferred; measure uses `any Clock<Duration>` directly.) |
| `String/` | `isBlank`/`trimmed`/`truncated`/case conversions/base64/hex/URL extraction; `NebulaRegex<Output>` (Sendable, conditional `where Output: Sendable`); `NebulaRegexPatterns` (curated literals: UUID/IPv4/hex/semver/ISO-timestamp — NO email, per Apple guidance); `NebulaStringDetectedEntity` over `NSDataDetector` (cached in `Mutex<NSDataDetector?>`); `NebulaStringLocalization` (Sendable struct over `String(localized:)` + `AttributedString(localized:)`, Foundation scope only — no SwiftUI/UIKit scopes). |
| `Number/` | `NebulaFormattingOptions` (Sendable + `.with*`) + `NebulaNumberFormatting` façade (percent/currency/bytes/list/measurement); `BinaryInteger`/`BinaryFloatingPoint`/`Decimal` extensions (clamped/rounded/isWholeNumber/measured(in:)). `Decimal.rounded(toDecimalPlaces:)` via `NSDecimalRound` (C) + `NSDecimalNumber.RoundingMode` (Decimal has NO value-level `rounded(_:)` — verified). `Decimal.FormatStyle` is its own struct (Decimal does NOT conform to `FloatingPoint`). |
| `Primitive/` | `Comparable.clamped(to:)` (fills SE-0177 gap), `BinaryInteger.isEven`/`isOdd`/`times(_:)` (non-escaping rethrows — matches `forEach`), `Optional.or(_:)`/`orThrow(_:)`/`isNilOrEmpty`, `NebulaNilError` (concrete Sendable), `UUID.shortString`/`isValid(_:)`. Gate `UUID.nebulaRandom()` at `@available(iOS 26.4, *)` (wraps `random(using:)` — `UUID()` remains the default random v4 generator). NEVER redeclare `Bool.toggle()`/`isMultiple(of:)`. |
| `Collection/` | `nebulaChunked(byCount:)`/`nebulaWindows(ofCount:)`/`nebulaUniqued(on:)`/`nebulaStablePartition(by:)`/`nebulaPartitioned(by:)`/`nebulaSorted(_:)` key-path/`nebulaMerging`. `nebula*` prefix on open `Collection`/`Sequence` (no stdlib pollution). Eager by default (conscious divergence from swift-algorithms' lazy `uniqued()`); non-escaping `rethrows` closures (NOT `@escaping @Sendable`). (`nebulaFiltered(by:)` over `Foundation.Predicate` and `NebulaFrequency` are deferred to post-0.1.0.) |
| `Codable/` | `NebulaJSONDecoder`/`NebulaJSONEncoder` (Sendable wrappers holding a configure-once-frozen `JSONDecoder`/`JSONEncoder` in a `let`); `NebulaJSONDecoderConfiguration`/`NebulaJSONEncoderConfiguration` (Sendable + `.with*`, all strategy enums Sendable); `NebulaDecodingError`/`NebulaEncodingError` + `DecodingError.nebula`/`NebulaError.decoding(_:)` bridges; `Decodable.init(fromJSON:)`/`Encodable.toJSONData`. NO `OutputFormatting.fragmentsAllowed` (does not exist — that's `JSONSerialization.WritingOptions`; `ReadingOptions` uses `.allowFragments`). |
| `DataURL/` | `Data.nebulaHexEncodedString`/`init?(nebulaHexEncoded:)` (Foundation has NO native hex); `NebulaHashAlgorithm` (Sendable enum over CryptoKit SHA256/384/512 — the ONLY `import CryptoKit`); `Data.nebulaDigest`/`nebulaHexDigest`; `URL.nebulaAppending(queryItem:)`/`nebulaSettingQueryItem`/`nebulaRemovingQueryItem`/`nebulaQueryItem(named:)`/`nebulaPercentEncoded()`; `URLComponents.nebulaWith(queryItem:)` fluent builders. Defer base64URL/omitPadding to Nebula 26.4. |

## Standardize

`Sources/Nebula/Standardize/` hosts `NebulaStandards` (+ `NebulaStandardsConfig`, the process-wide `Mutex` accessor) — a single Sendable façade over Foundation's modern `FormatStyle` family (Date/ISO8601/Decimal/Integer/FloatingPoint/Measurement/Currency/Percent/ByteCount/List/PersonNameComponents/URL/Duration). It pre-configures locale/timeZone/calendar once and returns Sendable `FormatStyle` values callers can chain (`.attributed`, `.precision`, `.grouping`, `.locale`, `.notation`). No legacy `NumberFormatter`/`DateFormatter`/`MeasurementFormatter`/`ByteCountFormatter`/`ISO8601DateFormatter` (superseded; `ListFormatter` wrapped per-call only — no FormatStyle replacement, not Sendable).

| Method | Returns |
|---|---|
| `date` / `iso8601` / `date(verbatim:)` | `Date.FormatStyle` / `Date.ISO8601FormatStyle` / `Date.VerbatimFormatStyle` |
| `decimal` / `integer<Value>()` / `double<Value>()` / `percent<Value>()` / `currency<Value>(code:)` | `Decimal.FormatStyle` / `IntegerFormatStyle` / `FloatingPointFormatStyle` / `.Percent` / `.Currency` |
| `byteCount(style:)` / `list(memberStyle:type:width:)` / `name` | `ByteCountFormatStyle` / `ListFormatStyle` / `PersonNameComponents.FormatStyle` |
| `measurement<U>(width:usage:numberFormatStyle:)` | `Measurement<U>.FormatStyle` (width/usage/numberFormatStyle — NO `unit` param; unit is implied by the `Measurement<U>` value) |
| `duration(units:width:maximumUnitCount:)` / `duration(pattern:)` | `Duration.UnitsFormatStyle` (`Set<Duration.UnitsFormatStyle.Unit>` — NOT `[Duration.Unit]`; there is NO top-level `Duration.Unit`) / `Duration.TimeFormatStyle` |
| `url` | `URL.FormatStyle` (iOS 16+) |
| `string(_:format:)` / `attributed(_:format:)` | convenience `String` / `AttributedString` (Sendable, iOS 15+) |

Gate `DateComponents.formatted(_:)` + `DateComponents.ISO8601FormatStyle` + `.iso8601` static at `@available(iOS 26, *)` (whole DateComponents formatting family is at-floor).

## Measure

`Sources/Nebula/Measure/` hosts the 4th configuration contract, pairing `Clock`/`Duration` (`_Concurrency`) for timing with `os.OSSignposter` for Instruments integration. Signpost integration lives on `NebulaSignposter` (under Logging); `NebulaMeasureConfiguration` is the contract that carries the measurement API — mirroring `NebulaLogConfiguration.log` — rather than a separate `NebulaMeasure` type.

| Type | Responsibility |
|---|---|
| `NebulaMeasureConfiguration` | Sendable struct (the 4th config contract): clock (`any Clock<Duration>`, default `ContinuousClock`), signposter, enabled, `@Sendable` handler, fluent `.with*`. Carries `measure(_:operation:)` sync+async (returning `(T, Duration)`) and `bench(_:iterations:warmup:operation:)` micro-bench ON the config. |
| `NebulaMeasureResult` | Sendable struct (name, iterations, total Duration, perIteration, components). Minimal for v1 (no p50/p99 — deferred). |
| `NebulaMeasureConfig` | Process-wide `Mutex<NebulaMeasureConfiguration>` accessor (`get()`/`set(_:)`). |

`ContinuousClock` counts sleep; `SuspendingClock` does not (WWDC22 110355) — document the choice so users pick the right clock. `bench()` is a quick micro-bench, not statistically rigorous (no p50/p99 yet).

**Deferred**: `NebulaClock` (ContinuousClock/SuspendingClock wrapper) — `NebulaMeasureConfiguration` uses `any Clock<Duration>` directly. Tracked in `ROADMAP.md` → "Later".

## Architecture

`Sources/Nebula/Architecture/` is the **second surface** of Nebula: the Clean Architecture toolkit. It ships **only the seams** — inner-owned marker protocols, a DTO contract, repository/gateway ports, a use-case type, validators, a registry, test doubles, async-flow helpers, and the Foundation-only navigation model — so an app can implement Clean Architecture efficiently without Nebula owning any database or framework code. Concrete adapters (repositories, gateways, presenters, URLSession networking) live in the app; the SwiftUI presentation layer lives in the **Meridian** sibling package (`Meridian/`, see [Presentation (Meridian)](#presentation-meridian)). Nebula itself owns no presenter and no SwiftUI. The toolkit is pure Swift + Foundation + `Synchronization`; every symbol sits at the Nebula 26 floor (no above-floor gates). The DocC catalog (`Sources/Nebula/Nebula.docc/Architecture.md`) is the canonical article.

| Subtree | Responsibility |
|---|---|
| `Domain/` | Markers `NebulaValue`/`NebulaEntity`/`NebulaAggregate` + the phantom-typed `NebulaID` identity value. |
| `Ports/` | Bare `Sendable` markers `NebulaInputPort`/`NebulaOutputPort`/`NebulaDTO`. Nebula defines no presenter. |
| `Errors/` | `NebulaFailure` protocol + per-layer open structs (`NebulaDomainError`, `NebulaValidationError`) bridging to the closed `NebulaError.Kind` via a caller-picked `toNebulaError(kind:)`, **plus** `NebulaUserError` + `RecoveryAction` (Wave N12 — the user-error bridge value, **not** a `NebulaFailure`). No new `Kind` cases. |
| `UseCase/` | `NebulaUseCase<I, O>` generic `Sendable` struct over a `@Sendable` async body + `NebulaUseCaseRole` (command/query); `.logged`/`.measured`/`.reported`/`.instrumented` decorators route to the existing configs (no 5th config); `executeTyped(_:) async throws(NebulaError)`. |
| `Repository/` | Capability protocols (`NebulaRepository` / read-only / keyed / writable / deletable) + `NebulaRepositoryError`. No CRUD mandate, no `update` verb. |
| `Gateway/` | `NebulaGateway` marker + `NebulaGatewayConfiguration` (reuses `NebulaJSONDecoder`/`Encoder`) + `NebulaGatewayConfig` accessor + the concrete `NebulaHTTPGateway` over `URLSession` (retries via `NebulaRetry`; `URLError`/HTTP-status → `NebulaError` `.network`) + `NebulaHTTPStatusError`. |
| `Network/` | The proper network surface (Waves N5–N8) + interceptors (Wave N10): `NebulaHTTPMethod` + `NebulaHTTPEndpoint` (port, `URLRequestConvertible`) + `NebulaHTTPRequest`/`NebulaHTTPBody` (concrete value, reused as the server-side parsed-request type) + `NebulaHTTPResponse` + `NebulaHTTPClient` (non-generic port refining `NebulaGateway`; verbs as default extensions); per-endpoint cache — `NebulaHTTPCachePolicy` + `NebulaHTTPCache` port (`NebulaCachedResponse` carries `isStale`) + `NebulaURLCache` façade (`Mutex<State>` over native `URLCache` + Nebula TTL metadata; `final class` derives `Sendable`, no `@unchecked`); local HTTP/1.1 server — `NebulaHTTPServer` (`final class: Sendable` over `NWListener`) + `NebulaHTTPRequestParser` (internal, bounded) + `NebulaHTTPServerError` (per-layer open struct `: NebulaFailure`); request/response interceptors — `NebulaHTTPInterceptor` (port, `adapt`/`retry`) + `NebulaHTTPInterceptorChain` (compose left-to-right, retry-once from the original endpoint, `CancellationError` not retried) + `NebulaInterceptedClient` + `NebulaHTTPClient.intercepted(by:)` + `NebulaTokenProvider` (PAT, erased `Token`) + `NebulaAuthInterceptor` (concrete **`actor`** — Nebula's first; single-flight 401 refresh via an actor-isolated `Task`, bearer injection, retry-once; no new `NebulaError.Kind` case — transparent, surfaces the provider error on refresh failure). Foundation + Network + Synchronization only; no `#if os()`; `Sendable` derived throughout. |
| `Network/Pinning/` | SSL/TLS public-key pinning (Wave N17a) — `NebulaSSLPinningPin` (32-byte SHA-256 SPKI digest; `init?(digest:)`/`init?(hexDigest:)`/`hexDigest`) + `NebulaSSLPinning.HostPins` (host + pin set) + `NebulaSSLPinning` (Sendable policy: `hostPins`/`includeSubdomains`/`validateChainFirst: true`/`failClosedForUnknownHosts: true`, fluent `.with*`) + `NebulaSSLPinningResult` + `NebulaSSLPinningEvaluator` (pure `static evaluate(trust:host:policy:)` — OWASP any-position SPKI, additive to system trust; `internal resolvedPins(for:policy:)` case-insensitive host lookup + subdomain walk) + `NebulaURLSessionDelegate` (`final class : NSObject, URLSessionDelegate, Sendable` — derived `Sendable`, **no `@unchecked`**, the `NebulaUNNotificationCenter` precedent; `@objc optional` completion-handler method; `internal disposition(for:policy:trust:)` testable seam) + `NebulaHTTPSession`/`NebulaPinnedSession` (`pinned(by:configuration:logger:)` returns BOTH the `URLSession` and the delegate — `URLSession` does NOT retain its delegate) + `NebulaSSLPinningError` (open-struct, no new `NebulaError.Kind`, mirrors `NebulaHTTPServerError`). **Pinning is transport-layer, NOT an interceptor** (a pin failure surfaces as `URLError` from `URLSession` before/under `data(for:)` → trust eval at `URLSessionDelegate`) → the gateway is untouched (Option A: caller injects a delegate-configured session). `import Security` in-bounds (Keychain precedent); the Security framework has no `.swiftinterface` (headers are ground truth); all `Sec*` below the `.v26` floor → no `@available` gate. SHA-256 via `Data.nebulaDigest(of: .sha256)` — no new `import CryptoKit`. Foundation + Security + Synchronization only; no `#if os()`; `Sendable` derived throughout. |
| `Network/Streaming/` | Streaming (Wave N17b) — two additive surfaces over the buffered HTTP gateway (the gateway is untouched; pinning reused via composition/forwarding, zero N17a change). **SSE:** `NebulaSSEEvent` (`Sendable`/`Equatable`/`Hashable`: `id`/`event` (default `"message"`)/`data` (multi-line joined `\n`, trailing newline stripped)/`retry: Int?`) + pure `internal NebulaSSEParser` (`@discardableResult mutating feed(_ line: String) -> NebulaSSEEvent?`, WHATWG state machine, no I/O — the testable seam) + `NebulaSSEConfiguration` (`Sendable` **NOT `Equatable`** — the `@Sendable` sleeper, mirroring `NebulaLogConfiguration`; per-call, NOT process-wide → **no `Mutex` accessor**; `reconnect`/`maxReconnectAttempts`/`reconnectDelay`/`sleeper`; fluent `.with*`) + `NebulaSSEEventStream` (`enum` namespace; `events(for:session:configuration:) -> AsyncThrowingStream<NebulaSSEEvent, any Error>` — the CLAUDE.md-mandated concrete return; **iterates the raw `bytes` NOT `.lines`** (`AsyncLineSequence` skips empty lines but SSE dispatches on blank lines → `.lines` would drop every dispatch boundary); custom reconnect loop with `Last-Event-ID` (NOT `NebulaRetry.withPolicy` — the request mutates per attempt; cancellation honored immediately, never retried)) + `NebulaSSEError` (open-struct, no new `NebulaError.Kind`, mirrors `NebulaHTTPServerError`). **WebSocket:** `NebulaWebSocketMessage` (Nebula-owned `case data`/`case string` enum mirroring `URLSessionWebSocketTask.Message` — never expose Apple's nested enum in a Nebula port; failable `init?(_:)` with `@unknown default: return nil`) + `NebulaWebSocketClient` (a `Sendable` port — mirror `NebulaHTTPClient` without gateway inheritance; `send`/`receive`/`sendPing` `async throws`, `close(with:reason:)` `async` non-throwing, `closeCode`/`maximumMessageSize`) + `NebulaURLSessionWebSocket` (`final class : NebulaWebSocketClient, Sendable` façade over `URLSessionWebSocketTask`; `Sendable` **derived** — `URLSessionWebSocketTask` IS `NS_SWIFT_SENDABLE` (`NSURLSession.h:1121`, correcting the prior research note), **no `@unchecked`**; `sendPing` wrapped in `withCheckedThrowingContinuation` (no async overlay); `internal mapSend/mapReceive/mapPing/isCancellation/box` testable seams) + `NebulaWebSocketSessionDelegate` (`final class : NSObject, URLSessionWebSocketDelegate, Sendable` — ONE object per session: pinning via **composition** (holds a `NebulaURLSessionDelegate`, forwards the auth-challenge method — zero N17a change) + WebSocket lifecycle (`didOpenWithProtocol`/`didCloseWith`); `Sendable` **derived** (probe EXIT=0, the `NebulaUNNotificationCenter` analogy — `URLSessionWebSocketDelegate` is `@objc` NOT `NS_SWIFT_SENDABLE`, but conformance does NOT block derived `Sendable` on a `final class` with all-`let` Sendable props), **no `@unchecked`**; no `import Security` — the eval lives in the N17a delegate) + `NebulaPinnedWebSocketSession`/`NebulaWebSocketSession` (`pinned(by:)` returns BOTH the `URLSession` and the delegate — `URLSession` does NOT retain its delegate; `open(url:using:)`/`open(request:using:)` the ergonomic entry point) + `NebulaWebSocketError` (open-struct; `closed(code:reason:)` carries the `CloseCode` in metadata + reason in message; no new `NebulaError.Kind`). All streaming APIs below the `.v26` floor on every platform → **no `@available` gate**. `AsyncThrowingStream` cancellation: the consumer's `for try await` ends **normally** on consumer cancel (`Iterator.next()` returns `nil` — does NOT throw `CancellationError`); the internal `finish(throwing:)` tears the `bytes(for:)` task down via `onTermination`. Foundation only (the delegate + session builder reuse N17a, no `import Security` here); `import Network` only in the test (an `NWListener` echo server — `URLProtocol` does NOT intercept `URLSessionWebSocketTask`). |
| `Network/Bodies/` | Bodies & downloads (Wave N17c) — three **additive** surfaces over the buffered HTTP gateway (the gateway is untouched; pinning reused via composition, zero N17a change). **Multipart:** `NebulaMultipartPart` (`Sendable`/`Equatable`/`Hashable`: `name`/`filename?`/`contentType?`/`body`; `.field(name:value:)` `Data`+`String` overloads / `.file(name:filename:contentType:data:)`) + `NebulaMultipartFormData` (`data`/`contentType`/`boundary`) + `NebulaMultipartBuilder` (immutable value-type builder; `adding(_:)` fluent; `build()` **pure** — a `multipart/form-data` (RFC 2388) `Data` composer; Foundation has NO multipart API → genuinely new; boundary auto-generated via 16 random bytes hex-encoded under `"----NebulaBoundary"` (no `import CryptoKit`); `file(in:)` writes a temp file for `URLSession.upload(for:fromFile:)`; **gateway-compatible** — feeds the existing `NebulaHTTPBody.data(_:contentType:)`, no new body case) + `NebulaMultipartError` (open-struct, no new `NebulaError.Kind`, mirrors `NebulaSSEError`). **Download:** `NebulaDownloadConfiguration` (`Sendable` **NOT `Equatable`** — the `@Sendable` `destination`/`sleeper`; per-call, NOT process-wide → **no `Mutex` accessor**; `destination`/`resume`/`maxResumeAttempts`/`resumeDelay`/`sleeper`/`logger`; fluent `.with*`) + `NebulaDownloadCompletion` (internal race-safe `Mutex<State>` pending/awaiting/resolved) + `NebulaDownloadHandle` (`progress: AsyncThrowingStream<Double, any Error>` (fraction 0.0–1.0) + `value() async throws -> URL` + `cancelByProducingResumeData() async throws -> Data?`) + `NebulaDownloadDelegate` (`final class : NSObject, URLSessionDownloadDelegate, Sendable` — the **per-task** delegate; `Sendable` **derived** (probe EXIT=0, the N17b analogy — `URLSessionDownloadDelegate` is `@objc` NOT `NS_SWIFT_SENDABLE`, but conformance does NOT block derived `Sendable` on a `final class` with all-`let` Sendable props; the task captured in `Mutex<URLSessionDownloadTask?>` for cancel-with-resume), **no `@unchecked`**; `didFinishDownloadingTo` moves the temp file via `FileManager.moveItem` overwrite-safe; `didWriteData` → progress (guards `expected > 0`)) + `NebulaDownload` (`enum` namespace; `download(for:session:configuration:)`/`resume(from:session:configuration:)`/`pinned(by:sessionConfiguration:configuration:logger:)` → `PinnedDownloadSession` (reuses the N17a `NebulaURLSessionDelegate` as the **session** delegate — the per-task delegate holds NO pinning); custom retry-with-resume loop (NOT `NebulaRetry.withPolicy` — the resume `Data` mutates the attempt; `URLError.downloadTaskResumeData`; `onTermination → loop.cancel()`)) + `NebulaDownloadError` (open-struct, no new `NebulaError.Kind`). All N17c symbols below the `.v26` floor → **no `@available` gate** (`URLSession.upload`/`download` macOS 12/iOS 15/watchOS 8/tvOS 15/visionOS 1.0+; `URLError.downloadTaskResumeData` macOS 10.15/iOS 13/watchOS 6/tvOS 13; `cancel(byProducingResumeData:)` macOS 10.9/iOS 7/watchOS 2/tvOS 9 — completion-handler-only, wrapped in `withCheckedContinuation`). **Delegate routing:** per-task `delegate:` (download callbacks) vs session delegate (pinning — session-level auth challenge). **Consumer cancellation** surfaces via the loop (resumes the completion box with `CancellationError`), NOT via `withCheckedThrowingContinuation` auto-cancellation (which does NOT throw on Task cancel without a `withTaskCancellationHandler` bridge); the consumer's `for try await` over `progress` ends normally on cancel (N17b semantics), `value()` throws `CancellationError`. Foundation + Synchronization only (no `import Security` — pinning forwarded to the N17a delegate; no `import CryptoKit`; no `import Network`). **Testability limitation:** a `URLProtocol`-backed live round-trip over the async `download(for:delegate:)` temp-file path hangs (the overlay never completes); the live round-trip is compile-only, pure seams + delegate-lifecycle synthesized + race-safe completion box cover the surface (the N17a pragmatic stance). |
| `Async/NebulaPagedSequence` | Generic pagination (Wave N17c) — `public struct NebulaPagedSequence<Page: Sendable>: Sendable` (derived — constrain the generic at the declaration, the `NebulaResultPipeline<T: Sendable>` precedent; no `@unchecked`). `first: @Sendable () async throws -> Page` + `next: @Sendable (Page) async throws -> Page?`; `stream() -> AsyncThrowingStream<Page, any Error>` (the CLAUDE.md-mandated concrete return) — build-closure + `Task` + `onTermination → loop.cancel()`: yields pages until `next` returns `nil`, honors cancellation (the consumer's iteration ends normally on cancel — N17b semantics), surfaces `first`/`next` errors by finishing the stream throwing. **Custom loop, NOT `NebulaRetry.withPolicy`** — the cursor mutates per page (the SSE `Last-Event-ID` shape; `withPolicy` is nullary); no retry (pagination surfaces a failed page's error — retry is the app's concern via `withPolicy` around the closures if desired). The `first`/`next` closures decouple cursor transport (the app's concern — URL query item, header, or body token) from the loop (Nebula's concern). Foundation only; no `@available` gate (`URLComponents`/`URLQueryItem` far below the floor). DocC `ArchitectureBodiesDownloads.md`. |
| `Validation/` | `NebulaValidator<T>` (sync, short-circuit) + `NebulaAsyncValidator<T>` (async; a thrown I/O error is distinct from a validation failure). |
| `Registry/` | `NebulaRegistryKey` (open struct) + `NebulaRegistryConfiguration` + `NebulaRegistry` (explicit injection) + `NebulaRegistryConfig` (process-wide). DI **without** a container. |
| `Testing/` | In-target test doubles `NebulaFakeRepository`/`NebulaStubUseCase`/`NebulaSpyUseCase` (`final class` + `let Mutex`; `Sendable` **derived** — final class with all-`let` `Sendable` properties, no `@unchecked`). |
| `Async/` | `NebulaResultPipeline<T>` (`map`/`flatMap`/`recover` over `Result<T, NebulaError>`) + `AsyncSequence.nebulaChunked(byCount:)`/`nebulaUniqued(on:)` + `NebulaRetryPolicy`/`NebulaRetry`/`NebulaRetryJitter` (framework-agnostic async retry — full-jitter backoff, cancellation-aware, injectable sleeper). |
| `Presentation/` | Foundation-only navigation model: `NebulaRoute` (marker: `Hashable`/`Sendable`/`Codable` + an additive `presentationStyle` default `.push`, Wave N20), `NebulaNavigationStack<Route>` (the typed `[Route]` stack — single source of truth via `static` stack mutators), `NebulaRouter<Route>: Sendable` (**async** navigation-intent port), `NebulaPresentationStyle` (`.push`/`.sheet`/`.fullScreenCover`, Wave N20), `NebulaPresentation<Route>` (navigation-as-data model owning the push path + a single modal slot — present/dismiss in `static` `inout` helpers, single source of truth), `NebulaPresentationRouter<Route>: NebulaRouter` (sub-protocol adding async `present`/`present(_:as:)`/`dismiss`, Wave N20), `NebulaViewModel` (bare marker), `NebulaSpyRouter<Route>` (test double — conforms to `NebulaPresentationRouter`). No SwiftUI here — Meridian binds it. |
| `Preferences/` | `NebulaPreferences` (a `Sendable` key-value port: 3 byte-level requirements + a `Codable`/`RawRepresentable` default extension every conformer inherits) + `NebulaDefaults` (a `final class` `Mutex<UserDefaults>` façade; `UserDefaults` is `@_nonSendable(_assumed)` so the `Mutex` gives region-based isolation, the `sending` init transfers ownership; `Sendable` derived, no `@unchecked`). |
| `Keychain/` | The secure-storage seam (Wave N9): `NebulaSecureStore` (a `Sendable` port — three `throws` byte-level requirements + a `Codable`/`RawRepresentable` default extension; **distinct** from `NebulaPreferences`, different threat model) + `NebulaKeychain` (a **stateless `final class`** façade over the Security.framework `SecItem*` C API — no Swift object to region-isolate, so `Sendable` is **derived** with no `Mutex`/no `@unchecked`, the `NebulaError.Box` precedent NOT the `NebulaDefaults` `Mutex` precedent; fresh query per call, `SecItemUpdate` over delete-re-add, `errSecInteractionNotAllowed` non-destructive) + `NebulaKeychainConfig` (`.with*`, `service` required, `useDataProtectionKeychain` default `true`) + `NebulaKeychainAccessible` (enum → `kSecAttrAccessible*`, 5-platform) + `NebulaKeychainError` (per-layer open struct, `errSec*` → `.cocoa`, no new `Kind` case). Foundation + Security only; no `#if os()`; `Sendable` derived throughout. |
| `FeatureFlags/` | The feature-flag seam (Wave N14): `NebulaFlagValue` (a `Sendable`/`Equatable`/`Hashable` value enum: `.bool`/`.string`/`.int`/`.double`/`.json(Data)`, `CustomStringConvertible` derived; no `Codable` yet) + `NebulaFeatureFlags` (a `Sendable` port — ONE `value(forKey:) -> NebulaFlagValue?` requirement + a typed default extension `bool`/`string`/`int`/`double`/`number`/`json` every conformer inherits; the seam model mirrors `NebulaPreferences`; `json` uses per-call `JSONDecoder()` decoupled from `NebulaJSONDecoder`; only `json` throws) + `NebulaLocalFeatureFlags` (a `final class` `Mutex<[String: NebulaFlagValue]>` façade; the store is a Sendable value → init by value, **no `sending`**, unlike `NebulaDefaults`'s `UserDefaults`; derived `Sendable`, no `@unchecked`; `setValue(nil)` removes) + `NebulaRemoteFeatureFlags` (`: NebulaFeatureFlags` + `refresh() async throws` — app-supplied backend; `dependencies: []` forbids Firebase/LaunchDarkly) + `NebulaCompositeFeatureFlags` (a `Sendable` struct + `[any NebulaFeatureFlags]` + `withSource(_:)`, first-non-nil resolution; generic — the app wires `local > remote > defaults`). No process-wide accessor (constructed-and-passed, mirror `NebulaDefaults`); no new `NebulaError.Kind` case (flag errors surface as `DecodingError`). Foundation only; no `#if os()`; `Sendable` derived throughout. |
| `Notifications/` | The notifications seam (Wave N15a): `NebulaNotificationCenter` (a `Sendable` port — `requestAuthorization`/`add`/`cancel`/`cancelAll`/`pendingRequests`; the delegate-handler surface is **not** on the port, it lives on the config) + `NebulaUNNotificationCenter` (a `final class : NSObject, NebulaNotificationCenter, UNUserNotificationCenterDelegate, Sendable` delegate-adapter façade; the `@objc` `weak` delegate mandates the `NSObject` base; `Mutex<NebulaNotificationsConfiguration>` for the `@Sendable` `willPresent`/`didReceive` handlers; `UNUserNotificationCenter.current()` fetched **locally per method, not stored** — the non-Sendable singleton cannot be `sending`-consumed; `delegate` is `weak` → no `deinit`; `Sendable` derived, no `@unchecked`) + value types (`NebulaNotificationContent`/`Trigger`[`.timeInterval`/`.calendar`]/`Request`/`Response`/`PresentationOptions`/`AuthorizationOptions`, all Sendable derived, all-5) + `NebulaNotificationsConfiguration` (the 6th config, `Sendable`-not-`Equatable`, synchronous-returning `@Sendable` handlers) + `NebulaNotificationsConfig` (`Mutex` accessor) + `NebulaNotificationsError` (open-struct, `NebulaKeychainError` precedent, no new `NebulaError.Kind`). **Establishes the `#if !os(<platform>)` precedent** for SDK symbols `API_UNAVAILABLE` — `@available(tvOS, unavailable)` and `if #available(..., *)` are both empirically invalid for an already-unavailable symbol; the compile gate is the only mechanism (applied to the `didReceive` override + the content-property mapping). Foundation + `UserNotifications` + `Synchronization`; `Sendable` derived throughout. |
| `Permissions/` | The permission-status value (Wave N15a): `NebulaPermissionStatus` (an 8-case `Sendable`/`Equatable`/`Hashable`/`CaseIterable`/`CustomStringConvertible` union-superset enum of Apple's per-framework status vocabularies) + `init?(UNAuthorizationStatus:)` (`.ephemeral` is `#if os(iOS)` — App Clips). All-5, no gate. The unified `NebulaPermissions` request port (AV/CL/PH/ATT adapters) is deferred (app-level glue) — only the status value is hoisted now. |
| `BackgroundTasks/` | The background-tasks seam (Wave N15b): `NebulaBackgroundTaskScheduler` (a `Sendable` port — `register`/`submit`/`cancel`/`cancelAll`/`pendingRequests`; the launch-handler surface is **not** on the port, it lives on the config) + `NebulaBGTaskScheduler` (a `final class : NebulaBackgroundTaskScheduler, Sendable` façade, **no `NSObject`** — no `@objc` protocol; `BGTaskScheduler.shared` fetched **locally per method, not stored** — the non-Sendable singleton cannot be `sending`-consumed; `register` `using: nil` explicit; **macOS/watchOS-unavailable** via the type-level `@available(macOS, unavailable) @available(watchOS, unavailable)` declaration gate, the `NebulaLogStoreExporter` precedent) + `NebulaBackgroundTask` (the Sendable launch-time handle — identifier + kind + two `@Sendable` `complete`/`onExpiration` closures that capture the Sendable façade + identifier, **never** the non-`Sendable` `BGTask`, and reach it through the façade's `Mutex`) + `NebulaBGTaskBox` (a `final class @unchecked Sendable` reference-type box holding the non-`Sendable` `BGTask` as an immutable `let`, the `NebulaMemoryLogHandler` precedent — the plan's `Mutex<[String, BGTask]>`-no-`@unchecked` design hit a Swift 6 region-isolation wall) + value types (`NebulaBackgroundTaskKind`/`NebulaBackgroundTaskRequest`, all Sendable derived, all-5) + `NebulaBackgroundTaskConfiguration` (the 7th config, `Sendable`-not-`Equatable`, `@Sendable` `launch` handler) + `NebulaBackgroundTaskConfig` (`Mutex` accessor) + `NebulaBackgroundTaskError` (open-struct, `NebulaKeychainError` precedent, no new `NebulaError.Kind`; factory statics carry `underlying: NebulaError.Box?`). **Establishes the `@available(<platform>, unavailable)` declaration-gate precedent** (form c) — `#if canImport(BackgroundTasks)` is rejected (the framework is physically present on macOS/watchOS so it gates nothing; the symbols are `API_UNAVAILABLE`); `@available(macOS 26, unavailable)` is a syntax error. The 4-form gating taxonomy is consolidated. **`submit` dual-path** — iOS-27 async `submitTaskRequest(_:)` (`#if swift(>=6.4)` + `if #available(iOS 27, tvOS 27, visionOS 27, *)`) + iOS-26 deprecated sync `submit(_:)` fallback, warning-clean under `.v26` (deprecation fires only at target ≥ 27). Foundation + `BackgroundTasks` + `Synchronization`; the façade + handle `Sendable` **derived** (`@unchecked` isolated to the single reference-type box). |
| `Metrics/` + `Analytics/` + `CloudKit/` | CloudKit-backed observability suite (Wave N19) — ports + CloudKit adapters, opt-in via configuration, `dependencies: []` pristine (CloudKit is a system framework). **Metrics:** `NebulaMetrics` (port: `record(_:)` + counter/histogram/gauge/timing default exts) + `NebulaMetricEvent`/`NebulaMetricValue`/`NebulaMetricKind` + `NebulaEventBuffer<Event: Sendable & Equatable>` (final-class `Mutex<[Event]>` batcher) + `NebulaMetricsConfiguration` (Sendable-NOT-Equatable, `@Sendable` handler) + `NebulaMetricsConfig` (Mutex accessor) + `NebulaLocalMetrics`. **Analytics:** `NebulaAnalytics` (port: `track(_:)` + track/screen/identify default exts) + `NebulaAnalyticsEvent` + `NebulaAnalyticsConfiguration`/`NebulaAnalyticsConfig`/`NebulaLocalAnalytics` (reuses `NebulaEventBuffer`). **Performance sink:** `NebulaPerformanceSink` routes `NebulaMeasureResult` → `NebulaMetricsConfiguration` as a `.timing` event (no new timing API). **CloudKit glue:** `NebulaCloudKitEnvironment` (Nebula-owned Sendable Equatable enum — `CKContainer.Environment` does NOT exist as a Swift type in the Xcode 27 Beta 3 SDK) + `NebulaCloudKitConfiguration` (Sendable, Equatable; no handler) + `NebulaCloudKitConfig` (Mutex accessor) + `NebulaCloudKitSync` (port: `sendChanges`/`fetchChanges` + `sync()` default ext) + `NebulaCloudKitSyncEngine` (final class wrapping `CKSyncEngine`; `CKContainer`/`CKDatabase` constructed in `init` and discarded so their preconcurrency Sendability never reaches a Nebula derivation; delegate is a final class with `@Sendable` closures → Sendable via SE-0302; **no `@unchecked` authored**). **CloudKit-backed preferences (N19d):** `NebulaCloudKitPreferences` (conforms `NebulaPreferences`) — local synchronous cache + injectable async sink that flushes each `NebulaCloudKitKVChange` to CloudKit (default sink maps a key/value to a `CKRecord`, save/delete stateless per call — the `NebulaKeychain` precedent, no `CKDatabase` stored, Sendable derived with no `@unchecked`) + `ensureZone()`/`refresh()` extras. **CloudKit-backed feature flags (N19d):** `NebulaCloudKitFeatureFlags` (conforms `NebulaRemoteFeatureFlags`) — read-only: `value(forKey:)` serves a local `Mutex<[String: NebulaFlagValue]>` cache; `refresh()` pulls `NebulaFlag` records and replaces the cache (unchanged on failure — the port contract); stateless-per-call CloudKit boundary (no `CKDatabase` stored, Sendable derived with no `@unchecked`); injectable `fetch` (default `defaultFetch` is compile-verified — runtime needs iCloud); `encode(_:into:)`/`decode(from:)` public codec (a `kind` tag + per-kind field; pure, unit-testable with an in-memory `CKRecord`, no iCloud). **Admissibility:** `CKContainer`/`CKDatabase`/`CKSyncEngine` are clean `Sendable` on all 5 platforms below the `.v26` floor → **no `@available` gate, no `#if os()`** (verified against `CloudKit.swiftmodule`); the `@unchecked` CK value types (`CKRecord`/`CKQuery`/`CKShare`/…) are **Apple classes**, not Nebula value types — handled by the `final class @unchecked` box + copy-to-Sendable at the adapter boundary (the `NebulaMemoryLogHandler`/`NebulaLogStoreExporter` precedent). Foundation + CloudKit + Synchronization; `Nebula*Configuration` per-call or process-wide `Mutex` accessor (the canonical config shape); open-struct errors reuse existing `NebulaError.Kind` (no new case). |

All public value types derive `Sendable`. The toolkit introduces **no** `@unchecked Sendable`: the two `final class` test-helpers (`NebulaFakeRepository`, `NebulaSpyUseCase`) derive `Sendable` (a `final class` with all-`let` `Sendable` properties synthesizes `Sendable`; `Mutex` is `Sendable` when its value is), needing no `@unchecked`. They are `final class` rather than `struct` because a `Mutex`-typed stored property propagates `~Copyable` to an owning *struct* — a non-copyable double is awkward to pass around a test; the class absorbs the `~Copyable` `Mutex` behind a copyable reference (the `NebulaError.Box` derived-Sendable-`final class` precedent, not the `NebulaMemoryLogHandler` `@unchecked` exception). The 10 locked design decisions and the consolidated risks live in the vault (`03-padroes/nebula-clean-architecture-toolkit.md`, `08-riscos/clean-architecture-toolkit-risks.md`, `08-riscos/clean-architecture-open-questions.md`); the ADR is in `DECISIONS.md`.

<a name="presentation-meridian"></a>

## Presentation (Meridian)

The presentation architecture is **MVVM `@Observable` + native typed-`[Route]` Router pattern** (no Coordinator tree — owner preference). It is split across two packages so the Clean Architecture dependency rule is **compiler-enforced**, not convention:

- **Nebula** ships the Foundation-only **navigation model** under `Sources/Nebula/Architecture/Presentation/`: `NebulaRoute` (the route contract: `Hashable`/`Sendable`/`Codable` + an additive `presentationStyle` default `.push`), `NebulaNavigationStack<Route>` (the typed `[Route]` stack — the "real navigation model" that holds push/pop/popToRoot/replaceStack logic once, in `static` mutators, with instance API delegating), `NebulaRouter<Route>: Sendable` (the **async** navigation-intent port), `NebulaPresentationStyle` (`.push`/`.sheet`/`.fullScreenCover`) + `NebulaPresentation<Route>` (the navigation-as-data model owning the push path + a single modal slot; present/dismiss in `static` `inout` helpers — single source of truth) + `NebulaPresentationRouter<Route>: NebulaRouter` (sub-protocol adding async `present`/`present(_:as:)`/`dismiss`) — all Wave N20, `NebulaViewModel` (a bare `Sendable` marker — Nebula ships no `@Observable`), and `NebulaSpyRouter<Route>` (a `Mutex`-backed test double — conforms to `NebulaPresentationRouter`). None of this imports SwiftUI.
- **Meridian** (`Meridian/`, a separate local SwiftPM package depending on Nebula via `../`) ships the SwiftUI binding: `@MainActor @Observable public final class Router<Route: NebulaRoute>: NebulaPresentationRouter<Route>` (observable `path`/`presented`/`presentedStyle`; push-path intents delegate to `NebulaNavigationStack` statics, modal intents to `NebulaPresentation` statics), `MeridianNavigationStack` (a `NavigationStack(path:)` + `navigationDestination(for:)` wrapper with `.sheet(isPresented:)`/`.fullScreenCover(isPresented:)` gated bindings driven by the single modal slot), `MeridianNavigationSplitView` (a `NavigationSplitView` with a sidebar + a `MeridianNavigationStack` detail — subsumes the deprecated `NavigationView`), and `MeridianTabView` (`TabView` with `Tab(value:)`, one `Router` per tab). One `Router`/`MeridianNavigationStack` per tab.

**Why the port is async.** Nebula has no default actor isolation (no SwiftUI → no `@MainActor`), yet the concrete `@MainActor @Observable` Meridian `Router` must conform to a Nebula port. The fix is the canonical Swift 6 idiom: `NebulaRouter`'s requirements are `async`; a synchronous `@MainActor` method **witnesses a nonisolated async requirement** (the `await` performs the actor hop). This keeps Nebula `@MainActor`-free while letting the on-actor `@Observable` Router conform — no SE-0411 isolated conformances (experimental/above-floor), no `@unchecked Sendable` on the Router (it is `Sendable` by `@MainActor` isolation). The async port is also the **cross-actor bridge**: a non-MainActor deep-link parser can `await router.replaceStack(with:)` to drive the UI. `NebulaPresentationRouter`'s `present`/`dismiss` are async for the same reason; sync concrete impls (the spy) still witness them.

**Why a sibling package.** Meridian is a *separate* package so `import Meridian` from inside Nebula is an unconditional hard compile error — the Clean Architecture rule "use cases / domain never import presentation" is enforced across packages (SR-1393 only blocks cross-module access *within one package's* `.build`; a separate module graph defeats it). This closes the Wave H open risk. Meridian lives as a subdir package (`Meridian/`, `path: "../`) in this repo — one repo, one CI; promoting it to its own public git repo is a documented future step. Versioning: Meridian N ↔ Nebula N ↔ OS N, in lockstep (`VERSIONING.md`).

**Per-route presentation styles + the modern container trio (Wave N20).** A route declares **how it is presented** via `NebulaRoute.presentationStyle` — `.push` (the default), `.sheet`, or `.fullScreenCover`; `router.present(route)` dispatches by the declared style (push → path; modal → the single `presented` slot), and `router.present(route, as: .sheet)` overrides at the call site. `dismiss()` clears an active modal, else pops one — never both. The Meridian adapter wires the modal slot to `.sheet(isPresented:)`/`.fullScreenCover(isPresented:)` (not `.sheet(item:)` — `Route` need not be `Identifiable`); `fullScreenCover` is gated `#if !os(macOS)` with a `.sheet` fallback on macOS (the API is unavailable there). The trio — `NavigationStack` (push) + `NavigationSplitView` (split) + `TabView` (`Tab(value:)`, one `Router` per tab) — is bound to the router, pickable per screen area; `NavigationView` is deprecated and not used.

Two patterns the foundation enables (DocC article `Meridian.docc/NavigationPatterns.md`, runnable `MeridianExample` executable as compile gate): **deep link as data** — a pure `URL → [Route]` function, asserted as a value, then `router.replaceStack(with:)`; and **type-driven modal destinations** — a single `Optional<Destination>` enum drives `sheet(item:)`, so "edit sheet AND delete alert showing" is a state the compiler refuses (no `@CasePathable` macro — `dependencies: []`). Wave N20 adds the route-driven modal: a route declared `.sheet`/`.fullScreenCover` presents modally through the same `destination` resolver. Vault: `03-padroes/nebula-presentation-architecture.md`, `nebula-presentation-seams.md`, `nebula-meridian-router.md`, `nebula-presentation-destinations-deeplink.md`; risks `08-riscos/presentation-architecture-risks.md`; ADR in `DECISIONS.md`.

<a name="data-network-aurora"></a>

## Data + Network (Aurora)

The data + network surface is split by **binding rule**, mirroring the presentation split. `URLSession` and `UserDefaults` are Foundation → they ship **in Nebula** (no binding-rule tension). SwiftData's `@Model` classes and `ModelContext` are **not `Sendable`** (verified against the Xcode 27 Beta 3 SDK) and the `@ModelActor` form wants its own module graph → a gated Nebula helper would re-create the Meridian tension already resolved by splitting packages, so SwiftData ships in a new sibling package, **Aurora**.

- **Network (in Nebula).** `NebulaRetryPolicy`/`NebulaRetry`/`NebulaRetryJitter` (`Architecture/Async/`) — a framework-agnostic retry loop for any `async throws` operation: `Sendable` value (NOT `Equatable` — stores a `@Sendable (any Error) -> Bool` `isRetriable` predicate so HTTP 5xx/408/429 retry alongside transport `URLError`), exponential backoff with full/equal jitter, cancellation-aware (`Task.checkCancellation()`; a `CancellationError` is never retried), injectable sleeper for tests. `NebulaHTTPGateway` (`Architecture/Gateway/`) is the concrete `NebulaGateway` over `URLSession` — `get`/`post`/`put`/`delete`, retries via `NebulaRetry`, bridges `URLError` + HTTP status to `NebulaError` (kind `.network`, built explicitly via `NebulaError(code:kind:)` — **no new `Kind` case**) reported through the config's handler. Waves N5–N8 rework this into a proper Clean-Architecture surface (`Architecture/Network/`): `NebulaHTTPEndpoint` (port) + `NebulaHTTPRequest`/`NebulaHTTPBody`/`NebulaHTTPResponse` + `NebulaHTTPClient` (a **non-generic** port refining `NebulaGateway` — `send(_:) -> NebulaHTTPResponse` is the one transport requirement, existential-friendly; the verbs are default extensions, so Wave N1 call sites are backward-compatible); a per-endpoint cache — `NebulaHTTPCachePolicy` + `NebulaHTTPCache` port + `NebulaURLCache` façade (Nebula owns TTL / stale-while-revalidate metadata, the native `URLCache` holds the bytes; `URLCache` is not `Sendable` so a `Mutex<State>` + `final class` derives `Sendable` with no `@unchecked`); and a simple local HTTP/1.1 server — `NebulaHTTPServer` over `NWListener`/`NWConnection` (Foundation + Network; `Sendable` derived) + `NebulaHTTPRequestParser` (bounded) + `NebulaHTTPServerError` (per-layer open struct bridging to `NebulaError`).
- **Preferences (in Nebula).** `NebulaPreferences` (`Architecture/Preferences/`) — a `Sendable` key-value port with three byte-level requirements (`data`/`setData`/`remove`) plus a **default extension** giving every conformer a `Codable` bridge and a `RawRepresentable` bridge. `NebulaDefaults` is the concrete façade over `UserDefaults`: a `final class` wrapping `Mutex<UserDefaults>` (region-based isolation — `UserDefaults` is `@_nonSendable(_assumed)` in Swift 6; the `Mutex` is the alternative to `@unchecked`), with a `sending` init (SE-0430) that transfers ownership at the call site. `Sendable` derived, no `@unchecked`.
- **Secure storage (in Nebula).** `NebulaSecureStore` (`Architecture/Keychain/`) — a `Sendable` secure-storage port with three **`throws`** byte-level requirements (`data`/`setData`/`remove`) plus the same `Codable`/`RawRepresentable` default extension. `data(forKey:)` returns `nil` for an absent key and `throws` for a genuine failure (auth failed, device locked, missing entitlement), so a caller can distinguish "no secret" from "could not read the secret". It is a **distinct** port from `NebulaPreferences` — secrets and user-tunable preferences have different threat models; an app injects a secure store and a preferences store as separate seams. `NebulaKeychain` is the concrete façade over the Security.framework `SecItem*` C API: a **stateless `final class`** with `let config: NebulaKeychainConfig`. Unlike `NebulaDefaults` (which wraps a non-`Sendable` `UserDefaults` object in a `Mutex`), the `SecItem*` calls are thread-safe free functions with **no Swift object to region-isolate**, so `Sendable` is **derived** with no `Mutex` and no `@unchecked` (the `NebulaError.Box` precedent, not the `NebulaDefaults` `Mutex` precedent). Fresh query dict per call; `setData` updates in place first (`SecItemUpdate` over delete-re-add, preserving access control); `errSecInteractionNotAllowed` (device locked) is non-destructive — no path deletes on this error; `remove` is idempotent. `NebulaKeychainError` is a per-layer open struct bridging `errSec*` OSStatus codes to the existing `NebulaError.Kind.cocoa` (no new `Kind` case); the raw `OSStatus` (`Int32`) is stored, not boxed. Biometry (`SecAccessControlCreateWithFlags` + `LAContext`) is deferred — `LAContext` is `API_UNAVAILABLE(tvOS)` at the class level. Wave N9; vault `03-padroes/nebula-keychain.md`.
- **HTTP interceptors + 401 refresh-and-retry (in Nebula).** `NebulaHTTPInterceptor` (`Architecture/Network/`) — a `Sendable` interceptor port with two phases: `adapt(_:)` (runs before every send; may transform the `NebulaHTTPEndpoint`) and `retry(_:for:attempt:)` (runs after a throw; return a fresh endpoint to retry with, `nil` to decline, or throw to abort and surface that error). `NebulaRetry.withPolicy` cannot mutate the request between attempts, so a 401 cannot refresh-and-retry through that seam — the interceptor port fills the gap. `NebulaHTTPInterceptorChain` composes interceptors left-to-right, sends once, and on failure offers each a **single** retry chance built from the **original** endpoint (no double-wrap); `CancellationError` is rethrown before the retry pass. `NebulaInterceptedClient` + `NebulaHTTPClient.intercepted(by:)` wire a chain around a client (the verbs funnel through unchanged). `NebulaTokenProvider` is a PAT (erased `Token: Sendable`) the app conforms to supply credentials (`currentToken()`/`refresh()`/`authorizationHeader(for:)`). `NebulaAuthInterceptor` is the concrete interceptor and **Nebula's first `actor`** — `adapt` injects `Authorization: Bearer <token>` (anonymous passthrough when there is no session); `retry` matches the 401 `NebulaError` `NebulaHTTPGateway` surfaces (domain `"Nebula.HTTP"`, code `401`) at `attempt == 0`, refreshes **single-flight** via an actor-isolated `Task<Token, any Error>?` (the first 401 refreshes; concurrent 401s `await` the same in-flight task), and retries once. The header is injected at the `URLRequest` layer via a private endpoint wrapper (works because `NebulaHTTPGateway.buildRequest` only fills config headers for absent fields). The interceptor is **transparent**: no new `NebulaError.Kind` case — it rethrows the client's errors and surfaces a refresh failure's app-supplied provider error in place of the 401. Wave N10; vault `03-padroes/nebula-auth-interceptor.md`.
- **SSL/TLS public-key pinning (in Nebula).** `NebulaSSLPinning` (`Architecture/Network/Pinning/`, Wave N17a) is a `Sendable` policy value type — per-host SPKI SHA-256 pin sets (`NebulaSSLPinningPin`, `NebulaSSLPinning.HostPins`) plus `includeSubdomains` (parent-domain walk, case-insensitive per RFC 1035), `validateChainFirst` (default `true` — pinning is **additive to system trust**, never replaces the OS trust store), and `failClosedForUnknownHosts` (default `true`), with fluent `.with*` builders. `NebulaSSLPinningEvaluator` is a **pure** trust evaluator (`static evaluate(trust:host:policy:)`) running the OWASP "any position" SPKI algorithm — match any cert in the chain (leaf or intermediate/CA — survives leaf rotation); `validateChainFirst` runs `SecTrustEvaluateWithError` first, then copies the chain via `SecTrustCopyCertificateChain` and SHA-256s each cert's public-key DER via the existing `Data.nebulaDigest(of: .sha256)` (**no new `import CryptoKit`**); a total extraction failure surfaces as `NebulaSSLPinningResult.spkiExtractionFailed`, a single un-extractable key is skipped. `NebulaURLSessionDelegate` is the `final class : NSObject, URLSessionDelegate, Sendable` façade — `Sendable` is **derived** (only `let` Sendable stored props), **no `@unchecked`**, the `NebulaUNNotificationCenter` precedent; the `@objc optional` completion-handler method is a thin guard → evaluate → log → map → completion, with the disposition mapping factored as the `internal disposition(for:policy:trust:)` testable seam (`.matched → .useCredential`; `.noPinForHost` fail-closed/open; failures `.cancelAuthenticationChallenge`). The delegate does NOT throw — on failure it logs + `.cancelAuthenticationChallenge`, and `URLSession` surfaces a `URLError` to `NebulaHTTPGateway`, which already bridges `URLError → NebulaError(urlError:)` (**no gateway change**). **Pinning is transport-layer, NOT an interceptor** — the interceptor surface shipped in N10; a pin failure surfaces as `URLError` from `URLSession` *before/under* `data(for:)`, so trust eval belongs at `URLSessionDelegate`, not in an `adapt`/`retry` interceptor. `NebulaHTTPSession.pinned(by:configuration:logger:)` returns a `NebulaPinnedSession` carrying BOTH the `URLSession` and the delegate (`URLSession` does NOT retain its delegate — the caller retains the pair); a caller composes a pinned `NebulaHTTPGateway` with zero gateway changes (the gateway already accepts an opaque `session: URLSession`). `NebulaSSLPinningError` is an open-struct error mirroring `NebulaHTTPServerError` — **no new `NebulaError.Kind` case**. `import Security` is in-bounds (the Keychain precedent; the research's open Q is resolved); the Security framework has no `.swiftinterface` (C/Obj-C via `module.modulemap`, headers are ground truth — WebFetch hallucinates availability), and all `Sec*` symbols are below the `.v26` floor → **no `@available` gate**. Foundation + Security + Synchronization only; no `#if os()`; `Sendable` derived throughout. Testability: the pure evaluator is unit-tested on the macOS host with a synthetic `SecTrust` built from a self-signed RSA cert embedded as a `[UInt8]` literal (no SPM `resources:`); the delegate method is NOT round-trip-testable (`URLProtectionSpace.serverTrust` not injectable via the public init) → the `disposition(for:policy:trust:)` extraction covers the logic (mirrors `NebulaUNNotificationCenter`). Wave N17a; vault `03-padroes/nebula-ssl-pinning.md`.
- **Streaming — SSE + WebSocket (in Nebula).** `Architecture/Network/Streaming/` (Wave N17b) ships two **additive** streaming surfaces over the existing buffered HTTP gateway — the gateway is untouched (Option A: a caller already injects an opaque `session: URLSession`), and pinning is reused via **composition/forwarding** (the new `NebulaWebSocketSessionDelegate` holds a `NebulaURLSessionDelegate` from N17a and forwards the auth-challenge method — zero N17a source change, zero pinning-logic duplication; the auth-challenge method lives on `URLSessionDelegate`, which `URLSessionWebSocketDelegate` inherits, so the **same** session delegate handles pinning for both HTTP(SSE) and WebSocket). No `NebulaGatewayConfiguration.streaming`/`.sse`/`.webSocket` field (streaming configs are per-call, not process-wide → **no `Mutex` accessor**, unlike logging/measurement/error). **SSE:** `NebulaSSEEvent` (`Sendable`/`Equatable`/`Hashable`: `id`/`event` (default `"message"`)/`data` (multi-line joined `\n`, trailing newline stripped per WHATWG)/`retry: Int?`) + a **pure** `internal NebulaSSEParser` (`@discardableResult mutating feed(_ line: String) -> NebulaSSEEvent?` — no `URLSession`, no `@Sendable` closure, no I/O; the testable seam) + `NebulaSSEConfiguration` (`Sendable` **NOT `Equatable`** — the `@Sendable` sleeper, mirroring `NebulaLogConfiguration`) + `NebulaSSEEventStream.events(for:session:configuration:) -> AsyncThrowingStream<NebulaSSEEvent, any Error>` (the CLAUDE.md-mandated concrete return — `some AsyncSequence` is illegal in a protocol requirement). **Critical: the stream iterates the raw `bytes` NOT `.lines`** — `URLSession.AsyncBytes.lines` (`AsyncLineSequence`) **skips empty lines**, but SSE dispatches on **blank lines**, so `.lines` would drop every dispatch boundary; the stream accumulates a `Data` line buffer and feeds the parser on `\n` (0x0A) including empty lines (an empty buffer on `\n` is the blank line that dispatches). The reconnect loop is **custom, NOT `NebulaRetry.withPolicy`** — the request mutates per attempt (the `Last-Event-ID` header advances with the cursor), and `withPolicy`'s `operation` is nullary; the custom loop mirrors `withPolicy`'s cancellation contract (cancellation honored immediately, **never retried**). **`AsyncThrowingStream` cancellation correction:** the consumer's `for try await` ends **normally** on consumer cancel (`Iterator.next()` returns `nil` — does NOT throw `CancellationError`); the internal `finish(throwing: CancellationError())` tears the `bytes(for:)` task down via `onTermination`, but the consumer never observes it (the plan/vault's "CancellationError rethrown" was incorrect, verified empirically). **WebSocket:** `NebulaWebSocketMessage` (a Nebula-owned `case data`/`case string` enum mirroring `URLSessionWebSocketTask.Message` — never expose Apple's nested enum in a Nebula port; failable `init?(_:)` with `@unknown default: return nil` for the non-frozen enum → a future case surfaces as `NebulaWebSocketError.unknown`) + `NebulaWebSocketClient` (a `Sendable` port — mirror `NebulaHTTPClient` without the gateway inheritance; WebSocket is its own axis, not request/response) + `NebulaURLSessionWebSocket` (`final class : NebulaWebSocketClient, Sendable` façade over `URLSessionWebSocketTask`; `Sendable` **derived** — `URLSessionWebSocketTask` IS `NS_SWIFT_SENDABLE` (`NSURLSession.h:1121`, correcting the prior research note that said "NOT Sendable"), so the `let task` derives Sendable, **no `@unchecked`**; `sendPing` is wrapped in `withCheckedThrowingContinuation` (only the completion-handler form exists — no async overlay)) + `NebulaWebSocketSessionDelegate` (`final class : NSObject, URLSessionWebSocketDelegate, Sendable` — ONE object per session: pinning via composition + WebSocket lifecycle (`didOpenWithProtocol`/`didCloseWith`); `Sendable` **derived** — probe EXIT=0, the `NebulaUNNotificationCenter` analogy: `URLSessionWebSocketDelegate` is an `@objc` protocol NOT `NS_SWIFT_SENDABLE`, but conformance to a non-Sendable `@objc` protocol does NOT block derived `Sendable` on a `final class` with all-`let` Sendable props; **no `@unchecked`**, no `import Security` here) + `NebulaPinnedWebSocketSession`/`NebulaWebSocketSession` (`pinned(by:)` returns BOTH the `URLSession` and the delegate — `URLSession` does NOT retain its delegate, same footgun as `NebulaPinnedSession`; `open(url:using:)`/`open(request:using:)` is the ergonomic entry point). `NebulaSSEError` + `NebulaWebSocketError` are open-struct errors mirroring `NebulaHTTPServerError`/`NebulaSSLPinningError` — **no new `NebulaError.Kind` case**; `coarseKind` switches to existing `.network`/`.unknown` with `default → .unknown`; `NebulaWebSocketError.closed(code:reason:)` carries the `CloseCode` in metadata + reason in message. All streaming APIs (`URLSession.bytes`/`URLSessionWebSocketTask`/`URLSessionWebSocketDelegate`/`NWListener`/`NWProtocolWebSocket`) are below the `.v26` floor on every platform → **no `@available` gate**. Foundation only in the product target (the delegate + session builder reuse N17a, no `import Security` here); `import Network` only in the test (an `NWListener` echo server — `URLProtocol` does NOT intercept `URLSessionWebSocketTask`, so the WebSocket live round-trip needs a real listener; `Network.framework` is admissible, the `NebulaHTTPServer` precedent; the echo `send` MUST carry `NWProtocolWebSocket.Metadata(opcode:)` in the `ContentContext` or the frame is invalid). Wave N17b; vault `03-padroes/nebula-streaming.md`. The DocC article `Nebula.docc/ArchitectureCompositionRoot.md` (Wave N11, indexed in `Architecture.md` after `ArchitectureRegistry`) names what the existing "Dependency injection without a framework" paragraph leaves implicit: how the app assembles its object graph at launch. The composition root is the **app's job** (at `@main`/`App.init`; there is no Apple DI framework — community consensus, not an Apple-published position), built via explicit constructor injection of `Sendable` values — `NebulaRegistryConfiguration` via `.withFactory(for:_:)`, `NebulaRegistryConfig.set(…)` once, resolve concrete adapters, inject them as explicit parameters into the `NebulaUseCase` `@Sendable` body, decorate with `.instrumented()`, and hand the use case to a `@MainActor @Observable` viewmodel (which lives in the Meridian sibling — forbidden in Foundation-only Nebula). The article carries the guardrail "do not extend `NebulaRegistry` toward singleton/scoping/auto-injection — it is a factory map, not a container" (scope-creep risk #3) and the `@MainActor`-factory runtime-isolation caveat (Factory#322: keep registry factories `nonisolated @Sendable`, let the app do the actor hop). **No new source type** — a `NebulaCompositionRoot`/`NebulaAppContainer` helper is deliberately not shipped (trivial = redundant, non-trivial = drifts toward a container). The runnable `@MainActor @Observable` vertical is the follow-up N11b in a sibling. Wave N11; vault `03-padroes/nebula-composition-root.md`.
- **Bodies & downloads — multipart + download + pagination (in Nebula).** `Architecture/Network/Bodies/` (Wave N17c) ships three **additive** surfaces over the existing buffered HTTP gateway — the gateway is untouched (`NebulaHTTPGateway`/`NebulaHTTPClient`/`NebulaHTTPBody`/`NebulaHTTPRequest`/`NebulaHTTPRequestParser` diffs empty), and pinning is reused via **composition** (zero N17a source change). No `NebulaGatewayConfiguration.bodies`/`.download`/`.multipart` field (the download config is per-call, not process-wide → **no `Mutex` accessor**, unlike logging/measurement/error). **Multipart:** `NebulaMultipartBuilder` is a pure `multipart/form-data` (RFC 2388) `Data` composer — **Foundation has NO multipart API** (a full grep of `Foundation.swiftinterface` returns zero hits), so this is genuinely new app-layer code, not a wrapper. `NebulaMultipartPart` (field/file) is assembled under a random boundary (16 random bytes hex-encoded under `"----NebulaBoundary"`, within RFC 2046 §5.1.1's 0–70-char limit — **no `import CryptoKit`**, multipart doesn't hash). `build()` is **pure** (no `URLSession`, no `@Sendable` closure, no I/O except the explicit `file(in:)` temp-file writer for `URLSession.upload(for:fromFile:)` — streaming from disk, suitable for large/background uploads). **Gateway-compatible by design**: the built `Data` + content-type feed the existing `NebulaHTTPBody.data(_:contentType:)` — **no new `NebulaHTTPBody` case**, no `NebulaHTTPRequest`/parser ripple. **Download:** `NebulaDownload.download(for:session:configuration:)` → `NebulaDownloadHandle` — a façade over `URLSession.download(for:delegate:)` (a temp-file URL deleted on return — the façade moves it via a caller-supplied `destination` closure). The handle exposes `progress: AsyncThrowingStream<Double, any Error>` (fraction 0.0–1.0, bridged from `URLSessionDownloadDelegate.didWriteData` — Foundation has NO async `for await` progress API, verified) and `value() async throws -> URL` (the moved destination). `cancelByProducingResumeData()` wraps the completion-handler-only `URLSessionDownloadTask.cancel(byProducingResumeData:)` in a `withCheckedContinuation` (no async form; the task is captured in the delegate's `Mutex<URLSessionDownloadTask?>` since the async overlay does not expose it). The retry-with-resume loop is **custom, NOT `NebulaRetry.withPolicy`** — the resume `Data` mutates the attempt (`download(resumeFrom:)`), and `withPolicy`'s `operation` is nullary (the SSE `Last-Event-ID` precedent); failure-path resume reads `URLError.downloadTaskResumeData`. **Delegate routing (load-bearing):** the per-task `delegate:` passed to `download(for:delegate:)` receives the `URLSessionDownloadDelegate` callbacks; SSL/TLS pinning rides the **session** delegate (the server-trust auth challenge is a session-level `URLSessionDelegate` method). So `NebulaDownloadDelegate` (`final class : NSObject, URLSessionDownloadDelegate, Sendable` — `Sendable` **derived**, probe EXIT=0, the N17b analogy: `URLSessionDownloadDelegate` is `@objc` NOT `NS_SWIFT_SENDABLE`, but conformance does NOT block derived `Sendable` on a `final class` with all-`let` Sendable props; **no `@unchecked`**) holds NO pinning — `pinned(by:sessionConfiguration:configuration:logger:)` sets the N17a `NebulaURLSessionDelegate` as the session delegate (**zero N17a change, no `import Security` in the product target**). **Consumer cancellation** surfaces via the **loop** (it resumes the completion box with `CancellationError` from `Task.checkCancellation()`/the `URLError(.cancelled)` branch), NOT via `withCheckedThrowingContinuation` auto-cancellation (which does NOT throw on Task cancel without a `withTaskCancellationHandler` bridge — verified, a test that relied on it deadlocked); the consumer's `for try await` over `progress` ends normally on cancel (N17b semantics), `value()` throws `CancellationError`. The race-safe `NebulaDownloadCompletion` (pending/awaiting/resolved `Mutex<State>`) handles the delegate-resolves-before-`value()`-registers case. **Pagination:** `NebulaPagedSequence<Page: Sendable>` (`Architecture/Async/`) — a generic `Sendable` helper returning `AsyncThrowingStream<Page, any Error>` (the CLAUDE.md-mandated concrete return). `first`/`next` `@Sendable` closures decouple cursor transport (the app's concern — URL query item, header, or body token) from the loop (Nebula's concern), stopping when `next` returns `nil`. Custom loop, NOT `withPolicy` (the cursor mutates per page — the SSE `Last-Event-ID` shape); no retry (pagination surfaces a failed page's error). `Sendable` derived (constrain `<Page: Sendable>` at the declaration, the `NebulaResultPipeline<T: Sendable>` precedent; no `@unchecked`). `NebulaMultipartError` + `NebulaDownloadError` are open-struct errors mirroring `NebulaSSEError` — **no new `NebulaError.Kind` case**; `coarseKind` switches to existing `.network`/`.unknown` with `default → .unknown`. All N17c symbols are below the `.v26` floor on every platform → **no `@available` gate** (`URLSession.upload`/`download` macOS 12/iOS 15/watchOS 8/tvOS 15/visionOS 1.0+; `URLError.downloadTaskResumeData` macOS 10.15/iOS 13/watchOS 6/tvOS 13; `cancel(byProducingResumeData:)` macOS 10.9/iOS 7/watchOS 2/tvOS 9; `URLComponents`/`URLQueryItem` far below). Foundation + Synchronization only (no `import Security` — pinning forwarded to the N17a delegate; no `import CryptoKit`; no `import Network`). **Testability limitation:** a `URLProtocol`-backed live round-trip over the async `download(for:delegate:)` temp-file path hangs (the overlay never completes — verified empirically); mirrors the N17a pragmatic stance — the pure logic + delegate-lifecycle synthesized + resume-data extraction seams + race-safe completion box cover the surface, the live round-trip is compile-only. Wave N17c; vault `03-padroes/nebula-bodies-downloads.md`. The N17 split is now complete (N17a → N17b → N17c).
- **User-error bridge (in Nebula).** `NebulaUserError` (`Architecture/Errors/`) is a Foundation-tier user-facing error value — `message` + `recoveryActions: [RecoveryAction]` + `helpAnchor` (`Sendable`/`Equatable`/`Hashable`) — that the presentation layer (Cosmos / the app) renders, produced by a configuration-layer map from the closed `NebulaError` envelope: `NebulaErrorConfiguration.withUserMessageMap(_:)` installs a `@Sendable` map `(NebulaError.Kind, [String: String]) -> NebulaUserError?` (the dictionary is `NebulaError.metadata`), default `{ _, _ in nil }` (opt-in); `.userError(for:)` resolves it (also `NebulaErrorConfig.userError(for:)`, the process-wide convenience) and is **not** gated on `isEnabled` (user-message mapping is orthogonal to reporting). `NebulaUserError.default(for:context:)` ships an English fallback per `Kind` with HIG-neutral tone (overridable; L10n via `String(localized:)` at the app layer). `RecoveryAction` (`.retry`/`.cancel`/`.dismiss`/`.custom(String)`) is Nebula-authored because Apple's `RecoverableError` is closure-based. **Mapping only — no new `NebulaError.Kind` cases**; `NebulaUserError` is the **opposite direction** from `NebulaFailure` (envelope → user value, not layer → envelope), so it is a plain value, **not** a `NebulaFailure`. Refuted/deferred: `RecoveryURL` (not public Apple API); `NebulaError: RecoverableError` (closure-based — app/Cosmos opt-in); a `NebulaErrorPresenter` port (Cosmos-only). DocC `ArchitectureUserError.md` (indexed after `ArchitectureErrors`). Wave N12; vault `03-padroes/nebula-user-error.md`.
- **Environment value + reader (in Nebula).** `NebulaEnvironment` (`Environment/`) is a closed `String`-backed enum (`.development`/`.staging`/`.production`; `default = .production` — safe-fail-to-production) that round-trips the app's `Info.plist` configuration string. Apple provides no Foundation `Environment` value type — the idiom is the Xcode `Configuration` build setting fed from `.xcconfig` + schemes and written into `Info.plist` (a key, conventionally `Configuration`, set from `$(CONFIGURATION)`); Nebula ships the **value + reader**, not the wiring. `NebulaEnvironment.fromBundle(_:key:)` reads `object(forInfoDictionaryKey:)` (default key `"Configuration"`, default `bundle .main`), casts `Any? → String?` **before** crossing isolation (the `infoDictionary` values are `Any` and not Sendable — the load-bearing Sendability step), and resolves to `.default` on an absent/unknown/non-`String` value (never `nil`); it is a pure function over the Sendable `Bundle` — no `Mutex` (unlike `NebulaDefaults` over the non-Sendable `UserDefaults`). `NebulaEnvironmentConfiguration` (the fifth cross-cutting config contract, `Sendable`-not-`Equatable` — family posture) carries the resolved environment + per-environment `baseURLs: [NebulaEnvironment: URL]` + string `overrides: [String: String]`; `baseURL(for:)` returns `nil` for an unregistered environment (Nebula ships no built-in URLs). `NebulaEnvironmentConfig` is the `Mutex` accessor (process-wide ergonomics) alongside explicit-parameter DI (the two-path rule). Naming `Configuration`/`Config` split mirrors the four-family convention (avoids the `NebulaEnvironmentConfigConfig` stutter). Deferred (app-tier): `.xcconfig`/scheme/`Info.plist` wiring; a `ProcessInfo`-based reader (documented alternative — the `Info.plist`-keyed reader is the only one shipped); a reader façade (a value builder suffices). DocC `Environment.md` (top-level, indexed in `Nebula.md` after `Standardize`; added to `### Configuration contracts`). Wave N13; vault `03-padroes/nebula-environment.md`.
- **Feature flags (in Nebula).** `NebulaFlagValue` (`Architecture/FeatureFlags/`) is a `Sendable`/`Equatable`/`Hashable` value enum (`.bool`/`.string`/`.int`/`.double`/`.json(Data)`, `CustomStringConvertible` derived; no `Codable` yet) — the storage representation a flag carries. Apple provides no Foundation remote-config or feature-flag API (zero `FeatureFlag`/`RemoteConfig`/`RolloutConfig` hits in `Foundation.swiftmodule`), and `dependencies: []` forbids Firebase/LaunchDarkly, so a Nebula remote flag is necessarily a **port** the app conforms. `NebulaFeatureFlags` is a `Sendable` port with ONE requirement `value(forKey:) -> NebulaFlagValue?` plus a default extension of typed accessors (`bool`/`string`/`int`/`double`/`number`/`json`) every conformer inherits — the seam model mirrors `NebulaPreferences`; `int`/`double` return only their own case (no silent coercion), `number` coerces either to `Double`, `json` decodes a `.json(_:)` blob via per-call `JSONDecoder()` (decoupled from `NebulaJSONDecoder`), and only `json` throws (flag errors surface as `DecodingError` — **no new `NebulaError.Kind` case**). `NebulaLocalFeatureFlags` is a `final class` wrapping `Mutex<[String: NebulaFlagValue]>` — the in-memory façade; `Sendable` derived, no `@unchecked`; the `final class` absorbs the `~Copyable` `Mutex`; the store is a `Sendable` value type, so the init takes it by value (**no `sending`**, unlike `NebulaDefaults`'s `UserDefaults`); `setValue(nil)` removes. `NebulaRemoteFeatureFlags` refines the port with `refresh() async throws` (the conformer serves the last-fetched cache; the backend is app-supplied; deviates from the research's non-throwing `refresh() async` — honest about fetch failure). `NebulaCompositeFeatureFlags` (a `Sendable` struct + `[any NebulaFeatureFlags]` + `withSource(_:)`, first-non-nil resolution — mirrors `NebulaHTTPInterceptorChain`) is a generic resolver; the app wires `[localOverrides, remote, builtInDefaults]` — the composite does not hardcode local/remote/defaults, and conforms to `NebulaFeatureFlags` so the typed accessors flow through. **No process-wide accessor** — all `final class` store façades are constructed-and-passed at the composition root (the `*Config` `Mutex` family is reserved for configuration values). Deferred: `NebulaDefaults`-backed persistent overrides (research marks optional — follow-up; needs `NebulaFlagValue: Codable`); SwiftUI `@Environment` injection / an `@Observable` flag manager (Cosmos-only); the remote backend itself (app-supplied); rollout-% / audience targeting (backend-computed — Nebula evaluates locally on a fetched value). DocC `ArchitectureFeatureFlags.md` (indexed in `Architecture.md` after `ArchitectureKeychain` — NOT in `Nebula.md` `### Configuration contracts`, as it is an Architecture-layer port+façade, not a config struct). Wave N14; vault `03-padroes/nebula-feature-flags.md`.
- **Notifications (in Nebula).** `NebulaNotificationCenter` (`Architecture/Notifications/`) is a `Sendable` port carrying the scheduling/authorization surface (`requestAuthorization(options:)` / `add(_:)` / `cancel(_:)` / `cancelAll()` / `pendingRequests()`); the delegate-handler surface is **not** on the port — it lives on the config. `NebulaUNNotificationCenter` is the concrete `final class : NSObject, NebulaNotificationCenter, UNUserNotificationCenterDelegate, Sendable` delegate-adapter façade: `UNUserNotificationCenterDelegate` is an `@objc` protocol backed by a `weak` Obj-C reference, so a plain Swift class can't back it → the façade subclasses `NSObject` (the `NebulaHTTPServer` `Sendable final class` precedent gains an `NSObject` base); the delegate methods are sync Obj-C callbacks → a `final class` + `Mutex<NebulaNotificationsConfiguration>` (not an actor); `Sendable` **derived** (the only stored property is the `Mutex<Config>`), no `@unchecked`. `UNUserNotificationCenter.current()` is non-Sendable AND a shared singleton → fetched **locally per method, never stored** (the singleton cannot be `sending`-consumed — the `NebulaDefaults` precedent does not apply); `delegate = self` set once in `init`; `delegate` is `weak` → auto-nils on dealloc → no `deinit`. The config handlers are **synchronous-returning** (`willPresent` returns `NebulaNotificationPresentationOptions`, `didReceive` returns `Void`) to avoid capturing the non-`@Sendable` Obj-C completion inside a `@Sendable` closure (a Swift 6 strict-concurrency wall). `NebulaNotificationsConfiguration` is the 6th config struct (`Sendable`, NOT `Equatable`); `NebulaNotificationsConfig` is the `Mutex` accessor. `NebulaNotificationsError` is an open-struct (`NebulaFailure`, the `NebulaKeychainError` precedent) — `coarseKind` (scheduling → `.cocoa`, rest → `.unknown`) + `toNebulaError(kind:)`, no new `NebulaError.Kind` case. The value types (`NebulaNotificationContent`/`Trigger`/`Request`/`Response`/`PresentationOptions`/`AuthorizationOptions`) are `Sendable` derived and all-5 (Nebula values, not SDK props). **New gating precedent — `#if !os(<platform>)` for SDK symbols `API_UNAVAILABLE`:** `UNUserNotificationCenterDelegate.didReceive`, `UNNotificationResponse`, and every user-facing `UNNotificationContent` property are `API_UNAVAILABLE(tvos)`; empirically verified via `swiftc -typecheck` across all 5 SDKs that `@available(tvOS, unavailable)` on the override fails ("cannot override … which has been marked unavailable") and `if #available(..., *)` fails ("'title' is unavailable in tvOS" — the `*` wildcard does NOT exclude tvOS for an `unavailable` symbol). The ONLY compile-safe mechanism is `#if !os(tvOS)` — applied to the `didReceive` override (absent on tvOS) and the content-property mapping (content-less on tvOS) — distinct from the `@available(<platform>, unavailable)` declaration gate and the `#if canImport(<framework>)` whole-file gate (deferred to N15b's `BackgroundTasks`). `UNUserNotificationCenter.current()` traps in a headless test bundle → the façade is never instantiated in tests; the mapping helpers are `internal` for a round-trip suite. Deferred: `.location` trigger (CoreLocation + 3-platform gate), `setBadgeCount` (watchOS unavailable), categories + delivered-notification management (tvOS unavailable), the unified `NebulaPermissions` request port (app-level glue), `NebulaBackgroundTask` (→ N15b). DocC `ArchitectureNotifications.md`. Wave N15a; vault `03-padroes/nebula-notifications.md`.
- **Background tasks (in Nebula).** `NebulaBackgroundTaskScheduler` (`Architecture/BackgroundTasks/`) is a `Sendable` port carrying the scheduling surface (`register(_:)` / `submit(_:)` / `cancel(_:)` / `cancelAll()` / `pendingRequests()`); the launch-handler surface is **not** on the port — it lives on the config. `NebulaBGTaskScheduler` is the concrete `final class : NebulaBackgroundTaskScheduler, Sendable` façade (no `NSObject` — there is no `@objc` protocol to back, unlike `NebulaUNNotificationCenter`); `BGTaskScheduler.shared` is non-Sendable AND a shared singleton → fetched **locally per method, never stored** (the singleton cannot be `sending`-consumed — the `NebulaDefaults` precedent does not apply); `register(forTaskWithIdentifier:using:launchHandler:)` takes `using: nil` explicit. When the system launches the app, the façade wraps the delivered `BGTask` in a `NebulaBGTaskBox`, builds a Sendable `NebulaBackgroundTask` handle whose `complete(success:)` / `onExpiration(_:)` closures capture the **Sendable façade + identifier** (never the `BGTask`) and reach the box through the façade's `Mutex`, and forwards the handle to the config's `launch` handler. **macOS/watchOS-unavailable** via the type-level `@available(macOS, unavailable) @available(watchOS, unavailable)` declaration gate — `BGTaskScheduler` is `API_UNAVAILABLE(macos) API_UNAVAILABLE(watchos)`. `NebulaBackgroundTaskConfiguration` is the 7th config struct (`Sendable`, NOT `Equatable`); `NebulaBackgroundTaskConfig` is the `Mutex` accessor. `NebulaBackgroundTaskError` is an open-struct (`NebulaFailure`, the `NebulaKeychainError` precedent) — `coarseKind` (scheduling → `.cocoa`, rest → `.unknown`) + `toNebulaError(kind:)`, no new `NebulaError.Kind` case; factory statics carry `underlying: NebulaError.Box?` to preserve the SDK `NSError`. The value types (`NebulaBackgroundTaskKind`/`NebulaBackgroundTaskRequest`) are `Sendable` derived and all-5 (Nebula values, not SDK props). **New gating precedent — `@available(<platform>, unavailable)` declaration gate (form c):** `BackgroundTasks.framework` is physically present on all 5 SDKs, so `#if canImport(BackgroundTasks)` is `true` on macOS/watchOS (it gates nothing) and the `BGTaskScheduler.shared` reference then fails; the compile-safe gate is the type-level declaration on the Nebula façade (the `NebulaLogStoreExporter` precedent — declaring a Nebula symbol unavailable suppresses body type-checking on mac/watch); `@available(macOS 26, unavailable)` is a syntax error (`unavailable` takes no version). This is the complement of N15a's `#if !os(<platform>)` compile gate (for an SDK symbol that can't take a declaration gate) — the **4-form gating taxonomy** is consolidated: (1) `@available(<platform>, unavailable)` Nebula-symbol declaration [N15b]; (2) `#if !os(<platform>)` SDK-symbol compile gate [N15a]; (3) `#if canImport(<framework>)` genuinely-absent-framework gate [reserved]; (4) `#if swift(>=6.4)` + `if #available` above-floor gate [N15b]. **Sendability — `@unchecked Sendable` reference-type box:** storing the non-`Sendable` `BGTask` (system-delivered in the `@Sendable` launch callback) directly in a `Mutex<[String: BGTask]>` fails whole-module Swift 6 with a region-isolation wall (`'inout sending' parameter '$0' cannot be task-isolated`) — a non-Sendable class arriving as a closure param cannot be `sending`-transferred into a `Mutex` region the compiler can't prove exclusive (the `NebulaDefaults` `Mutex<non-Sendable>` precedent does not apply). Resolved by a `final class @unchecked Sendable` `NebulaBGTaskBox` holding the `BGTask` as an immutable `let` (the `NebulaMemoryLogHandler` precedent — the binding forbids `@unchecked` on value types, not reference types). The box crosses isolation; the façade holds `Mutex<[String: NebulaBGTaskBox]>` (Sendable boxes → Sendable `Mutex`); the façade's `Sendable` is **derived** — `@unchecked` isolated to the single reference-type box. **`submit` dual-path, warning-clean under `.v26`:** iOS-27 async `submitTaskRequest(_:)` (`#if swift(>=6.4)` + `if #available(iOS 27, tvOS 27, visionOS 27, *)`) + iOS-26 deprecated sync `submit(_:)` fallback (deprecation fires only at deployment target ≥ 27). `BGTaskScheduler.shared` is non-functional in a headless test bundle → the façade is never instantiated in tests; the port seam (`FakeBackgroundTaskScheduler`) + the type-level conformance check + the `#if !os(macOS) && !os(watchOS)` mapping round-trip prove the architecture. Deferred: `BGContinuedProcessingTask` (iOS-26-only → N15c), `registerForRemoteNotifications` (app-lifecycle), the unified `NebulaPermissions` request port. DocC `ArchitectureBackgroundTasks.md`. Wave N15b; vault `03-padroes/nebula-background-tasks.md`.
- **CloudKit-backed observability (in Nebula).** `Architecture/Metrics/` + `Architecture/Analytics/` + `Architecture/CloudKit/` (Wave N19) — a ports + CloudKit-adapters observability suite, opt-in via configuration, `dependencies: []` pristine (CloudKit is a system framework, not an SPM dep). **Metrics:** `NebulaMetrics` (a `Sendable` port — `record(_:)` + counter/histogram/gauge/timing default extensions) + `NebulaMetricEvent`/`NebulaMetricValue`/`NebulaMetricKind` + `NebulaEventBuffer<Event: Sendable & Equatable>` (a `final class` wrapping `Mutex<[Event]>` — the `~Copyable` `Mutex` absorbed behind a copyable `Sendable` reference, derived **no `@unchecked`**; flush outside the lock) + `NebulaMetricsConfiguration` (Sendable-NOT-Equatable, `@Sendable` handler) + `NebulaMetricsConfig` (`Mutex` accessor) + `NebulaLocalMetrics`. **Analytics:** `NebulaAnalytics` (port — `track(_:)` + track/screen/identify default exts) + `NebulaAnalyticsEvent` + `NebulaAnalyticsConfiguration`/`NebulaAnalyticsConfig`/`NebulaLocalAnalytics` (reuses `NebulaEventBuffer`). **Performance sink:** `NebulaPerformanceSink` routes each `NebulaMeasureResult` from the Measure subsystem into `NebulaMetricsConfiguration` as a `.timing` event (no new timing API). **CloudKit glue:** `NebulaCloudKitEnvironment` (Nebula-owned `Sendable, Equatable` enum — `CKContainer.Environment` does NOT exist as a Swift type in the Xcode 27 Beta 3 SDK, verified across the iOS + macOS `CloudKit.swiftmodule` interfaces + Obj-C headers; the three database scopes map to `CKContainer.privateCloudDatabase`/`.publicCloudDatabase`/`.sharedCloudDatabase`) + `NebulaCloudKitConfiguration` (`Sendable, Equatable` — no handler, sync is stateful) + `NebulaCloudKitConfig` (`Mutex` accessor) + `NebulaCloudKitSync` (port — `sendChanges`/`fetchChanges` + `sync()` default ext) + `NebulaCloudKitSyncEngine` (`final class` wrapping `CKSyncEngine`; `CKContainer`/`CKDatabase` constructed in `init` and discarded so their preconcurrency Sendability never reaches a Nebula `Sendable` derivation; the Nebula-owned `Delegate` adapter is a `final class` whose stored props are `@Sendable` closures → `Sendable` via SE-0302; **no `@unchecked` authored**). **CloudKit-backed preferences (N19d):** `NebulaCloudKitPreferences` (conforms `NebulaPreferences`) — a local synchronous cache (the port contract) + an injectable async sink that flushes each `NebulaCloudKitKVChange` to CloudKit (default sink maps a key/value to a `CKRecord` in the configured zone, save/delete stateless per call — the `NebulaKeychain` precedent, no `CKDatabase` stored, `Sendable` derived with **no `@unchecked`**) + `ensureZone()`/`refresh()` extras; the sync cache is unit-tested, the async sink via a recording sink with a bounded await, the default CloudKit sink compile-verified (runtime needs an iCloud entitlement + account — app-owned, the `MeridianExample` precedent). **CloudKit-backed feature flags (N19d):** `NebulaCloudKitFeatureFlags` (conforms `NebulaRemoteFeatureFlags`) — **read-only** (the port has no write requirement; writing flag records to CloudKit is app-owned): `value(forKey:)` serves a local `Mutex<[String: NebulaFlagValue]>` cache; `refresh()` pulls `NebulaFlag` records and replaces the cache, leaving it **unchanged on failure** (the port contract — reads keep resolving to the last good fetch); stateless-per-call CloudKit boundary (no `CKDatabase` stored, `Sendable` derived with **no `@unchecked`**); injectable `fetch` (default `defaultFetch` is compile-verified; runtime needs iCloud); `encode(_:into:)`/`decode(from:)` public record codec (a `kind` tag + a per-kind field — pure, unit-testable with an in-memory `CKRecord`, no iCloud). **Admissibility (load-bearing):** verified against the authoritative `CloudKit.swiftmodule` `.swiftinterface` (Xcode 27 Beta 3 / Swift 6.4) — `CKContainer`, `CKDatabase`, `CKSyncEngine` are **clean `Sendable`** (no `@unchecked`) on all 5 platforms, `CKSyncEngine` `@available(macOS 14, iOS 17, tvOS 17, watchOS 10, *)` + visionOS via `*` → all below Nebula's `.v26` floor, so **no `@available` gate and no `#if os()`** on the core sync path. The `@unchecked Sendable` CK value types (`CKRecord`/`CKQuery`/`CKOperation`/`CKSubscription`/`CKShare`/`CKRecordZone`/`CKAsset`/…) are **Apple classes**, NOT Nebula value types — the binding's "no `@unchecked` on Nebula-defined value types" rule does NOT forbid them; handled by the documented `final class @unchecked Sendable` plain-`let` box (the `NebulaMemoryLogHandler`/`NebulaBGTaskBox` precedent) and copied into Sendable Nebula values at the adapter boundary (the `NebulaLogStoreExporter` precedent). **Corrects the prior "Defer (sibling)" verdict** in `nebula-app-readiness-research.md` (a snapshot from an older SDK). Foundation + `CloudKit` + `Synchronization`; the `Nebula*Configuration` per-call / process-wide `Mutex`-accessor shape; open-struct errors bridge to existing `NebulaError.Kind` (no new case). DocC `ArchitectureCloudKitObservability.md`. Wave N19 (N19a–e complete); vault `03-padroes/nebula-cloudkit-observability.md` + ADR `02-decisoes/adr-cloudkit-observability-suite.md`.
- **Permission status (in Nebula).** `NebulaPermissionStatus` (`Architecture/Permissions/`) is an 8-case `Sendable`/`Equatable`/`Hashable`/`CaseIterable`/`CustomStringConvertible` enum — the union superset of Apple's per-framework status vocabularies (`notDetermined`/`restricted`/`denied`/`authorized`/`provisional`/`ephemeral`/`authorizedAlways`/`authorizedWhenInUse`), so a future `NebulaPermissions` request port maps each framework's status into one type. `init?(UNAuthorizationStatus:)` bridges the one status source shipped in N15a (`.ephemeral` is `#if os(iOS)` — App Clips); the AV/CoreLocation/Photos/ATT bridges come with the deferred `NebulaPermissions` port. All-5, no gate. DocC `ArchitecturePermissions.md`. Wave N15a; vault `03-padroes/nebula-notifications.md`. `Aurora/` (a separate local SwiftPM package depending on Nebula via `../`, mirroring Meridian) ships `AuroraEntityMapping` — a **type-level** protocol (static methods) bridging a SwiftData `@Model` (`PersistentModel`) to a Nebula `NebulaEntity` DTO — and `AuroraRepository<Mapping: AuroraEntityMapping & Sendable>`, a `@ModelActor` `actor` conforming to all four `NebulaRepository` capability ports (read-only / keyed / writable / deletable). `@ModelActor` synthesizes the actor-isolated `ModelContext`; the mapping bridges the non-`Sendable` `@Model` to the `Sendable` DTO so nothing non-`Sendable` escapes. `stream()` is `nonisolated` (the port returns an `AsyncThrowingStream`, not `async`) and spawns a `Task` that hops to the actor; `count()`/`find(id:)`/`save(_:)`/`delete(_:)` are `async` on the actor. `Mapping: Sendable` absorbs the SE-0470 isolated-conformance warning.

**Why a sibling package (again).** The same reasoning as Meridian: `import Aurora` from inside Nebula is an unconditional hard compile error (verified), so the Clean Architecture rule "domain / use cases never import persistence" is enforced across packages. Aurora lives as a subdir package (`Aurora/`, `path: "../`) — one repo, one CI; promoting it to its own public git repo is a documented future step. Versioning: Aurora N ↔ Nebula N ↔ OS N, in lockstep (`VERSIONING.md`). The runnable `AuroraExample` executable is the compile gate / living docs (NOT a shipped product). Vault: `03-padroes/nebula-data-network-architecture.md` (canonical), `nebula-network-retry.md`, `nebula-preferences.md`, `nebula-aurora-swiftdata.md`; open questions `08-riscos/data-network-open-questions.md` (Q1 decided c); ADR in `DECISIONS.md`.

## Concurrency

There is **no** `Sources/Nebula/Concurrency/` directory and **no** `NebulaLocked`/`NebulaFlag`/`NebulaOnce` wrapper layer shipped in 0.1.0. Nebula's shared mutable state uses `Mutex<T>`/`Atomic<T>` from `import Synchronization` directly (process-wide config accessors are `let Mutex<…>` globals; `NebulaMemoryLogHandler` backs its ring buffer with a `Mutex`). The `~Copyable`/`Sendable` Nebula-prefixed wrappers were designed (see `DECISIONS.md` ADR row "Ship `NebulaLocked`/`NebulaFlag`/`NebulaOnce` wrappers") but deferred — their `~Copyable`+`Sendable` synthesis still needs compile-validation against the exact Swift 6.4 toolchain. Tracked in `ROADMAP.md` → "Later".

The Swift 6 concurrency rules that the wrappers would have embodied still apply throughout the codebase (see `CLAUDE.md` → "Concurrency — Modern Swift 6, zero warnings"): all public value types `Sendable` by derived conformance (no `@unchecked` on Nebula-defined value types); handlers `@Sendable`; `Mutex.withLock` uses `sending` (SE-0430, NOT `transferring`); the three frozen ordering structs `AtomicLoadOrdering`/`AtomicStoreOrdering`/`AtomicUpdateOrdering` (`.acquiringAndReleasing` is invalid for `load`); region-based isolation (SE-0414) before `@unchecked`; actors not global actors.

## Conventions

- All public types are prefixed with `Nebula` (top-level types) or use natural names on extension methods that fill a stdlib gap (`clamped(to:)`, `or(_:)`).
- `nebula*` method-label prefix on open `Collection`/`Sequence` ergonomics to avoid stdlib namespace pollution.
- Configurations are `Sendable` structs with `@Sendable` handlers and fluent `.with*` builders — no `@Entry`, no `@Observable`, no `@Environment`.
- Extensions derive `Sendable`; no `@unchecked Sendable` on Nebula-defined types.
- `@available` gates include all 5 platforms: `@available(iOS 26, macOS 26, tvOS 26, watchOS 26, visionOS 26, *)`.
- macOS-only gating uses `#if os(macOS)` or explicit per-platform `@available(<platform>, unavailable)` — NOT `@available(macOS 12, *)` alone.
- DocC on every public symbol.
- The `.swiftinterface` (Xcode 27 Beta 3 SDK) is the authoritative ground truth for Apple API availability — not WebFetch (which has hallucinated availability tables).

## Dependencies

- None at runtime.
- Swift Testing is part of the Swift toolchain.
- CryptoKit is an Apple framework (allowed); the only non-Foundation import, isolated behind `NebulaHashAlgorithm` in `Extensions/DataURL/`.

## Targets

`Nebula` ships as a single SPM target. Consumers import one module:

```swift
import Nebula
```

This keeps the public surface simple while still organizing code internally as `Nebula.swift` (top-level), `Logging`, `Errors`, `Extensions`, `Standardize`, and `Measure`.