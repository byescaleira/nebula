---
tags: [moc, index]
aliases: [Nebula vault, knowledge vault]
related: []
---

# Nebula — Knowledge Vault

Synthesis/navigation layer for the [[Nebula]] foundation/architecture SwiftPM library (sibling of Cosmos, the SwiftUI design system). The **source of truth stays the root docs** (`ARCHITECTURE.md`, `DECISIONS.md`, `VERSIONING.md`, `CLAUDE.md`, `PROPOSAL.md`, `ROADMAP.md`, `CONTRIBUTING.md`); this vault is a synthesis layer. On conflict, the root doc wins — update the note.

The `.swiftinterface` (Xcode 27 Beta 3 SDK) is the authoritative ground truth for Apple API availability — not WebFetch, which has hallucinated availability tables repeatedly (UUID.random, percentEncodedQueryItems, `OutputFormatting.fragmentsAllowed`, `convertFromKebabCase`, `base64Encode`, a parameterless `UUID.random()`, "UUID is not Comparable"). Cite interface line numbers when verifying.

## Folders

- `01-fundamentos/` — foundation subsystems (logging, errors, extensions, formatting, measure)
- `02-decisoes/` — new ADRs / decisions
- `03-padroes/` — patterns (SPM architecture, Swift 6 concurrency)
- `07-metodologia/` — workflows / methods
- `08-riscos/` — open risks / refuted specs

## Foundation index

- [[nebula-logging]] — `os.Logger` façade (`NebulaLogger`/`NebulaLogConfiguration`/`NebulaSignposter`/`NebulaMemoryLogHandler`/`NebulaLogStoreExporter`); no `swift-log`; `OSLogStore`/`OSLogEntry`/`getEntries` live in the OSLog clang module + a separate `OSLog.swiftmodule` overlay (NOT `os.swiftmodule`); `.Scope.system`/`.local()` macOS-only via `#if os(macOS)` or explicit per-platform unavailable — NOT `@available(macOS 12, *)` alone; `OSMetricOperation` internal to the os overlay (cannot re-export); `OSLogEntry` not Sendable → copy into `NebulaLogStoreEntry`.
- [[nebula-errors]] — `NebulaError` envelope (`Error`/`LocalizedError`/`CustomNSError`/`Sendable`/`Hashable`); `underlying: Box?` via `final class Box` (struct recursion illegal — verified compile); `NebulaErrorConfiguration` Sendable ONLY (NOT `Equatable` — `@Sendable` closure not Equatable, mirrors `CosmosErrorConfiguration`); lossy mapping inits (drop the `any Error` existential, keep Sendable fragments); `NebulaError.wrap`; `NebulaErrorConfig` process-wide `Mutex` accessor; `NebulaDecodingError`/`NebulaEncodingError` bridges.
- [[nebula-date-time-extensions]] — `Date` arithmetic/predicates (DST-safe via `calendar.dateInterval(of:for:)`); `DateInterval.init(start:duration: Swift.Duration)` distinct from Foundation's TimeInterval overload; `NebulaDateFormat`/`NebulaDurationFormat`/`NebulaClock` façades; `Calendar.gregorian` does NOT exist → use `Calendar(identifier: .gregorian)`; `RelativeFormatStyle` init-based (no fluent chain, no `.offset`/`.timer`); ISO/stable presets pinned to `Locale(identifier: "en_US_POSIX")` + `.gmt`.
- [[nebula-string-extensions]] — `String`/`AttributedString` extensions (Foundation scope only — no SwiftUI/UIKit scopes); `NebulaRegex<Output>` (Sendable, conditional `where Output: Sendable`); `NebulaRegexPatterns` (curated literals: UUID/IPv4/hex/semver/ISO-timestamp — NO email per Apple guidance); `NebulaStringDetectedEntity` over `NSDataDetector` (cached in `Mutex<NSDataDetector?>`); `NebulaStringLocalization` over `String(localized:)`; above-floor 26.4 gates for `base64URLAlphabet`/`omitPaddingCharacter`/`ianaName` getter AND `init?(ianaName:)`.
- [[nebula-number-measurement-extensions]] — `NebulaFormattingOptions` (Sendable + `.with*`) + `NebulaNumberFormatting` façade (percent/currency/bytes/list/measurement); `BinaryInteger`/`BinaryFloatingPoint`/`Decimal` extensions; `Decimal` NOT `FloatingPoint` (its own `FormatStyle`); `Decimal.rounded(toDecimalPlaces:)` via `NSDecimalRound` (no value-level `rounded(_:)`); `ListFormatter` per-call only (no FormatStyle replacement, not Sendable, NO `ItemStyle` enum); `measurementWidth` passed explicitly (generic over `Unit: Dimension` — cannot be a non-generic struct field).
- [[nebula-primitive-extensions]] — `Comparable.clamped(to:)` (SE-0177 gap); `BinaryInteger.isEven`/`isOdd`/`times(_:)` (non-escaping rethrows — matches `forEach`, NOT `@Sendable`); `Optional.or(_:)`/`orThrow(_:)`/`isNilOrEmpty`; `NebulaNilError` (concrete Sendable, avoids `Wrapped==Error` overload collision); `UUID.shortString`/`isValid(_:)`; `UUID.random(using:)` at `@available(iOS 26.4, *)` (NOT parameterless `random()` — `UUID()` remains default v4); NEVER redeclare `Bool.toggle()`/`isMultiple(of:)`.
- [[nebula-collection-extensions]] — `nebulaChunked`/`nebulaWindows`/`nebulaUniqued`/`nebulaStablePartition`/`nebulaPartitioned`/`nebulaSorted` key-path/`nebulaMerging`; `nebula*` prefix on open Collection/Sequence (no stdlib pollution); eager by default (conscious divergence from swift-algorithms' lazy `uniqued()`); non-escaping `rethrows` (NOT `@escaping @Sendable`); `@available` gates include all 5 platforms incl. `visionOS 26`; `KeyPathComparator` requires Sendable keypaths. NOTE: `nebulaFiltered(by: Foundation.Predicate)` + `NebulaFrequency` were designed here but DEFERRED to post-0.1.0 (not shipped in 0.1.0).
- [[nebula-codable-foundation]] — `NebulaJSONDecoder`/`NebulaJSONEncoder` (Sendable wrappers holding configure-once-frozen `JSONDecoder`/`JSONEncoder` in a `let`); `NebulaJSONDecoderConfiguration`/`NebulaJSONEncoderConfiguration` (Sendable + `.with*`, all strategy enums Sendable); `NebulaDecodingError`/`NebulaEncodingError` + `DecodingError.nebula`/`NebulaError.decoding(_:)` bridges; NO `OutputFormatting.fragmentsAllowed` (does not exist — that's `JSONSerialization.WritingOptions`; `ReadingOptions` uses `.allowFragments`); NO `convertFromKebabCase`/`base64Encode`.
- [[nebula-data-url-extensions]] — `Data.nebulaHexEncodedString`/`init?(nebulaHexEncoded:)` (Foundation has NO native hex); `NebulaHashAlgorithm` (Sendable enum over CryptoKit SHA256/384/512 — the ONLY `import CryptoKit`); `Data.nebulaDigest`/`nebulaHexDigest`; `URL.nebulaAppending(queryItem:)`/`nebulaSettingQueryItem`/`nebulaRemovingQueryItem`/`nebulaQueryItem(named:)`/`nebulaPercentEncoded()`; `URLComponents.nebulaWith(queryItem:)` fluent builders; defer base64URL/omitPadding to Nebula 26.4.
- [[nebula-standardize-measure]] — `NebulaStandards` formatting façade (3rd config struct, NO handler — formatting is stateless) over the `FormatStyle` family (Date/ISO8601/Decimal/Integer/FloatingPoint/Measurement/Currency/Percent/ByteCount/List/PersonNameComponents/URL/Duration); `NebulaMeasureConfiguration` (4th config struct, carries `measure(_:_:)`/`bench(_:_:)` ON the config like `NebulaLogConfiguration.log`, clock `any Clock<Duration>` default `ContinuousClock`, signposter `NebulaSignposter?` default `nil` opt-in, `isEnabled`+`@Sendable` handler); `NebulaMeasureResult` (Sendable+Equatable, `perIteration = total/iterations`); reuses the existing `NebulaSignposter` (Wave A, Logging/) via `.osSignposter`. Corrected/verified: `Clock.measure` returns ONLY `Duration` (no `(T,Duration)` tuple) → capture result in the closure; `withIntervalSignpost` has NO async variant → async `measure` uses manual `beginInterval`/`endInterval`; `durationUnits(allowed:width:)` uses `Set<Duration.UnitsFormatStyle.Unit>` (NO top-level `Duration.Unit`); `measurement<U>(width:locale:usage:numberFormatStyle:)` (NO `unit` param, locale BEFORE usage); `ListFormatStyle` floor is iOS 15/macOS 12 (NOT 13/16); `DateComponents.formatted(_:)` + `DateComponents.ISO8601FormatStyle` at-floor `@available(iOS 26, *)`; `NebulaStandardsConfig`/`NebulaMeasureConfig` process-wide `Mutex` accessors.

## Patterns index

- [[nebula-spm-architecture]] — single-target SPM manifest mirroring Cosmos: `swift-tools-version: 6.3` (dual Xcode 26.4+ / Xcode 27 build; Xcode 26.0–26.3 shipped Swift 6.2 and will NOT parse a 6.3 manifest — require Xcode 26.4+), 5 platforms `.v26`, `swiftLanguageModes: [.v6]`, `defaultLocalization: "en"`, single `.library` product `Nebula` + `.testTarget` `NebulaTests` (Swift Testing), no third-party deps, `.process("Resources")` commented (no String Catalog by default), DocC catalog auto-discovered at `Sources/Nebula/Nebula.docc/`, OS-27-only SDK symbols compile-gated `#if swift(>=6.4)`, platform-specific surfaces `#if os(...)`.
- [[nebula-clean-architecture]] — Uncle Bob Clean Architecture layers → Swift 6 / Nebula constructs (Entity `Sendable` struct/`actor`; UseCase `Sendable` protocol with `@Sendable execute` + `Input/Output: Sendable`, untyped `throws`; Interface Adapters = `NebulaInputPort`/`NebulaOutputPort`/`NebulaRepository`/`NebulaGateway` Sendable protocols owned inner, implemented outer; `NebulaDTO` marker; Frameworks & Drivers outside Nebula). Dependency rule enforced by protocol ownership + a lightweight `NebulaCompositionRoot` (Mutex-backed factory bindings, NOT a DI container; `dependencies: []` forbids Resolver/Factory/Swinject). DIP = inner port defined in Nebula, outer presenter/repo implements. DTOs cross boundaries (never Entities). Fowler Repository/Gateway. Single-target → compile-time enforcement needs a separate app module (open risk). Out of scope: MVVM/MVC/VIP/VIPER.
- [[nebula-error-taxonomy-toolkit]] — architecture-toolkit error dimension: `NebulaFailure: Error, Sendable` protocol + per-layer open structs (`NebulaDomainError`/`NebulaRepositoryError`/`NebulaValidationError`) bridging to the CLOSED `NebulaError.Kind` enum via caller-picked `toNebulaError(kind:)` — NO new Kind cases; fine taxonomy in open `var code: String`. SE-0413 typed throws per layer (`throws(NebulaDomainError)` etc.), untyped `throws`/`Result<T, NebulaError>` at boundaries. Rejects single-`NebulaDomainError` (no layer enforcement).
- [[nebula-swift6-concurrency]] — Swift 6 concurrency foundation: Sendable-by-derivation; `@Sendable` handlers; no `NSLock`/`DispatchQueue`/`nonisolated(unsafe)` → `Mutex<T>`/`Atomic<T>` from `import Synchronization` (SwiftStdlib 6.0 → iOS 18/macOS 15, below `.v26`); once-token `static let` + `swift_once` for idempotent setup; region-based isolation (SE-0414) before `@unchecked`; typed throws (SE-0413) opt-in with `NebulaError` as concrete `Failure`; no SwiftUI constructs (`@Entry`/`@Observable`/`@Environment`); corrected `Atomic` ordering types (three frozen structs — `AtomicLoadOrdering`/`AtomicStoreOrdering`/`AtomicUpdateOrdering`, NOT a single `Ordering` enum; `.acquiringAndReleasing` invalid for `load`); `Mutex.withLock` uses `sending` (SE-0430) not `transferring`; `NebulaLocked`/`NebulaFlag`/`NebulaOnce` `~Copyable` wrappers.

## Risks index

- [[above-floor-26-4-apis]] — above-floor (Nebula 26.4) APIs requiring `@available(iOS 26.4, macOS 26.4, tvOS 26.4, watchOS 26.4, visionOS 26.4, *)` gates: `Data.Base64EncodingOptions.base64URLAlphabet`/`.omitPaddingCharacter`, `String.Encoding.ianaName` getter AND `init?(ianaName:)`, `UUID.random(using:)` (swift-foundation SF-0031 / FoundationPreview 6.3, surfaced as OS 26.4; NOT parameterless `random()`).
- [[osmetricoperation-internal]] — `OSMetricOperation` (the OS 26 signpost-metrics enum) is `@usableFromInline internal` in the os overlay — cannot be re-exported across module boundaries. If NebulaSignposter exposes metric APIs, Nebula must define its own public `NebulaMetricOperation` enum gated `@available(iOS 26, *)`; defer to Nebula 27.
- [[visionos-oslogstore-scope-uncertain]] — visionOS availability of `OSLogStore.Scope.system` and `.local()` is UNCERTAIN (the `Store.h` header does NOT list visionOS in `API_UNAVAILABLE`). Treat as macOS-only by safe gating (`#if os(macOS)` or explicit per-platform `@available(<platform>, unavailable)` including visionOS); confirm at compile time on the visionOS SDK.
- [[webfetch-availability-hallucinations]] — WebFetch on `developer.apple.com` has repeatedly hallucinated availability tables (UUID.random iOS 13+ + parameterless `random()` + "UUID is not Comparable"; `percentEncodedQueryItems` iOS 16+; `OutputFormatting.fragmentsAllowed`; `convertFromKebabCase`; `base64Encode`). The `.swiftinterface` (Xcode 27 Beta 3 SDK) is authoritative; never rely on WebFetch for availability.
- [[mutex-sending-not-transferring]] — `Mutex.withLock` uses `sending` (SE-0430 standardized on `sending`), NOT `transferring` (the earlier SE-0433 spelling was revised). Wrapper forwarding signatures must use `sending` to match.
- [[atomic-ordering-three-structs]] — there is NO single `Ordering` enum. Three frozen Sendable structs: `AtomicLoadOrdering` (`.relaxed`/`.acquiring`/`.sequentiallyConsistent` — `.acquiringAndReleasing` is INVALID for `load`), `AtomicStoreOrdering`, `AtomicUpdateOrdering` (`.acquiringAndReleasing` valid here, used by `compareExchange`/`exchange`). `NebulaFlag.load` uses `.acquiring`; `set` uses `.releasing`; `compareExchange` uses `.acquiringAndReleasing`.

## Architecture index (Clean Architecture toolkit)

The architecture half of Nebula (`ARCHITECTURE.md:5` "foundation/architecture"): the seams (inner-owned protocols + markers + DTO contract + a wiring helper) that let an app implement Clean Architecture efficiently, WITHOUT Nebula owning any presentation/DB/framework code. Presentation patterns (MVVM/MVC/VIP/VIPER) are explicitly out of scope. Canonical hub: [[nebula-clean-architecture-toolkit]].

> **Shipped in Nebula 0.2.0 (2026-07-19).** All 11 notes below carry `status: shipped` / `shipped: "0.2.0"` in frontmatter. The source lives at `Sources/Nebula/Architecture/` (Domain/Ports/Errors/UseCase/Repository/Gateway/Validation/Registry/Testing/Async); 509 tests / 107 suites green, zero concurrency warnings. Root-doc ADR: `DECISIONS.md` (Wave H, Accepted).

- [[nebula-clean-architecture-toolkit]] — canonical surface: Uncle Bob layers + dependency rule → Swift 6 constructs, the full toolkit type table, Sendable strategy per type family, DI-without-a-framework, typed-throws policy, enforcement-is-convention caveat. Links all notes below.
- [[nebula-usecase]] — `NebulaUseCase<I, O>` generic `Sendable` struct over a `@Sendable (I) async throws -> O` body (NOT a PAT + `AnyUseCase` box); `NebulaUseCaseRole` closed command/query enum; `.decorate`/`.logged`/`.measured`/`.reported`/`.instrumented` decorator seam routing to the EXISTING log/measure/error configs (no 5th config); `name: StaticString` required for signposting (`NebulaSignposter.swift:8-13`); `executeTyped(_:) async throws(NebulaError)`.
- [[nebula-repository]] — `NebulaRepository<Element>: Sendable` (PAT + `any NebulaRepository<E>: Sendable` compiler-verified, no `@unchecked`) + CQRS capability sub-protocols (`ReadOnly`/`Writable`/`Deletable`, no CRUD mandate, no `update` verb); streaming returns concrete `AsyncThrowingStream<Element, any Error>` (`some AsyncSequence` illegal in a protocol requirement); open-struct `RepositoryError` + `NebulaError(repositoryError:kind:)` caller-picked bridge (no new `Kind` cases); `NebulaGateway` + 5th config pair + optional `NebulaHTTPGateway`.
- [[nebula-domain-error]] — per-layer open structs (`NebulaDomainError`/`RepositoryError`/`NebulaValidationError`) conforming to `NebulaFailure: Error, Sendable`, bridging to the CLOSED `NebulaError.Kind` via caller-picked `toNebulaError(kind:)` — NO new cases (`DECISIONS.md` row 31). Deeper spec in [[nebula-error-taxonomy-toolkit]]; envelope in [[nebula-errors]].
- [[nebula-validation-invariants]] — `NebulaValue`/`NebulaEntity` (reuses stdlib `Identifiable` `Swift.swiftmodule:12443-12446`, adds `ID: Sendable`, deliberately NOT `Equatable`)/`NebulaAggregate` (marker, boundary is semantic not compiled)/`NebulaID<Entity>` (phantom-typed); `isSameIdentity(as:)`; recursive aggregates via `final class Box` (the `NebulaError.swift:91-99` precedent); parse-don't-validate; `NebulaValidator`/`NebulaAsyncValidator`/`NebulaInvariant` seam.
- [[nebula-registry-di]] — `NebulaRegistry`/`NebulaRegistryKey` (open struct, mirrors `NebulaLogCategory`)/`NebulaRegistryConfig`: `Mutex`-backed (`let`) factory bindings + generic `resolve(_:as:)` — deliberately NOT a DI container (no scoping/graph/lifecycle); `dependencies: []` (`Package.swift:28/32`) forbids Resolver/Factory/Swinject; primary path = explicit-parameter constructor injection (`DECISIONS.md` row 27).
- [[nebula-test-doubles]] — `NebulaFakeRepository` (in-memory; `final class` + `let Mutex`, derived `Sendable`)/`NebulaStubUseCase` (Sendable struct, canned `Result<O, NebulaError>`)/`NebulaSpyUseCase` (`final class` + `let Mutex` record log, derived `Sendable`)/`NebulaMockRepository` (deferred); the toolkit doubles use NO `@unchecked` (derived on a `final class` with all-`let` `Sendable` properties — the `NebulaError.Box` precedent, NOT the `NebulaMemoryLogHandler` `@unchecked` exception, `DECISIONS.md` row 25); Swift Testing assertions (`Testing.swiftmodule:657/665/738`); injected via explicit parameter, never `NebulaRegistryConfig`.
- [[nebula-async-flow]] — `AsyncSequence` `nebula*` gap-filler helpers (prefixed, never redeclare stdlib `map`/`filter`/`reduce`); `NebulaCancellation` cooperative helper (caller's `Task` owns cancellation, `Task.checkCancellation()` reused); `NebulaResultPipeline<T>` Sendable struct chaining with `NebulaError` propagation; `NebulaError.wrap` is SYNC-only (`NebulaError+Mapping.swift:158`) — async path needs its own `do/catch`/`wrapAsync`.
- [[nebula-clean-architecture-tdd]] — outside-in Red-Green-Refactor workflow (port → use case → adapter) with Swift Testing; test against `NebulaFakeRepository`/`NebulaStubUseCase`, never the real adapter; per-layer assertion table; Sendable discipline in tests.
- [[clean-architecture-toolkit-risks]] — consolidated risks, most-severe first: completeness (7 task-listed types NOT shipped — 6 intentional rejections `NebulaCommand`/`NebulaQuery`/`AnyUseCase`/`NebulaUseCaseMiddleware`/`NebulaUseCaseConfig`/`AnyRepository` + 1 `RepositoryError` naming gap vs the prefix-conforming `NebulaRepositoryError`), dependency-rule enforcement is convention, composition-root → DI-container creep, `Sendable` PAT + type-erased registry, `wrap` sync-only, lossy error bridging, marker protocols are documentation, synthesized `Equatable` entity footgun, actor-boundary chafe, `StaticString` friction, refuted/uncertain research claims carried forward.
- [[clean-architecture-open-questions]] — the ~10 open design questions with alternatives + recommendations: UseCase granularity (fine-grained), Command/Query split (closed enum), error taxonomy shape (per-layer open structs), Repository shape (capability protocols, no CRUD), DI scope (factories only), sync/async validator (split), `NebulaUseCaseConfig` 5th config (do NOT introduce), test-doubles location (main target), Cosmos boundary (bare `NebulaOutputPort`), compile-time enforcement (single-target + documented convention).

Related (already in Patterns index): [[nebula-clean-architecture]] (short layer-mapping primer), [[nebula-error-taxonomy-toolkit]] (deeper `NebulaFailure` protocol + per-layer structs spec), [[nebula-swift6-concurrency]] (concurrency ground truth — `Mutex`/`Atomic`/`sending`/ordering structs), [[nebula-spm-architecture]] (single-target manifest), [[nebula-errors]] (foundation `NebulaError` envelope).

## Presentation architecture index (the third surface — Q4 decided (d) Meridian; Wave I shipped 2026-07-19)

The presentation/architecture half: which state pattern + navigation/routing the Nebula/Cosmos ecosystem should recommend and ship seams for, under the binding rules (`dependencies: []` — no third-party; Nebula Foundation-only / no SwiftUI; Swift 6.3 / OS 26 / `@Observable` era). **Decision landed: MVVM `@Observable` + native typed-`[Route]` Router (no Coordinator tree) + option (d) Meridian sibling package.** Wave I (Foundation-only seams in Nebula) shipped; Wave II (Meridian `@Observable Router` + `NavigationStack` wiring) follows. Canonical hub: [[nebula-presentation-architecture]]; shipped seams: [[nebula-presentation-seams]].

- [[nebula-presentation-architecture]] — canonical: 2026 landscape shortlist (MVVM `@Observable` + native Router/Coordinator #1; type-driven enum destinations as inspiration; VIPER as structural reference; TCA/MVI/BLoC out), the recommendation (MVVM `@Observable` + native Router/Coordinator, zero deps), TDD fit (Tier 1 — `NebulaFakeRepository`/`Stub`/`Spy` plug in via constructor injection with zero adaptation), Nebula-infrastructure fit (viewmodel = driving adapter over `NebulaUseCase`/`NebulaRepository`; Router owns nav state; `Route` enum = DTO-shaped value), SwiftUI-states fit (`@Observable`/`@State`/`@Bindable`/`@Environment`, `@MainActor @Observable final class` viewmodel, `.task` inherits MainActor, `NavigationStack` + `navigationDestination(for: Route.self)`, typed `[Route]` over type-erased `NavigationPath`, SE-0466 app-level / SE-0475 `Observations`).
- [[nebula-presentation-target-split]] — the module/target decision: 5 options (a Foundation-only seams in Nebula / b `NebulaUI` target in-package / c in Cosmos / d **new sibling package "Meridian" — recommended** / e app-only), industry precedent (`swift-navigation` Foundation-core+SwiftUI-target split = the model; TCA single-target = the anti-pattern; Apple Backyard Birds = 3 separate local packages, enum `AppScreen` + `NavigationStack`, no Router type), compile-time enforcement regimes (single-target / multi-target-in-package with SR-1393 leakage / **separate packages = only hard-error regime, closes Wave H**), recommendation (d primary, a+e holding-pattern fallback), the real trade-off (third-package maintenance budget vs. architecture-story completeness).
- [[presentation-architecture-risks]] — consolidated risks, most-severe first: TCA rejects Nebula DI + Swift 6 sharp edges; SR-1393 SwiftPM doesn't enforce import graph; `@Observable` outside SwiftUI Swift 6 friction (→ Nebula ships marker, not `@Observable`); `NavigationStack` broken on macOS (→ Foundation-only core is defensive); FlowStacks iOS 26 infinite-loop regression; "Foundation-only Router is hollow" critique (collapses if core holds typed path/stack); `@MainActor` parallelism tax; snapshot-as-TDD-tool smell; test-hostile patterns to ban (`@Environment` coordinators, `@ViewBuilder` `_ConditionalContent`, singletons, ViewInspector-as-primary, fire-and-forget `Task`); SwiftUI nav footguns; `presentationContentShape` is NOT a new OS 26 API (zoom/morph is `.navigationTransition` + `.matchedTransitionSource`); third-package maintenance cost.
- [[presentation-architecture-open-questions]] — the ~8 owner decisions with alternatives + recommendations: Q1 pattern (MVVM `@Observable` + native typed-`[Route]` Router — **no Coordinator tree**); Q2 nav shape (**Router**, data-driven — owner preference); Q3 does Nebula ship Foundation-only marker seams (yes); **Q4 target split — DECIDED: new sibling package (d) Meridian**; Q5 naming (Meridian); Q6 version coordination (Meridian N ↔ Nebula N ↔ OS N); Q7 `@Observable` location (NOT in Nebula); Q8 test-support target (defer). Q4 gates Q5/Q6; ADR appended to `DECISIONS.md` (Wave IV).
- [[nebula-presentation-seams]] — **SHIPPED (Wave I)**: the Foundation-only presentation half — `NebulaRoute` / `NebulaNavigationStack<Route>` (typed `[Route]` model, `Sendable`/`Codable`/`Equatable`) / `NebulaRouter<Route>` (intent port, primary assoc type, **async** requirements) / `NebulaViewModel` (marker) / `NebulaSpyRouter` (spy, `Sendable` derived). Navigation-as-data, testable in pure Swift. The sibling Meridian package (Wave II) builds its `@Observable Router` on these. 525 tests / 110 suites.
- [[nebula-meridian-router]] — **SHIPPED (Wave II)**: the SwiftUI-bearing sibling package `Meridian/` (separate local SwiftPM package, path-dep on Nebula — `import Meridian` from Nebula is a hard compile error, closes Wave H). `@MainActor @Observable final class Router<Route>: NebulaRouter<Route>` + `MeridianNavigationStack` (`NavigationStack(path: $router.path)` + `navigationDestination(for:)`). Async port = the Swift 6 fix for an on-actor conformer of a nonisolated Foundation-only port. 6 tests / 1 suite.
- [[nebula-presentation-destinations-deeplink]] — **SHIPPED (Wave III)**: the two patterns the Router foundation enables — **deep-link-as-data** (pure `URL → [Route]`, `replaceStack` primitive, async port = cross-actor bridge, `Codable` restoration) and **type-driven modal destinations** (single `Optional<Destination>` enum → `sheet(item:)`, "impossible states unrepresentable", no `@CasePathable` — `Identifiable` hand-rolled). Runnable `MeridianExample` executable (compile gate + living docs) + 7 tests (13 total in Meridian).

Related (already in Patterns/Risks index): [[nebula-clean-architecture-toolkit]] (the shipped foundation+architecture seams this builds on), [[nebula-registry-di]] (constructor-injection DI — the viewmodel's port source), [[nebula-usecase]] (the viewmodel's engine), [[nebula-swift6-concurrency]] (`@MainActor`/`Sendable`/`Mutex` ground truth), [[nebula-spm-architecture]] (single-target manifest — what a (d) sibling package would extend), [[nebula-test-doubles]] (the doubles that plug into viewmodels with zero adaptation).