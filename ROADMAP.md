# Roadmap

> Last updated: 2026-07-21 (Wave N17c — Bodies & downloads shipped as 0.16.0 (additive minor — new public types; no breaking change; the third and final sub-wave of the N17 split — three additive surfaces, the gateway is untouched; pinning reused via composition, zero N17a change, no `import Security` in the product target; `NebulaMultipartBuilder` is a pure `multipart/form-data` `Data` composer — Foundation has NO multipart API, genuinely new — gateway-compatible via the existing `NebulaHTTPBody.data(_:contentType:)` (no new body case); `NebulaDownload` façade over `URLSession.download(for:delegate:)` with progress bridged from `didWriteData` (no async `for await` progress API) + resume via `cancel(byProducingResumeData:)` wrapped + `URLError.downloadTaskResumeData`; `NebulaDownloadDelegate` `final class : NSObject, URLSessionDownloadDelegate, Sendable` derived (probe EXIT=0, NO `@unchecked`); `NebulaPagedSequence<Page: Sendable>` generic pagination; delegate routing — per-task delegate gets download callbacks, pinning rides the session delegate; custom loops NOT `withPolicy`; a `URLProtocol`-backed live download round-trip hangs → documented limitation, pure seams cover the surface). Waves A–H + I–III + N1–N15b + N17a + N17b + N17c shipped — the N17 split is complete; 908 Nebula tests / 178 suites + 12 Aurora tests / 3 suites green.)
>
> This roadmap reflects the project state **after the foundation research cycle** that produced
> the 12 verified research dimensions. The vault notes (`01-fundamentos/` + `03-padroes/`) are the
> synthesis layer; the root docs (`ARCHITECTURE.md`, `DECISIONS.md`, `VERSIONING.md`, `CLAUDE.md`)
> are the source of truth.

## Done (0.1.0 — first complete foundation release)

### Wave A — Package scaffold + governance + vault
- [x] Bootstrap SPM package `Nebula` for Apple v26 platforms (iOS / macOS / tvOS / watchOS / visionOS, all `.v26`)
- [x] `swift-tools-version: 6.3` (dual Xcode 26.4+ / Xcode 27 build); `swiftLanguageModes: [.v6]`; `defaultLocalization: "en"`
- [x] Single `Nebula` target + `NebulaTests` (Swift Testing); no third-party dependencies; `.process("Resources")` commented (foundation emits developer-facing English; no String Catalog by default)
- [x] `Sources/Nebula/` folder tree: `Nebula.swift` (top-level `Nebula` enum + `NebulaVersion`), `Logging/`, `Errors/`, `Extensions/{DateTime,String,Number,Primitive,Collection,Codable,DataURL}/`, `Standardize/`, `Measure/`, `Nebula.docc/` (no `Core/`, `Concurrency/`, or `Formatting/` dirs — those planned subdirs were not shipped; `NebulaVersion` lives in the top-level `Nebula.swift`, and there is no `NebulaLocked`/`NebulaFlag`/`NebulaOnce` concurrency wrapper layer)
- [x] `NebulaVersion` (in `Nebula.swift` — Nebula N == OS N baseline; `static let version = NebulaVersion(major: 26, minor: 0, patch: 0)`)
- [x] Governance docs: `CLAUDE.md`, `ARCHITECTURE.md`, `DECISIONS.md`, `VERSIONING.md`, `ROADMAP.md`, `CONTRIBUTING.md`, `PROPOSAL.md`, `README.md`, `CHANGELOG.md`
- [x] Obsidian vault MOC (`vault/Home.md`) + 12 research notes across `01-fundamentos/` (10) and `03-padroes/` (2: `nebula-spm-architecture`, `nebula-swift6-concurrency`)

### Wave B — Logging + signposts — DONE
- [x] Shipped: `NebulaLogLevel`, `NebulaLogCategory`, `NebulaLogger` (Sendable struct over `os.Logger`; exposes `osLogger` for the redaction-preserving `OSLogMessage` path and `String` convenience level methods for the simple path — `os.Logger` cannot be wrapped, verified), `NebulaLogEvent`, `NebulaLogConfiguration` (+ `NebulaLogConfig` process-wide `Mutex` accessor), `NebulaSignposter`/`NebulaSignpostID`/`NebulaSignpostIntervalState`/`NebulaSignpostMetadata` (typealias = `os.SignpostMetadata`), `NebulaMemoryLogHandler` (`final class @unchecked Sendable`, `Mutex<T>`-backed ring buffer; test/preview-only).
- Deferred: `NebulaLogStoreExporter`/`NebulaLogStoreEntry` (macOS-only `.system`/`.local()` gated `#if os(macOS)` + explicit per-platform unavailable — NOT `@available(macOS 12, *)` alone) — moved to "Later".

### Wave C — Errors — DONE
- [x] Shipped: `NebulaError` (`Error`/`LocalizedError`/`CustomNSError`/`Sendable`/`Hashable`) + `Code`/`Kind`/`Context`/`Box` (final class — struct recursion illegal); lossy mapping inits (`NSError`/`DecodingError`/`URLError`/`CocoaError`/`any Error`) + `EncodingError` bridge + `NebulaError.wrap(_:) -> Result<T, NebulaError>`; `NebulaErrorConfiguration` (Sendable ONLY — NOT `Equatable`) + `NebulaErrorEvent` (Sendable + Equatable) + fluent `.with*`; `NebulaErrorConfig` process-wide `Mutex<NebulaErrorConfiguration>` accessor; `NebulaDecodingError`/`NebulaEncodingError`.

### Wave D — Extensions batch 1 — DONE
- [x] Shipped (Extensions/DateTime): `Date` arithmetic/predicates (DST-safe via `calendar.dateInterval(of:for:)`); `DateComponents` builders; `DateInterval.init(start:duration: Swift.Duration)`; `NebulaDateFormat`/`NebulaDurationFormat` façades. ISO/stable presets pinned to `Locale(identifier: "en_US_POSIX")` + `.gmt`. No `NebulaClock` (deferred).
- [x] Shipped (Extensions/String): `isBlank`/`nilIfEmpty`/`trimmed`/`truncated(to:with:)`/case conversions/base64/hex/URL extraction; `NebulaRegex<Output>` (Sendable, conditional `where Output: Sendable`); `NebulaRegexPatterns` (curated literals — UUID/IPv4/hex/semver/ISO-timestamp; NO email); `NebulaStringDetectedEntity` over `NSDataDetector` (cached in `Mutex<NSDataDetector?>`); `NebulaStringLocalization` (Foundation scope only).
- [x] Shipped (Extensions/Primitive): `Comparable.clamped(to:)`; `BinaryInteger.isEven`/`isOdd`/`times(_:)`; `Optional.or(_:)`/`orThrow(_:)`/`isNilOrEmpty`; `NebulaNilError`; `UUID.shortString`/`isValid(_:)`; `UUID.random(using:)` gated `@available(iOS 26.4, *)`.
- [x] Shipped (Extensions/Collection): `nebulaChunked`/`nebulaWindows`/`nebulaUniqued`/`nebulaStablePartition`/`nebulaPartitioned`/`nebulaSorted`/`nebulaMerging`. `nebula*` prefix; eager by default; non-escaping `rethrows`. (`nebulaFiltered(by:)` + `NebulaFrequency` were deferred — see "Later".)
- [x] Shipped above-floor 26.4 gates: `Data.Base64EncodingOptions.base64URLAlphabet`/`.omitPaddingCharacter`, `String.Encoding.ianaName` getter AND `init?(ianaName:)`, `UUID.random(using:)`.

### Wave E — Extensions batch 2 — DONE
- [x] Shipped (Extensions/Number): `NebulaFormattingOptions` (Sendable + `.with*`); `NebulaNumberFormatting` façade (percent/currency/bytes/list/measurement — no legacy Formatter subclasses; `ListFormatter` per-call only); `BinaryInteger`/`BinaryFloatingPoint`/`Decimal` extensions; `Decimal.rounded(toDecimalPlaces:)` via `NSDecimalRound` + `NebulaDecimalRoundingRule` enum.
- [x] Shipped (Extensions/Codable): `NebulaJSONDecoder`/`NebulaJSONEncoder` (Sendable wrappers holding configure-once-frozen `JSONDecoder`/`JSONEncoder` in a `let`); `NebulaJSONDecoderConfiguration`/`NebulaJSONEncoderConfiguration` (Sendable + `.with*`); `Decodable.init(fromJSON:)`/`Encodable.toJSONData`/`toJSONString`/`Data.asPrettyJSONString`. No `OutputFormatting.fragmentsAllowed` (does not exist).
- [x] Shipped (Extensions/DataURL): `Data.nebulaHexEncodedString`/`init?(nebulaHexEncoded:)`; `NebulaHashAlgorithm` (Sendable enum over CryptoKit SHA256/384/512 — the ONLY `import CryptoKit`); `Data.nebulaDigest`/`nebulaHexDigest`; `URL.nebulaAppending(queryItem:)`/`nebulaSettingQueryItem`/`nebulaRemovingQueryItem`/`nebulaPercentEncoded()`; `URLComponents.nebulaWith(queryItem:)`. base64URL/omitPadding gated at Nebula 26.4.

### Wave F — Standardize + Measure — DONE
- [x] Shipped (Standardize): `NebulaStandards` (formatting façade over `FormatStyle` family; `.withLocale`/`.withTimeZone`/`.withCalendar`; typed accessors only — no polymorphic `.format(_:)`) + `NebulaStandardsConfig` (process-wide `Mutex` accessor). DateComponents accessors gated `@available(iOS 26, macOS 26, tvOS 26, watchOS 26, visionOS 26, *)`.
- [x] Shipped (Measure): `NebulaMeasureConfiguration` (the 4th config struct — carries `measure(_:operation:)`/`bench(_:iterations:warmup:operation:)` ON the config, mirroring `NebulaLogConfiguration.log`; NO separate `NebulaMeasure` type) + `NebulaMeasureResult` (minimal: name/iterations/total/perIteration — no p50/p99 yet) + `NebulaMeasureConfig` (process-wide `Mutex` accessor). `NebulaSignposter` lives under Logging. No `NebulaClock` (deferred — measure uses `any Clock<Duration>` directly, default `ContinuousClock`).

### Wave G — DocC + CI + polish — DONE (tag pending)
- [x] DocC catalog articles at `Sources/Nebula/Nebula.docc/` (root `Nebula.md` + subsystem articles) — auto-discovered by SwiftPM
- [x] GitHub CI (`.github/workflows/ci.yml`) — 5-platform matrix (iOS/macOS/tvOS/watchOS/visionOS) + `swift build -c release` to exercise `#if os()` coverage; Xcode 26.4+ pinned
- [x] DocC generation via `xcodebuild docbuild` (no `swift-docc-plugin` — `Package.swift` keeps `dependencies: []` pristine)
- [x] README examples (logger + error + format) — corrected to the shipped API (`NebulaErrorConfig.report(error)`, redaction-preserving `logger.osLogger.info(...)` vs simple `logger.info(...)`)
- [x] Per-platform `#if os()` coverage pass — no `#if os(...)` in `Sources/` today (no platform-specific API shipped yet); the CI matrix is the forward-looking guard for when the deferred macOS-only `NebulaLogStoreExporter` lands
- [ ] Tag `0.1.0` — ready to tag (user action)

## Done (0.2.0 — Clean Architecture toolkit)

### Wave H — Clean Architecture toolkit — DONE
- [x] Shipped `Sources/Nebula/Architecture/` — the **second surface** of Nebula (foundation + architecture). Seams only — no presentation, DB, or framework code. Presentation patterns (MVVM/MVC/VIP/VIPER) explicitly out of scope; Cosmos is the presentation layer. Pure Swift + Foundation + `Synchronization`; every symbol at the Nebula 26 floor (no above-floor gates).
- [x] `Domain/` — `NebulaValue`/`NebulaEntity`/`NebulaAggregate` markers + `NebulaID<Entity>` phantom-typed UUID identity. 1-param (generic-parameter defaults rejected on this toolchain — verified `swiftc -parse`).
- [x] `Ports/` — bare `Sendable` markers `NebulaInputPort`/`NebulaOutputPort`/`NebulaDTO`.
- [x] `Errors/` — `NebulaFailure` protocol + per-layer open structs `NebulaDomainError`/`NebulaValidationError` (Equatable/Hashable derived) bridging to the closed `NebulaError.Kind` via caller-picked `toNebulaError(kind:)`. `NebulaError+Mapping.swift` dispatches `NebulaFailure` before the `NSError` fallback. No new `Kind` cases.
- [x] `UseCase/` — `NebulaUseCase<I, O>` generic `Sendable` struct + `NebulaUseCaseRole` (command/query) + `execute(_:)` (untyped `throws`) / `executeTyped(_:) async throws(NebulaError)` (SE-0413); `.logged`/`.measured`/`.reported`/`.instrumented` decorators route to the existing log/measure/error configs (no 5th config).
- [x] `Repository/` — PAT `NebulaRepository<Element>` + capability sub-protocols (read-only / keyed / writable / deletable); `stream()` returns concrete `AsyncThrowingStream` (`some AsyncSequence` illegal in a protocol requirement); `NebulaRepositoryError` (`Source` enum + open `Kind` + factory statics). No CRUD mandate, no `update` verb.
- [x] `Gateway/` — `NebulaGateway` marker + `NebulaGatewayConfiguration` (reuses `NebulaJSONDecoder`/`Encoder`) + `NebulaGatewayConfig` Mutex accessor. Concrete `NebulaHTTPGateway` deferred.
- [x] `Validation/` — `NebulaValidator<T>` (sync, short-circuit, `+`) + `NebulaAsyncValidator<T>` (async; a thrown I/O error propagates, NOT a `.failure`).
- [x] `Registry/` — `NebulaRegistryKey` (open struct) + `NebulaRegistryConfiguration` + `NebulaRegistry` (explicit injection) + `NebulaRegistryConfig` (process-wide Mutex). DI **without** a container.
- [x] `Testing/` — in-target test doubles `NebulaFakeRepository` / `NebulaStubUseCase` / `NebulaSpyUseCase` (`final class` + `let Mutex`; `Sendable` **derived** — final class with all-`let` `Sendable` properties, no `@unchecked`; the spy is explicitly `: Sendable` so it can be shared across tasks). Swift Testing `confirmation(expectedCount:)` for the spy.
- [x] `Async/` — `NebulaResultPipeline<T>` (`map`/`flatMap`/`recover` over `Result<T, NebulaError>`) + `AsyncSequence.nebulaChunked(byCount:)`/`nebulaUniqued(on:)`/`nebulaUniqued()`.
- [x] Tests — `ArchitectureDomainTests`/`PortsTests`/`ErrorsTests`/`UseCaseTests`/`RepositoryTests`/`GatewayTests`/`ValidationTests`/`RegistryTests`/`TestDoublesTests`/`AsyncFlowTests`. 509 tests / 107 suites green; zero concurrency warnings under Swift 6 mode (`rm -rf .build && swift build && swift test && swift build -c release`).
- [x] DocC — `Architecture.md` canonical article + 10 per-subsystem articles; linked from the module root.
- [x] Root docs — `ARCHITECTURE.md` (Architecture section), `DECISIONS.md` (Wave H ADR), `CHANGELOG.md` (0.2.0), `VERSIONING.md` (toolkit at-floor), this roadmap. Vault: 11 architecture notes marked shipped.
- [ ] Tag `0.2.0` — ready to tag (user action)

### Deferred (tracked below in "Later")
- `NebulaInvariant` (decision #6), `NebulaMockRepository` (#8), `NebulaHTTPGateway` (#8-resolved), `NebulaCancellation`/`NebulaError.wrapAsync` (#13), template multi-module product (#10).

## Done (0.3.0 — Presentation architecture + Meridian)

### Wave I — Nebula Foundation-only presentation seams — DONE
- [x] `Sources/Nebula/Architecture/Presentation/` — `NebulaRoute` (marker: `Hashable`/`Sendable`/`Codable`), `NebulaNavigationStack<Route>` (typed `[Route]` stack; single source of truth via `static` stack mutators, instance API delegates), `NebulaRouter<Route>: Sendable` (**async** navigation-intent port), `NebulaViewModel` (bare `Sendable` marker — Nebula ships no `@Observable`), `NebulaSpyRouter<Route>` (`Mutex`-backed test double). No SwiftUI.
- [x] Async port — the Swift 6 conformance fix: a sync `@MainActor` method witnesses a nonisolated async requirement (await hops the actor), so Nebula stays `@MainActor`-free while the Meridian `@Observable` Router conforms. Two `pop` requirements (`pop()` + `pop(_ count:)`) because default args are illegal in protocol methods.
- [x] Tests — `ArchitecturePresentationTests` (16 tests). 525 Nebula tests green; zero concurrency warnings (`rm -rf .build && swift build && swift test && swift build -c release`).
- [x] DocC — `ArchitecturePresentation.md` canonical article; vault `03-padroes/nebula-presentation-seams.md`.

### Wave II — Meridian package scaffold — DONE
- [x] `Meridian/` sibling SwiftPM package (`Package.swift`: `swift-tools-version: 6.3`, `swiftLanguageModes: [.v6]`, 5 platforms `.v26`, `defaultLocalization: "en"`, `.package(name: "Nebula", path: "../")`). One repo, one CI, separate module graph → `import Meridian` from Nebula is a hard compile error (closes Wave H open risk; SR-1393 only applies within one package's `.build`).
- [x] `@MainActor @Observable public final class Router<Route: NebulaRoute>: NebulaRouter<Route>` — sync `@MainActor` impls delegating to `NebulaNavigationStack` statics (witness async port via hop); `Sendable` by `@MainActor` isolation (no `@unchecked`).
- [x] `MeridianNavigationStack` — `NavigationStack(path:)` + `navigationDestination(for:)` wrapper, `@Bindable` binding.
- [x] Tests — `RouterTests` (6 tests). 6 Meridian tests green; zero warnings; release clean. Vault `03-padroes/nebula-meridian-router.md`.

### Wave III — enum destinations + deep-link + example — DONE
- [x] `MeridianExample` executable target (`@main App`) — `Router<AppRoute>` + `MeridianNavigationStack` + `Destination` enum sheet + `onOpenURL` deep link. Compile gate / living docs (NOT a shipped product).
- [x] Pattern 1 — deep link as data: pure `URL → [Route]` function, asserted as a value, then `router.replaceStack(with:)`.
- [x] Pattern 2 — type-driven modal destinations: single `Optional<Destination>` enum drives `sheet(item:)` ("impossible states unrepresentable", no `@CasePathable` — `dependencies: []`).
- [x] Tests — `DeepLinkTests` (7 tests). 13 Meridian tests / 3 suites green; zero warnings; release clean (incl. executable). Vault `03-padroes/nebula-presentation-destinations-deeplink.md`; DocC `Meridian.docc/NavigationPatterns.md`.

### Wave IV — ADR + versioning + final gate — DONE
- [x] `DECISIONS.md` — presentation-architecture ADR (option d, async port, sibling package). `VERSIONING.md` — Meridian N ↔ Nebula N ↔ OS N lockstep policy. `ARCHITECTURE.md` — Presentation (Meridian) section + subtree row. This roadmap.
- [x] Vault — `presentation-architecture-open-questions.md` Q4 marked decided (d); `vault/Home.md` index updated.
- [x] Tag `0.3.0` — tagged + pushed (user action, completed)
- [ ] Promote Meridian to its own public git repo + tag stream (documented future step; until then Meridian ships untagged, consumed by path)

## Done (0.4.0 — Data + Network + Aurora)

### Wave N1 — Network (NebulaHTTPGateway + NebulaRetry) — DONE
- [x] `Architecture/Async/NebulaRetry.swift` — `NebulaRetryPolicy` (Sendable value, NOT `Equatable` — stores a `@Sendable (any Error) -> Bool` `isRetriable` predicate; `maxAttempts`/`baseDelay`/`multiplier`/`maxDelay`/`jitter` + `.with*` builders; `delay(forFailedAttempt:)` exponential + full/equal jitter; `defaultIsRetriable` retries transient `URLError` + HTTP 408/429/500/502/503/504) + `NebulaRetry.withPolicy(_:sleeper:operation:)` (cancellation-aware — `Task.checkCancellation()`; a `CancellationError` is never retried; injectable sleeper) + `NebulaRetryJitter`.
- [x] `Architecture/Gateway/NebulaHTTPGateway.swift` — concrete `NebulaGateway` over `URLSession`: `get`/`post`/`put`/`delete` (decode `T` or raw `Data`); reuses `NebulaGatewayConfiguration`'s `NebulaJSONDecoder`/`Encoder`; retries via `NebulaRetry`; bridges `URLError` + HTTP status → `NebulaError` (kind `.network`, built explicitly via `NebulaError(code:kind:)` — NO new `Kind` case) + `NebulaHTTPStatusError`.
- [x] Tests — `ArchitectureRetryTests` (16) + `ArchitectureHTTPGatewayTests` (13, `URLProtocol`-backed `URLSession`, `@Suite(.serialized)`). 557 Nebula tests / 113 suites green; zero warnings; release clean. Vault `03-padroes/nebula-network-retry.md`; DocC `ArchitectureAsync.md`/`ArchitectureGateway.md`.

### Wave N2 — Preferences (NebulaPreferences + NebulaDefaults) — DONE
- [x] `Architecture/Preferences/NebulaPreferences.swift` — `Sendable` key-value port: 3 byte-level requirements (`data`/`setData`/`remove`) + a **default extension** (`value`/`setValue` Codable bridge + `rawValue`/`setRawValue` RawRepresentable bridge, `RawValue: Codable`) every conformer inherits. Plain `JSONEncoder`/`JSONDecoder` (per-call, decoupled from the gateway config).
- [x] `Architecture/Preferences/NebulaDefaults.swift` — `final class` `Mutex<UserDefaults>` façade; `UserDefaults` is `@_nonSendable(_assumed)` (verified) so the `Mutex` gives region-based isolation; `init(_ defaults: sending UserDefaults = .standard)` (SE-0430 — ownership transfers at the call site); `Sendable` derived, no `@unchecked`.
- [x] Tests — `ArchitecturePreferencesTests` (17) — byte-level, Codable (round-trip/absent/corrupt→`DecodingError`), RawRepresentable (String/Int/unmappable→nil), an `InMemoryPrefs` `final class` proving the default extension works on a non-`UserDefaults` conformer, existential holding both impls, Sendable-across-`Task` + 50-task concurrent access. 574 Nebula tests / 118 suites green; zero warnings; release clean. Vault `03-padroes/nebula-preferences.md`; DocC `ArchitecturePreferences.md`.

### Wave N3 — Persistence (Aurora sibling package — SwiftData) — DONE
- [x] `Aurora/` sibling SwiftPM package (`Package.swift`: `swift-tools-version: 6.3`, `swiftLanguageModes: [.v6]`, 5 platforms `.v26`, `defaultLocalization: "en"`, `.package(name: "Nebula", path: "../")`). One repo, one CI, separate module graph → `import Aurora` from Nebula is a hard compile error (verified — closes the SwiftData placement risk; mirrors Meridian).
- [x] `AuroraEntityMapping` — type-level protocol (static methods) bridging a SwiftData `@Model` (`PersistentModel`) to a Nebula `NebulaEntity` DTO: `toEntity`/`insert(_:in:)`/`update(_:from:)`/`descriptor(for:)`/`descriptor()`. Conformed with a caseless `enum: AuroraEntityMapping, Sendable`.
- [x] `AuroraRepository<Mapping: AuroraEntityMapping & Sendable>` — `@ModelActor` `actor` conforming to `NebulaRepository` + read-only + keyed + writable + deletable. `@ModelActor` synthesizes the isolated `ModelContext` + `init(modelContainer:)`. `stream()` is `nonisolated` + Task hop (the port is synchronous); `count()`/`find(id:)`/`save(_:)`/`delete(_:)` are `async` on the actor. `Mapping: Sendable` absorbs the SE-0470 isolated-conformance warning. SwiftData errors rethrown untyped.
- [x] `AuroraExample` executable — `@Model` + `NebulaEntity` + mapping + in-memory `ModelContainer` + `AuroraRepository` CRUD round-trip; `swift run AuroraExample` is the compile gate (NOT a shipped product).
- [x] Tests — `AuroraRepositoryTests` (12) — CRUD (save insert + add-or-replace, find present/absent, count, stream all/empty, delete present/absent-no-op), port conformance for all four capability ports (assign-to-existential + cast-back), Sendable-across-`Task`. 12 Aurora tests / 3 suites green; zero warnings; release clean. Vault `03-padroes/nebula-aurora-swiftdata.md`; DocC `Aurora.docc/Aurora.md`.

### Wave N4 — ADR + versioning + final gate — DONE (tag pending)
- [x] `DECISIONS.md` — data+network ADR (Q1 = (c) Aurora sibling package, mirroring Meridian (d)). `VERSIONING.md` — Aurora N ↔ Nebula N ↔ OS N lockstep policy. `ARCHITECTURE.md` — Data + Network (Aurora) section + subtree rows + Structure tree. This roadmap.
- [x] Vault — `data-network-open-questions.md` Q1 marked decided (c); `vault/Home.md` index updated (N1/N2/N3 shipped links).
- [~] Tag `0.4.0` — **not tagged separately**; N1–N4 folded into the `0.5.0` release (owner decision, 2026-07-19). The `Done (0.4.0)` section above remains the milestone record.
- [ ] Promote Aurora to its own public git repo + tag stream (documented future step; until then Aurora ships untagged, consumed by path)

### Deferred (tracked below in "Later")
- `NebulaCancellation`/`NebulaError.wrapAsync` (Wave H decision #13; N1 used a minimal `Task.checkCancellation()` form), WebSocket/SSE/higher-level remote API client (Q4 — defer until a second use earns it), preferences key-namespace policy + change observation (Q6 — lean v0.4 surface), Aurora `@Query` helper / `ModelContainer` factory / schema migration / relationship-walking (lean v0.4 surface).

## Done (0.5.0 — Network layer)

### Wave N5 — Endpoint / Client / Request model + gateway refactor — DONE
- [x] `Architecture/Network/NebulaHTTPMethod.swift` — `enum: String, Sendable, Equatable, Hashable` (get/post/put/patch/delete/head).
- [x] `Architecture/Network/NebulaHTTPEndpoint.swift` — the **port**: `protocol: Sendable { func urlRequest(against baseURL: URL?) throws -> URLRequest }` (non-generic, `URLRequestConvertible`, existential-friendly); `cachePolicy` via a default extension (`.protocolDefault`), overridable by conformers (a protocol requirement so existential dispatch honors the override).
- [x] `Architecture/Network/NebulaHTTPRequest.swift` — the concrete **value type** (`struct: NebulaHTTPEndpoint, Sendable, Equatable`): `method`/`path`/`query`/`headers`/`body`/`cachePolicy`; `urlRequest(against:)` resolves the URL (relative→baseURL, absolute path, query appended not replaced — replicates Wave N1). **Reused as the server-side parsed-request type.**
- [x] `Architecture/Network/NebulaHTTPBody.swift` — `enum: Sendable, Equatable { none, data(Data, contentType:), static func json(_:using:) throws }` — `.json` encodes **eagerly** so the value stays `Sendable`.
- [x] `Architecture/Network/NebulaHTTPResponse.swift` — `struct: Sendable, Equatable { statusCode, headers, body: Data }` + `decode<T: Decodable & Sendable>(_:using:)`.
- [x] `Architecture/Network/NebulaHTTPClient.swift` — the **client port**: `protocol NebulaHTTPClient: NebulaGateway { func send(_ endpoint:) async throws -> NebulaHTTPResponse }` (non-generic, Point-Free `send(_:)->Response` shape — existential-friendly); default extensions `send<T>(_:as:)` (decode) + the verb conveniences (`get`/`post`/`put`/`delete`) preserving the Wave N1 signatures.
- [x] `Architecture/Gateway/NebulaHTTPGateway.swift` — refactored to conform to `NebulaHTTPClient` (verbs → default extension, `send(_:)` is core; `buildRequest` merges config headers for keys the request didn't set — per-request overrides config defaults; error bridging unchanged, no new `Kind` case).
- [x] Tests — `ArchitectureNetworkTests` (endpoint→URLRequest building, response decode, client decode + verbs) + existing `ArchitectureHTTPGatewayTests` pass unchanged.

### Wave N6 — Per-endpoint cache (Nebula over native URLCache) — DONE
- [x] `Architecture/Network/NebulaHTTPCachePolicy.swift` — `enum: Sendable, Equatable, Hashable { protocolDefault, bypass, store(ttl: Duration), staleWhileRevalidate(ttl: Duration, maxStale: Duration) }`; defaulted on `NebulaHTTPEndpoint` to `.protocolDefault`.
- [x] `Architecture/Network/NebulaHTTPCache.swift` — the **cache port**: `protocol: Sendable { response(for:policy:) -> NebulaCachedResponse?, store(_:for:policy:), remove(for:), removeAll() }`; `NebulaCachedResponse` carries `isStale` so the gateway kicks a background revalidate only when stale.
- [x] `Architecture/Network/NebulaURLCache.swift` — the **façade**: `final class` wrapping `Mutex<State>` (native `URLCache` + Nebula TTL metadata; `URLCache` not `Sendable` → `Mutex` is the isolation boundary, `final class` derives `Sendable`, no `@unchecked`); `init(_ cache: sending URLCache = .shared)`. Fresh within TTL; stale within `ttl + maxStale`; orphaned-metadata cleanup if bytes evicted.
- [x] `Architecture/Gateway/NebulaGatewayConfiguration.swift` — `cache: NebulaHTTPCache?` (default `nil`) threaded through all `.with*` builders + `withCache(_:)`.
- [x] `Architecture/Gateway/NebulaHTTPGateway.swift` — `send` consults the cache (fresh → skip network; stale → serve + `Task.detached` background revalidate-and-store; 2xx → store); `.store`/`.staleWhileRevalidate` set `URLRequest.cachePolicy = .reloadIgnoringLocalCacheData` so Nebula's TTL wins.
- [x] Tests — `ArchitectureHTTPCacheTests` (10) + 5 gateway cache integration tests in `ArchitectureHTTPGatewayTests`. 635 Nebula tests / 127 suites green; zero warnings; release clean. Vault `03-padroes/nebula-network-endpoint-client.md`; DocC `ArchitectureHTTPCache.md`.

### Wave N7 — Local HTTP server (Network.framework NWListener) — DONE
- [x] `Architecture/Network/NebulaHTTPServer.swift` — `final class: Sendable` over `NWListener`; each `NWConnection` runs in a `Task` (async-wrapped `receive`/`send`); `OnceFlag` `Mutex<Bool>` so `start()` resumes its continuation exactly once; `init(port:handler:)`/`start() async throws`/`stop()`; `port: NWEndpoint.Port?`. Scope "simple" (plain HTTP/1.1, no TLS/chunked/keep-alive, `Content-Length` body only). `Sendable` derived (no `@unchecked`).
- [x] `Architecture/Network/NebulaHTTPRequestParser.swift` — internal bounded HTTP/1.1 parser; rejects negative/non-numeric/oversized (`> 10 MiB`) `Content-Length` (adversarial-review crash fix — a negative value would build a reversed `Range` and trap).
- [x] `Architecture/Network/NebulaHTTPServerError.swift` — per-layer open struct `: NebulaFailure, Equatable, Hashable` (mirrors `NebulaRepositoryError`); `coarseKind` → `NebulaError.Kind`; `toNebulaError(kind:)`; `NWError` folded into message (not boxed, lossy).
- [x] Serializer hardenings — case-insensitive `Content-Length` overwrite; `\r\n` strip on handler headers.
- [x] Tests — `ArchitectureHTTPServerTests` (parser / serializer / error + real localhost round-trip `NebulaHTTPServer` + `NebulaHTTPGateway` over `URLSession`, `@Suite(.serialized)`, OS-assigned ephemeral port — no `URLProtocol` stub). 635 Nebula tests / 127 suites green; zero warnings; release clean. Vault `03-padroes/nebula-network-endpoint-client.md`; DocC `ArchitectureHTTPServer.md`.

### Wave N8 — ADR + governance + final gate — DONE (tag pending)
- [x] `DECISIONS.md` — Network layer ADR (Waves N5–N8: non-generic client, cache-over-native-URLCache, NWListener server; Accepted). `ARCHITECTURE.md` — `Network/` subtree row + structure tree + Data + Network prose. This roadmap.
- [x] Vault — `nebula-network-endpoint-client.md` marked `status: shipped` / `shipped: "0.5.0"`; `vault/Home.md` index updated (N5–N8 shipped link).
- [x] DocC — `ArchitectureNetwork.md`/`ArchitectureHTTPCache.md`/`ArchitectureHTTPServer.md` (indexed in `Architecture.md` after `ArchitectureGateway`).
- [x] Final gate — `rm -rf .build && swift test` (635 tests / 127 suites, zero warnings); Aurora green; `swift build -c release` clean; per-platform `xcodebuild` (iOS/macOS/tvOS/watchOS/visionOS); DocC `xcodebuild docbuild`.
- [x] Tag `0.5.0` — tagged (user action, completed)

### Deferred (tracked below in "Later")
- Request middleware/interceptors (auth-token injection, 401 refresh-and-retry) — **shipped as Wave N10 / 0.7.0** (`NebulaHTTPInterceptor` + chain + `NebulaTokenProvider` + `NebulaAuthInterceptor` actor + `intercepted(by:)`). Streaming (`bytes(for:)`/SSE/WebSocket — Q4 deferred), multipart upload, `download(for:)`-to-disk, pagination `AsyncSequence` remain later waves.

## Done (0.6.0 — Keychain + secure store)

> App-readiness Wave N9 — the secure-storage seam. First wave under the resolved "non-UI Apple system frameworks" scope (`import Security` in-bounds). Ships as Nebula 0.6.0 (additive minor within Nebula 26 — new types only, no breaking changes). Tagging deferred to the owner's gate decision.

### Wave N9 — Keychain façade + SecureStore port — DONE (tag pending)
- [x] `Architecture/Keychain/NebulaSecureStore.swift` — the **port**: `protocol NebulaSecureStore: Sendable` with three `throws` byte-level requirements (`data(forKey:)` nil=absent / `setData(_:forKey:)` nil=remove / `remove(forKey:)`) + a default extension (`value`/`setValue` Codable bridge, `rawValue`/`setRawValue` RawRepresentable bridge, per-call coders). Distinct from `NebulaPreferences` (different threat model).
- [x] `Architecture/Keychain/NebulaKeychainAccessible.swift` — `enum: Sendable, Equatable, Hashable` mapping 1:1 to `kSecAttrAccessible*` (5-platform, no gates). Default `.whenUnlocked`.
- [x] `Architecture/Keychain/NebulaKeychainConfig.swift` — `struct: Sendable, Equatable` (`service` required / `accessGroup`? / `accessible` / `useDataProtectionKeychain` default `true`) + `.withService`/`.withAccessGroup`/`.withAccessible`/`.withUseDataProtectionKeychain`. No `static let default`; no process-wide `Mutex` accessor.
- [x] `Architecture/Keychain/NebulaKeychain.swift` — the **stateless `final class`** façade over `SecItemAdd`/`SecItemCopyMatching`/`SecItemUpdate`/`SecItemDelete`. `let config` → `Sendable` **derived** (no `Mutex`, no `@unchecked` — the `NebulaError.Box` precedent, NOT the `NebulaDefaults` `Mutex` precedent). Fresh query per call; `SecItemUpdate` over delete-re-add; `errSecInteractionNotAllowed` non-destructive; `remove` idempotent.
- [x] `Architecture/Keychain/NebulaKeychainError.swift` — per-layer open struct `: NebulaFailure, Equatable, Hashable`; open-struct `Kind` (presets); stores `OSStatus` (not boxed); `coarseKind` → `.cocoa`/`.unknown`; `toNebulaError(kind:)` writes `NebulaOSStatus` metadata. No new `NebulaError.Kind` case.
- [x] Tests — `ArchitectureKeychainTests` (27): error struct, port seam (`InMemorySecureStore` conformer), real macOS host Keychain integration (`@Suite(.serialized)`, unique service + `defer` cleanup, `useDataProtectionKeychain = false` for the unsigned host), Sendable + 50-task concurrency. 662 Nebula tests / 131 suites green; zero warnings; release clean.
- [x] Test-stability fix — `MeasureTests.swift` `NebulaMeasureConfigTests` marked `@Suite(.serialized)` (pre-existing race on the process-wide `NebulaMeasureConfig` accessor under Swift Testing parallelism, surfaced by N9's shifted scheduling).
- [x] DocC — `ArchitectureKeychain.md` (indexed in `Architecture.md` after `ArchitecturePreferences`); `NebulaKeychainError` added to the error-taxonomy paragraph. `xcodebuild docbuild` → BUILD DOCUMENTATION SUCCEEDED.
- [x] Final gate — `rm -rf .build && swift test` (662 tests / 131 suites, zero warnings); Aurora (12) + Meridian green; `swift build -c release` clean; per-platform `xcodebuild` (iOS/macOS/tvOS/watchOS/visionOS) BUILD SUCCEEDED; DocC `xcodebuild docbuild`.
- [x] Vault — `nebula-keychain.md` marked `status: shipped` / `shipped: "0.6.0"`; `vault/Home.md` index updated (N9 shipped link); hub `nebula-app-readiness-research.md` N9 row → shipped.
- [x] Governance — `DECISIONS.md` N9 ADR (Accepted); `ARCHITECTURE.md` `Keychain/` subtree row + structure tree + prose; this roadmap; `CHANGELOG.md` 0.6.0 entry.
- [ ] Tag `0.6.0` — **pending** (owner gate decision).

### Deferred (tracked below in "Later")
- Biometry + `SecAccessControlCreateWithFlags` (`LAContext` is `API_UNAVAILABLE(tvOS)` at the class level → can't be 5-platform); the rest of the app-readiness sequence (N11–N18 + A1–A3 per the hub `nebula-app-readiness-research.md`).

## Done (0.7.0 — HTTP interceptors + 401 refresh-and-retry)

> App-readiness Wave N10 — the interceptor seam + 401 refresh-and-retry. Nebula's first `actor` (`NebulaAuthInterceptor`, single-flight refresh). Ships as Nebula 0.7.0 (additive minor within Nebula 26 — new types only, no breaking changes). The existing `NebulaRetry.withPolicy` cannot mutate the request between attempts, so a 401 cannot refresh-and-retry through that seam — the interceptor port fills the gap. Tagging deferred to the owner's gate decision.

### Wave N10 — Interceptor port + chain + auth actor — DONE (tag pending)
- [x] `Architecture/Network/NebulaHTTPInterceptor.swift` — the **port** (`protocol NebulaHTTPInterceptor: Sendable` — `adapt(_:)` / `retry(_:for:attempt:)`) + `NebulaHTTPInterceptorChain` (`Sendable` struct, compose left-to-right, retry-once from the original endpoint, `CancellationError` not retried) + `NebulaInterceptedClient` (`Sendable` struct conforming to `NebulaHTTPClient`) + `NebulaHTTPClient.intercepted(by:)` default extension.
- [x] `Architecture/Network/NebulaTokenProvider.swift` — the **port** (PAT, `associatedtype Token: Sendable`; `currentToken()` / `refresh()` / `authorizationHeader(for:)`).
- [x] `Architecture/Network/NebulaAuthInterceptor.swift` — the concrete **`actor`** (Nebula's first). `adapt` injects `Authorization: Bearer <token>` (anonymous passthrough when no token); `retry` matches the 401 `NebulaError` at `attempt == 0`, refreshes **single-flight** via an actor-isolated `Task<Token, any Error>?`, retries once. Private `_NebulaAuthAdaptingEndpoint` wrapper injects the header at the `URLRequest` layer. No new `NebulaError.Kind` case.
- [x] Tests — `ArchitectureAuthTests` (15): chain, adapt, 401 (refresh-and-retry / non-401 / second-401 no-loop / refresh-failure), concurrency `@Suite(.serialized)` (single-flight: two concurrent 401s share one refresh; 50 concurrent always-200), `intercepted` (verbs funnel, composition). 677 Nebula tests / 136 suites green; zero warnings; release clean.
- [x] DocC — `ArchitectureAuth.md` (indexed in `Architecture.md` after `ArchitectureHTTPServer`, network cluster). Error-taxonomy paragraph unchanged (N10 adds no error type). `xcodebuild docbuild` → BUILD DOCUMENTATION SUCCEEDED.
- [x] Final gate — `rm -rf .build && swift test` (677 tests / 136 suites, zero warnings, 3× runs deterministic); Aurora (12) + Meridian green; `swift build -c release` clean; per-platform `xcodebuild` (iOS/macOS/tvOS/watchOS/visionOS) BUILD SUCCEEDED; DocC `xcodebuild docbuild`.
- [x] Vault — `nebula-auth-interceptor.md` marked `status: shipped` / `shipped: "0.7.0"`; `vault/Home.md` index updated (N10 shipped link); hub `nebula-app-readiness-research.md` N10 row → shipped; `nebula-keychain-auth.md` "Shipped as N10" pointer.
- [x] Governance — `DECISIONS.md` N10 ADR (Accepted); `ARCHITECTURE.md` `Network/` subtree (interceptor types) + structure tree + prose; this roadmap; `CHANGELOG.md` 0.7.0 entry.
- [ ] Tag `0.7.0` — **pending** (owner gate decision).

### Deferred (tracked below in "Later")
- Biometry + `SecAccessControlCreateWithFlags` (`LAContext` is `API_UNAVAILABLE(tvOS)` at the class level → can't be 5-platform); configurable max-retry count for the auth interceptor; a public `NebulaError.Code`/domain constant for the 401 match; the rest of the app-readiness sequence (N15b–N18 + A1–A3 per the hub `nebula-app-readiness-research.md`).

## Done (0.15.0 — Streaming (SSE + WebSocket))

> App-readiness Wave N17b — the streaming toolkit (the second sub-wave of the N17 split; N17c bodies+downloads follows). **Two additive surfaces; the gateway is untouched; pinning is reused via composition/forwarding (zero N17a change).** SSE + WebSocket are additive over the existing buffered HTTP gateway (`NebulaHTTPGateway.send` returns a `NebulaHTTPResponse` with `body: Data`; there is no streaming method on `NebulaHTTPClient`). The gateway diff is empty. `NebulaWebSocketSessionDelegate` holds a `NebulaURLSessionDelegate` (N17a) and forwards `urlSession(_:didReceive:completionHandler:)` to it — **zero N17a source change, zero pinning-logic duplication** (the auth-challenge method lives on `URLSessionDelegate`, which `URLSessionWebSocketDelegate` inherits, so the **same** session delegate handles pinning for both HTTP(SSE) and WebSocket). No `NebulaGatewayConfiguration.streaming`/`.sse`/`.webSocket` field; the `Nebula.md` config list is unchanged. **Ground-truth correction — `URLSessionWebSocketTask` IS `NS_SWIFT_SENDABLE`** (`NSURLSession.h:1121`) — the prior research note's "non-Sendable" premise was FALSE for this SDK → the façade's `let task` **derives** `Sendable`; the combined `NebulaWebSocketSessionDelegate: final class : NSObject, URLSessionWebSocketDelegate, Sendable` **derives** `Sendable` (probe EXIT=0, Xcode 27 Beta 3 SDK; the `NebulaUNNotificationCenter` analogy — `URLSessionWebSocketDelegate` is an `@objc` protocol NOT `NS_SWIFT_SENDABLE`, but conformance to a non-Sendable `@objc` protocol does NOT block derived `Sendable` on a `final class` with all-`let` Sendable props). **Zero `@unchecked`** in all of N17b. **CRITICAL — `AsyncLineSequence` skips empty lines:** `URLSession.AsyncBytes.lines` does NOT yield empty lines, but SSE dispatches on **blank lines**, so `.lines` is UNSUITABLE. The stream iterates the raw `bytes` (UInt8) directly, accumulates into a `Data` line buffer, feeds the parser on `\n` (0x0A) including empty lines (an empty buffer on `\n` is the blank line that dispatches); trailing partial line flushed at stream end. **CRITICAL — `AsyncThrowingStream` cancellation semantics:** the plan/vault said "CancellationError rethrown to consumer" — INCORRECT (verified empirically): when the consumer cancels its `Task`, `Iterator.next()` returns `nil` (the `for try await` ends **normally**, does NOT throw `CancellationError`); `onTermination` fires with `.cancelled`. The internal `finish(throwing: CancellationError())` tears the `bytes(for:)` task down via `onTermination → loop.cancel()` — the consumer never observes it. The `cancellationEndsTheStreamGracefully` test asserts graceful termination, NOT `throws: CancellationError`. **Custom SSE reconnect loop, NOT `NebulaRetry.withPolicy`** — the request mutates per attempt (the `Last-Event-ID` header advances with the cursor), and `withPolicy`'s `operation` is nullary. The custom loop mirrors `withPolicy`'s cancellation contract — cancellation is honored immediately and **never retried**. **SSE parser is pure** — `internal struct NebulaSSEParser` with `@discardableResult mutating feed(_ line: String) -> NebulaSSEEvent?`; no `URLSession`, no `@Sendable` closure, no I/O — unit-tested with canned `[String]`. WHATWG-compliant: `:` comment ignored; `field: value` split on first `:`, one space after stripped; `event`/`data`/`id`/`retry` fields; `data` multi-line join with `\n` + trailing-newline strip; `id` rejected if U+0000; blank line dispatches (when `data` non-empty), resetting `data`/`event` but **preserving** `lastEventID`/`retry`; unknown fields ignored; trailing `\r` stripped defensively. **`NebulaWebSocketMessage` failable bridge** — Nebula-owned enum (`case data`/`case string`) mirroring `URLSessionWebSocketTask.Message` (the idiom — never expose Apple's nested enum in a Nebula port); `init?(_ message:)` with `@unknown default: return nil` (non-frozen → future case surfaces as `NebulaWebSocketError.unknown`). `URLSessionWebSocketTask.Message` is `Sendable` but NOT `Equatable` → bridge tests use round-trip + pattern match. **`NebulaSSEConfiguration` is per-call, NOT process-wide** → **no `Mutex` accessor**; `Sendable` **NOT `Equatable`** (the `@Sendable` sleeper, mirroring `NebulaLogConfiguration`). `sendPing` has only the completion-handler form (no async overlay) → wrapped in `withCheckedThrowingContinuation`. **Open-struct errors, no new `NebulaError.Kind` case** — `NebulaSSEError` (`connect-failed`/`parse-failed`/`reconnect-exhausted`/`cancelled`/`unknown`) + `NebulaWebSocketError` (`connect-failed`/`send-failed`/`receive-failed`/`ping-failed`/`closed`/`cancelled`/`unknown`; `closed(code:reason:)` carries the `CloseCode` in metadata + reason in message); both `coarseKind` switch to existing `.network`/`.unknown` with `default → .unknown`; domains `"Nebula.NebulaSSEError"`/`"Nebula.NebulaWebSocketError"`. **Testability asymmetry — SSE via `URLProtocol`, WebSocket via `NWListener`:** `URLProtocol` intercepts `URLSession.bytes(for:)` data tasks (a `NebulaSSETestProtocol` serves canned SSE bytes + a `hang` flag for the cancellation test; `@Suite(.serialized)`); `URLProtocol` does NOT intercept `URLSessionWebSocketTask` → the live round-trip uses an `NWListener` echo server (`Network.framework` admissible, `NebulaHTTPServer` precedent) with `NWProtocolWebSocket.Options.autoReplyPing` + `setClientRequestHandler` handshake. **Echo bug found+fixed:** the echo `send` MUST carry `NWProtocolWebSocket.Metadata(opcode:)` in the `ContentContext` (extracted from `context.protocolMetadata`) or the frame is invalid and the client receive fails with "Socket is not connected"; `NWConnection.send` label is `contentContext` (NOT `context`); `isComplete` means message-complete NOT connection-done. Ships as Nebula 0.15.0 (additive minor within Nebula 26). Tagging deferred to the owner's gate decision.

### Wave N17b — Streaming (SSE + WebSocket) — DONE (tag pending)
- [x] `Sources/Nebula/Architecture/Network/Streaming/NebulaSSEEventStream.swift` — `NebulaSSEEvent` (`Sendable`/`Equatable`/`Hashable`: `id`/`event` (default `"message"`) /`data` (multi-line joined `\n`, trailing newline stripped)/`retry: Int?` (ms)) + pure `NebulaSSEParser` (`internal struct`, `@discardableResult mutating feed(_:) -> NebulaSSEEvent?`, WHATWG state machine, `lastEventID` preserved across dispatches) + `NebulaSSEConfiguration` (`Sendable` NOT `Equatable`; per-call — no `Mutex` accessor; `reconnect`/`maxReconnectAttempts`/`reconnectDelay`/`sleeper: @Sendable`; fluent `.with*`) + `NebulaSSEEventStream` (`enum` namespace; `events(for:session:configuration:) -> AsyncThrowingStream<NebulaSSEEvent, any Error>` — the CLAUDE.md-mandated concrete return; iterates raw `bytes` NOT `.lines`; custom reconnect loop with `Last-Event-ID`, cancellation never retried). `import Foundation` only.
- [x] `Sources/Nebula/Architecture/Network/Streaming/NebulaSSEError.swift` — open-struct (`NebulaFailure, Equatable, Hashable`), nested `Kind` presets (`.connectFailed`/`.parseFailed`/`.reconnectExhausted`/`.cancelled`/`.unknown`), `coarseKind` (`.network`/`.unknown`), `toNebulaError(kind:)` (domain `"Nebula.NebulaSSEError"`, no new `NebulaError.Kind`); factory statics with `underlying: NebulaError.Box?`. Mirrors `NebulaHTTPServerError`. Foundation-only.
- [x] `Sources/Nebula/Architecture/Network/Streaming/NebulaWebSocketClient.swift` — `NebulaWebSocketMessage` (`Sendable`/`Equatable`/`Hashable` enum: `case data`/`case string`; failable `init?(_ message: URLSessionWebSocketTask.Message)` with `@unknown default: return nil`; `var rawMessage`) + `public protocol NebulaWebSocketClient: Sendable` (port, mirror `NebulaHTTPClient` without gateway inheritance; `send`/`receive`/`sendPing` `async throws`, `close(with:reason:)` `async` non-throwing, `closeCode: CloseCode?`, `maximumMessageSize: Int { get set }`). Foundation-only.
- [x] `Sources/Nebula/Architecture/Network/Streaming/NebulaURLSessionWebSocket.swift` — `public final class NebulaURLSessionWebSocket: NebulaWebSocketClient, Sendable` (façade over `URLSessionWebSocketTask`; `let pinned`/`let task`/`let logger`; `Sendable` derived — `URLSessionWebSocketTask` IS `NS_SWIFT_SENDABLE`, NO `@unchecked`; `send`/`receive`/`sendPing` (wrapped `withCheckedThrowingContinuation`)/`close`/`closeCode`/`maximumMessageSize`; `internal mapSend/mapReceive/mapPing/isCancellation/box` testable seams). Foundation-only.
- [x] `Sources/Nebula/Architecture/Network/Streaming/NebulaWebSocketSessionDelegate.swift` — `public final class : NSObject, URLSessionWebSocketDelegate, Sendable` (ONE object per session: pinning via composition + WebSocket lifecycle; `let pinningDelegate: NebulaURLSessionDelegate`/`let logger`/`let onOpen`/`let onClose`; `Sendable` derived — probe EXIT=0, NO `@unchecked`; auth-challenge **forwards** to `pinningDelegate.urlSession(_:didReceive:completionHandler:)`; `didOpenWithProtocol`/`didCloseWith`). Foundation-only (no `import Security` — the eval lives in the N17a delegate).
- [x] `Sources/Nebula/Architecture/Network/Streaming/NebulaWebSocketSession.swift` — `NebulaPinnedWebSocketSession: Sendable` (session + delegate — `URLSession` does NOT retain its delegate) + `enum NebulaWebSocketSession` + `pinned(by:configuration:logger:onOpen:onClose:)` (composes `NebulaURLSessionDelegate` inside `NebulaWebSocketSessionDelegate`) + `open(url:protocols:using:)` / `open(request:using:)` (creates the task, `.resume()`, returns the façade — the ergonomic entry point). Foundation-only.
- [x] `Sources/Nebula/Architecture/Network/Streaming/NebulaWebSocketError.swift` — open-struct mirroring `NebulaSSEError`; `Kind` presets (`.connectFailed`/`.sendFailed`/`.receiveFailed`/`.pingFailed`/`.closed`/`.cancelled`/`.unknown`); `closed(code:reason:)` carries `CloseCode` in metadata + reason in message; `coarseKind` (`.network`/`.unknown`); domain `"Nebula.NebulaWebSocketError"`; no new `NebulaError.Kind`. Foundation-only.
- [x] `Tests/NebulaTests/ArchitectureSSETests.swift` — A. parser (pure, WHATWG vectors: single/multi-line `data`, `event`, `id`/`retry`, `:` comment, unknown field, blank-line dispatch preserves `lastEventID`/`retry`, trailing `\r`, Sendable across `Task`); B. config value semantics (`.with*` distinct copies, default sleeper, Sendable-not-Equatable); C. live stream via `NebulaSSETestProtocol` (`@Suite(.serialized)`, canned SSE bytes, `lastEventID`); D. reconnect with `Last-Event-ID` + graceful cancellation (`hang` flag → 0 events, `task.value` returns promptly); E. error mapping (`URLError` → `connectFailed`, `coarseKind == .network`, domain). 18 tests / 2 suites.
- [x] `Tests/NebulaTests/ArchitectureWebSocketTests.swift` — A. message bridge (round-trip + pattern match, NOT `==`; Equatable/Hashable); B. error mapping (`internal` seams, dummy task via `webSocketTask(with:)`; `URLError(.cancelled)` → `coarseKind == .network`; `closed` carries code+reason; `cancelled` → `.unknown`); C. session builder (`pinned.session.delegate === pinned.delegate`; `pinningDelegate.pinning == pinning`; `open` returns façade retaining pinned); D. delegate lifecycle + forwarding (synthesize `didOpenWithProtocol`/`didCloseWith` → `onOpen`/`onClose` fire; auth-challenge forward via real `NebulaURLSessionDelegate` + non-server-trust challenge → `.performDefaultHandling`); E. live round-trip `WebSocketLiveEchoTests` `NWListener` echo (`sendReceivePingAndCloseRoundTrip` — echo with `NWProtocolWebSocket.Metadata(opcode:)`). 15 tests / 2 suites.
- [x] DocC — `ArchitectureStreaming.md` (Overview/SSE/WebSocket/Errors/Sendability/Testability note/Topics); `Architecture.md` index `- <doc:ArchitectureStreaming>` after `- <doc:ArchitectureSSLPinning>`; `Nebula.md` config list unchanged (SSE/WebSocket configs are per-call).
- [x] Vault — `nebula-streaming.md` (shipped — documents the `URLSessionWebSocketTask` Sendability correction + the raw-bytes-NOT-`.lines` fix + the `AsyncThrowingStream` cancellation-ends-normally correction + the custom SSE reconnect loop + the composition/forwarding reuse + the `URLProtocol`/`NWListener` testability asymmetry + the echo `Metadata(opcode:)` bug + invariants/deferred N17c); `Home.md` N17b shipped link; hub `nebula-app-readiness-research.md` N17b row → ✅ shipped 0.15.0 + wave line.
- [x] Governance — `DECISIONS.md` N17b ADR (Accepted); `ARCHITECTURE.md` prose bullet + structure tree + table row; this roadmap; `CHANGELOG.md` 0.15.0 entry. 869 Nebula tests / 175 suites green (+33 tests / +4 suites); zero warnings; release clean. Adversarial code review (end of N17b): no CONFIRMED bugs; the two load-bearing ground-truth corrections (`AsyncLineSequence` skips empty lines; `AsyncThrowingStream` cancellation ends normally) + the echo `Metadata(opcode:)` bug were fixed during implementation, not in review.
- [ ] Tag `0.15.0` — **pending** (owner gate decision).

## Done (0.16.0 — Bodies & downloads)

> App-readiness Wave N17c — the bodies & downloads toolkit (the **third and final sub-wave of the N17 split** — N17a pinning → N17b streaming → N17c bodies/downloads). **Three additive surfaces; the gateway is untouched** (`NebulaHTTPGateway`/`NebulaHTTPClient`/`NebulaHTTPBody`/`NebulaHTTPRequest`/`NebulaHTTPRequestParser` diffs empty); pinning is reused via composition (zero N17a source change, no `import Security` in the product target). **Multipart — `NebulaMultipartBuilder`, a pure `multipart/form-data` (RFC 2388) `Data` composer.** Foundation has NO multipart API (a full grep of `Foundation.swiftinterface` returns zero hits) — genuinely new app-layer code. `NebulaMultipartPart` (field/file) assembled under a random boundary (16 random bytes hex-encoded under `"----NebulaBoundary"`, within RFC 2046 §5.1.1's 0–70-char limit; **no `import CryptoKit`** — multipart doesn't hash). `build()` pure (no `URLSession`, no `@Sendable` closure, no I/O except the explicit `file(in:)` temp-file writer for `URLSession.upload(for:fromFile:)` — streaming from disk, suitable for large/background uploads). **Gateway-compatible by design**: the built `Data` + content-type feed the existing `NebulaHTTPBody.data(_:contentType:)` — **no new `NebulaHTTPBody` case**, no `NebulaHTTPRequest`/parser ripple. **Download — `NebulaDownload.download(for:session:configuration:)` → `NebulaDownloadHandle`.** A façade over `URLSession.download(for:delegate:)` (a temp-file URL deleted on return — the façade moves it via a caller-supplied `destination` closure). `progress: AsyncThrowingStream<Double, any Error>` (fraction 0.0–1.0, bridged from `URLSessionDownloadDelegate.didWriteData` — Foundation has NO async `for await` progress API, verified) + `value() async throws -> URL` (the moved destination). `cancelByProducingResumeData()` wraps the completion-handler-only `URLSessionDownloadTask.cancel(byProducingResumeData:)` in a `withCheckedContinuation` (no async form; the task is captured in the delegate since the async overlay does not expose it). The retry-with-resume loop is **custom, NOT `NebulaRetry.withPolicy`** — the resume `Data` mutates the attempt (`download(resumeFrom:)`), and `withPolicy`'s `operation` is nullary (the SSE `Last-Event-ID` precedent); failure-path resume reads `URLError.downloadTaskResumeData`. **Delegate routing (load-bearing)** — the per-task `delegate:` receives the download callbacks; SSL/TLS pinning rides the **session** delegate (session-level auth challenge) → `NebulaDownloadDelegate` holds NO pinning, `pinned(by:sessionConfiguration:configuration:logger:)` sets the N17a `NebulaURLSessionDelegate` as session delegate — **zero N17a change, no `import Security` in the product target**. **Pagination — `NebulaPagedSequence<Page: Sendable>`.** Generic `Sendable` helper returning `AsyncThrowingStream<Page, any Error>` (the CLAUDE.md-mandated concrete return). `first`/`next` `@Sendable` closures decouple cursor transport (the app's concern) from the loop (Nebula's concern), stopping when `next` returns `nil`. Custom loop, NOT `withPolicy` (the cursor mutates per page — the SSE `Last-Event-ID` shape); no retry (pagination surfaces a failed page's error). **Sendability — all N17c symbols below the `.v26` floor → no `@available` gate.** `URLSession.upload`/`download` (macOS 12/iOS 15/watchOS 8/tvOS 15/visionOS 1.0+), `URLError.downloadTaskResumeData` (macOS 10.15/iOS 13/watchOS 6/tvOS 13), `cancel(byProducingResumeData:)` (macOS 10.9/iOS 7/watchOS 2/tvOS 9), `URLComponents`/`URLQueryItem` (far below). `URLSession`/`URLSessionDownloadTask`/`Progress`/`URLComponents`/`URLQueryItem` are `NS_SWIFT_SENDABLE` (verified). `NebulaDownloadDelegate` is a `final class : NSObject, URLSessionDownloadDelegate, Sendable` — `Sendable` **derived** (all stored props are `let`s of Sendable type; `URLSessionDownloadDelegate` is `@objc` NOT `NS_SWIFT_SENDABLE`, but conformance does NOT block derived `Sendable` on a `final class` with all-`let` Sendable props — the N17b analogy; probe EXIT=0, Xcode 27 Beta 3 SDK; **no `@unchecked`**). `NebulaDownloadConfiguration` is per-call, `Sendable` NOT `Equatable` (the `@Sendable` closures); no `Mutex` accessor, `Nebula.md` config list unchanged. **Consumer cancellation** surfaces via the **loop** (it resumes the completion box with `CancellationError` from `Task.checkCancellation()`/the `URLError(.cancelled)` branch), NOT via `withCheckedThrowingContinuation` auto-cancellation (which does NOT throw on Task cancel without a `withTaskCancellationHandler` bridge); the consumer's `for try await` over `progress` ends normally on cancel (N17b semantics), `value()` throws `CancellationError`. **Open-struct errors, no new `NebulaError.Kind` case** — `NebulaMultipartError` (`build-failed`/`io-failed`/`cancelled`/`unknown`) + `NebulaDownloadError` (`download-failed`/`move-failed`/`resume-failed`/`cancelled`/`unknown`); both `coarseKind` switch to existing `.network`/`.unknown` with `default → .unknown`; domains `"Nebula.NebulaMultipartError"`/`"Nebula.NebulaDownloadError"`. **Testability stance (documented limitation)** — a `URLProtocol`-backed live round-trip over the async `URLSession.download(for:delegate:)` temp-file path **hangs** (`URLProtocol` does not cleanly bridge the async download overlay's temp-file + `didFinishDownloadingTo` dispatch — the overlay never completes); mirrors the N17a pragmatic stance (a non-injectable transport seam is documented, not forced) — pure logic + delegate-lifecycle synthesized + resume-data extraction seams + race-safe completion box cover the surface; live round-trip is compile-only. Ships as Nebula 0.16.0 (additive minor within Nebula 26 — new public types; no breaking change). **N17 split complete.** Tagging deferred to the owner's gate decision.

### Wave N17c — Bodies & downloads — DONE (tag pending)

- [x] `Sources/Nebula/Architecture/Network/Bodies/NebulaMultipartBuilder.swift` — pure `multipart/form-data` (RFC 2388) `Data` composer; `NebulaMultipartPart` (field/file) + `NebulaMultipartFormData`; boundary via 16 random bytes hex-encoded under `"----NebulaBoundary"`; `build()` pure (local `append(_ string:)` helper — `Data` has no `append(String)`); `file(in:)` temp-file writer (`.atomic`); `NebulaMultipartError` open-struct (no new `NebulaError.Kind`). `import Foundation` only.
- [x] `Sources/Nebula/Architecture/Network/Bodies/NebulaDownload.swift` — `NebulaDownloadConfiguration` (per-call, Sendable-not-Equatable; `.withDestination`/`.withResume`/`.withMaxResumeAttempts`/`.withResumeDelay`/`.withSleeper`/`.withLogger`) + `NebulaDownloadCompletion` (race-safe `Mutex<State>` pending/awaiting/resolved) + `NebulaDownloadHandle` (`progress`/`value()`/`cancelByProducingResumeData()`) + `NebulaDownloadDelegate` (`final class : NSObject, URLSessionDownloadDelegate, Sendable` derived — probe EXIT=0, NO `@unchecked`; per-task download callbacks; `didFinishDownloadingTo` moves the temp file via `FileManager.moveItem` overwrite-safe; `didWriteData` guards `expected > 0`; task captured in `Mutex<URLSessionDownloadTask?>` for cancel-with-resume) + `NebulaDownload.download(for:session:configuration:)`/`resume(from:session:configuration:)`/`pinned(by:sessionConfiguration:configuration:logger:)` → `PinnedDownloadSession` (reuses N17a `NebulaURLSessionDelegate` as session delegate — zero N17a change) + custom retry-with-resume loop (NOT `withPolicy`; `onTermination → loop.cancel()`). `import Foundation` + `import Synchronization` only.
- [x] `Sources/Nebula/Architecture/Network/Bodies/NebulaDownloadError.swift` — open-struct mirroring `NebulaSSEError`; `coarseKind` `.network`/`.unknown`; domain `"Nebula.NebulaDownloadError"`; factory statics.
- [x] `Sources/Nebula/Architecture/Async/NebulaPagedSequence.swift` — `public struct NebulaPagedSequence<Page: Sendable>: Sendable` (derived); `first`/`next` `@Sendable` closures; `stream() -> AsyncThrowingStream<Page, any Error>`; build-closure + Task + `onTermination → loop.cancel()`; custom loop (not `withPolicy`).
- [x] Tests — `ArchitectureMultipartTests.swift` (14 tests: builder pure + temp-file writer + error mapping) + `ArchitectureDownloadTests.swift` (18 tests: config + error mapping + delegate lifecycle synthesized + race-safe completion box + resume-data extraction seams; live `URLProtocol` round-trip dropped — documented limitation) + `ArchitecturePagedSequenceTests.swift` (7 tests: loop + stop + error + cancellation graceful + Sendable). Swift Testing; no `preconditionFailure`/`fatalError`. 908 Nebula tests / 178 suites green (+39 tests / +3 suites); zero warnings.
- [x] Docs — `ArchitectureBodiesDownloads.md` (new, mirrors `ArchitectureStreaming.md`); index link in `Architecture.md` (after `ArchitectureStreaming`); `NebulaPagedSequence` cross-reference in `ArchitectureAsync.md`.
- [x] Vault — `nebula-bodies-downloads.md` (shipped — documents the three surfaces + the ground-truth table (all below `.v26`, no gate) + the delegate routing decision + the gateway-compatibility of multipart + the custom loops + the per-call configs + the `URLProtocol`-hang testability limitation + the completion-box cancellation contract + invariants/deferred (N17 split complete)); `Home.md` N17c shipped link; hub `nebula-app-readiness-research.md` N17c row → ✅ shipped 0.16.0.
- [x] Governance — `DECISIONS.md` N17c ADR (Accepted); `ARCHITECTURE.md` prose bullet + structure tree + table row; this roadmap; `CHANGELOG.md` 0.16.0 entry. Adversarial code review (end of N17c): no CONFIRMED bugs.
- [ ] Tag `0.16.0` — **pending** (owner gate decision).

## Done (0.14.0 — SSL/TLS public-key pinning)

> App-readiness Wave N17a — the SSL/TLS public-key pinning toolkit (the first sub-wave of the N17 split; N17b streaming / N17c bodies+downloads follow). **Scope correction — pinning is transport-layer, NOT interceptor.** The research listed N17a as "interceptors + pinning scaffolding"; empirically the interceptor half already shipped in N10/0.7.0 (`NebulaHTTPInterceptor`/`NebulaHTTPInterceptorChain`/`NebulaAuthInterceptor`), and pinning is a transport-layer concern: a pin failure surfaces as a `URLError` from `URLSession` *before/under* the `data(for:)` call, so trust evaluation has to happen at the `URLSessionDelegate` layer — an `adapt`/`retry` interceptor only mutates the `URLRequest` / reacts to a thrown error, it cannot evaluate trust. So N17a **does not touch `NebulaHTTPGateway` at all** (Option A — the gateway already accepts an opaque `session: URLSession`; a caller injects a delegate-configured session today with zero gateway changes). No `NebulaGatewayConfiguration.pinning` field (pinning is per-session, not per-config); no process-wide `NebulaSSLPinningConfig` accessor; the `Nebula.md` config list is unchanged. **`import Security` in-bounds** (Keychain precedent; the research's open Q is resolved). **Security framework has NO `.swiftinterface`** (C/Obj-C via `module.modulemap`; headers are ground truth — WebFetch hallucinates availability). All pinning symbols below the `.v26` floor on every platform → **no `@available` gate in N17a**: `SecTrustEvaluateWithError` (mac 10.14/iOS 12/tvOS 12/watchOS 5), `SecTrustCopyCertificateChain` (mac 12/iOS 15/tvOS 15/watchOS 8 — preferred over deprecated `SecTrustGetCertificateAtIndex`), `SecCertificateCopyKey` (mac 10.14/iOS 12/tvOS 12/watchOS 5 — cross-platform, NOT deprecated platform-split `SecCertificateCopyPublicKey`), `SecKeyCopyExternalRepresentation` (mac 10.12/iOS 10/tvOS 10/watchOS 3). `URLSessionDelegate` is `NS_SWIFT_SENDABLE`; `urlSession(_:didReceive:completionHandler:)` is `@objc optional` with `@escaping @Sendable` completion, no async variant. **Sendability — `final class : NSObject, URLSessionDelegate, Sendable` DERIVED, no `@unchecked`:** probed against the Xcode 27 Beta 3 SDK with `swiftc -typecheck -swift-version 6 -strict-concurrency=complete -warnings-as-errors` → EXIT=0, zero warnings. `URLSessionDelegate` being `NS_SWIFT_SENDABLE` means conformance to a Sendable `@objc` protocol does not block derived `Sendable` on a `final class` whose only stored props are immutable `let`s of Sendable type. Matches `NebulaUNNotificationCenter` (N15a). `NSObject` is Foundation (not UIKit); required because `URLSessionDelegate` is an `@objc` protocol with `@objc optional` methods. **The key contrast with N15b**: there the non-`Sendable` `BGTask` arrived in a `@Sendable` closure and could not be stored in a `Mutex` (region-isolation wall) → required a `@unchecked Sendable` reference-type box. Here no non-Sendable type is stored — the policy is a Sendable value, the `SecTrust` is consumed and discarded inside the synchronous delegate call (never persisted). **Zero `@unchecked`** in all of N17a. **Testability — synthetic `SecTrust` + `disposition(for:policy:trust:)` seam:** the pure evaluator is unit-testable on the macOS host with a synthetic `SecTrust` built from a self-signed RSA-2048 cert (CN=test.example.com) embedded as a `[UInt8]` literal (694 bytes, no SPM `resources:`); the golden pin (`7badc2c8…3b2a`, SHA-256 of the public-key DER) is hardcoded. The delegate method is NOT round-trip-testable (`URLProtectionSpace.serverTrust` not injectable via the public init) → the disposition mapping is extracted as `internal disposition(for:policy:trust:)` and unit-tested directly. The live `URLSession`+delegate+TLS round-trip is a documented compile-only limitation (mirrors `NebulaUNNotificationCenter`). **SPKI algorithm — OWASP "any position", additive to system trust:** match any cert in the chain (leaf or intermediate/CA — survives leaf rotation); `validateChainFirst` defaults `true` (run `SecTrustEvaluateWithError` first; pinning never replaces the OS trust store). SHA-256 via `Data.nebulaDigest(of: .sha256)` → `NebulaHashAlgorithm`/sha256 → `CryptoKit.SHA256` — **no new `import CryptoKit`** (only `NebulaHashAlgorithm.swift` imports it). Host lookup is `internal resolvedPins(for:policy:)` — exact match, then parent-domain walk when `includeSubdomains`, stopping before the single-label public suffix; case-insensitive (RFC 1035). `NebulaHTTPSession.pinned(by:configuration:logger:)` returns a `NebulaPinnedSession` carrying BOTH the `URLSession` and the delegate (`URLSession` does NOT retain its delegate — footgun documented). `NebulaSSLPinningError` is an open-struct error mirroring `NebulaHTTPServerError` (no new `NebulaError.Kind` case); the delegate does NOT throw (the `@objc optional` method has no `throws`) — on failure it logs + `.cancelAuthenticationChallenge`; `URLSession` surfaces a `URLError` to `NebulaHTTPGateway`, which already bridges `URLError → NebulaError(urlError:)` (no gateway change). Ships as Nebula 0.14.0 (additive minor within Nebula 26). Tagging deferred to the owner's gate decision.

### Wave N17a — SSL/TLS public-key pinning — DONE (tag pending)
- [x] `Sources/Nebula/Architecture/Network/Pinning/NebulaSSLPinning.swift` — `NebulaSSLPinningPin` (32-byte SHA-256 SPKI digest; `init?(digest:)`/`init?(hexDigest:)` via `Data(nebulaHexEncoded:)`, re-validates 32 bytes; `hexDigest`) + `NebulaSSLPinning.HostPins` (host + `Set<pin>`) + `NebulaSSLPinning` (`Sendable` policy: `hostPins`/`includeSubdomains`/`validateChainFirst: true`/`failClosedForUnknownHosts: true`, fluent `.with*`, `static pins(for:_:)`). Sendable derived.
- [x] `Sources/Nebula/Architecture/Network/Pinning/NebulaSSLPinningEvaluator.swift` — pure `static evaluate(trust:host:policy:)` (OWASP any-position SPKI, additive to system trust; `validateChainFirst` → `SecTrustEvaluateWithError` first; chain via `SecTrustCopyCertificateChain`; per-cert `SecCertificateCopyKey` → `SecKeyCopyExternalRepresentation` → `Data.nebulaDigest(of: .sha256)`; `extractedAny` tracking → `.spkiExtractionFailed` on total extraction failure, else `.noMatchingPin`) + `NebulaSSLPinningResult` + `internal resolvedPins(for:policy:)` (case-insensitive exact + subdomain walk, stops before the single-label public suffix). `import Security` + Foundation.
- [x] `Sources/Nebula/Architecture/Network/Pinning/NebulaSSLPinningError.swift` — open-struct (`NebulaFailure, Equatable, Hashable`), nested `Kind` presets (`.noMatchingPin`/`.noPinForHost`/`.chainValidationFailed`/`.spkiExtractionFailed`/`.cancelled`/`.unknown`), `coarseKind` (`.network`/`.unknown`), `toNebulaError(kind:)` (domain `"Nebula.NebulaSSLPinningError"`, no new `NebulaError.Kind`); factory statics carry `underlying: NebulaError.Box?`. Mirrors `NebulaHTTPServerError`. Foundation-only.
- [x] `Sources/Nebula/Architecture/Network/Pinning/NebulaURLSessionDelegate.swift` — `final class : NSObject, URLSessionDelegate, Sendable` (derived `Sendable`, NO `@unchecked`, `NebulaUNNotificationCenter` precedent); `let pinning` + `let logger`; `@objc optional urlSession(_:didReceive:completionHandler:)` thin guard→evaluate→log→map→completion; `internal disposition(for:policy:trust:)` testable seam (`.matched → .useCredential`+credential; `.noPinForHost` fail-closed/open; failures `.cancelAuthenticationChallenge`). `import Security` + Foundation.
- [x] `Sources/Nebula/Architecture/Network/Pinning/NebulaHTTPSession.swift` — `NebulaPinnedSession: Sendable` (session + delegate) + `NebulaHTTPSession` `enum` namespace + `pinned(by:configuration:logger:) -> NebulaPinnedSession` (returns BOTH; `URLSession` does NOT retain its delegate — footgun documented). Foundation-only.
- [x] `Tests/NebulaTests/ArchitectureSSLPinningTests.swift` — A. value semantics (pin digest/hex validation, `HostPins`/policy `Equatable`/`Hashable`, fluent `.with*` distinct copies, Sendable across `Task`); B. host lookup + subdomain walk (`resolvedPins` exact/walk/no-walk-when-disabled/public-suffix-no-match/case-insensitive); C. synthetic-`SecTrust` SPKI extraction (golden match with `validateChainFirst: false`, `.noMatchingPin` wrong pin, `.noPinForHost` empty, `.chainValidationFailed` self-signed with default `validateChainFirst: true`, subdomain match; documented untested `extractedAny == false` + chain-copy branches — unreachable with well-formed certs); D. `disposition(for:policy:trust:)` (matched/noPinForHost fail-closed/fail-open/failures cancel); session builder; error (factory statics/coarseKind/toNebulaError domain+metadata/Sendable).
- [x] DocC — `ArchitectureSSLPinning.md` (Overview/Policy/Evaluator/Delegate/Session builder/Errors/Security note/Testability note/Topics); `Architecture.md` index `- <doc:ArchitectureSSLPinning>` after `- <doc:ArchitectureAuth>`; `Nebula.md` config list unchanged (pinning is per-session, not a config).
- [x] Vault — `nebula-ssl-pinning.md` (shipped — documents the transport-not-interceptor scope correction + the Security-header ground truth + the derived-`Sendable` `NSObject` delegate verdict + the synthetic-`SecTrust` testability + the `URLProtectionSpace.serverTrust`-not-injectable limitation + the `NebulaPinnedSession` ownership + the `Data.nebulaDigest` reuse + the two code-review refinements); `nebula-network-hardening.md` (shipped banner → N17a/0.14.0); `Home.md` N17a shipped link; hub `nebula-app-readiness-research.md` N17 row split (N17a ✅ shipped 0.14.0 + N17b/N17c pending) + SSL-pinning row + wave line.
- [x] Governance — `DECISIONS.md` N17a ADR (Accepted); `ARCHITECTURE.md` prose bullet + structure tree; this roadmap; `CHANGELOG.md` 0.14.0 entry. 836 Nebula tests / 171 suites green; zero warnings; release clean; all 5 platforms `xcodebuild` BUILD SUCCEEDED (clean `.build`); DocC `BUILD DOCUMENTATION SUCCEEDED` (zero new warnings). Adversarial code review (end of N17a): no CONFIRMED bugs; two low-severity plausible findings incorporated (`.spkiExtractionFailed` now fires on total extraction failure; host matching case-insensitive).
- [ ] Tag `0.14.0` — **pending** (owner gate decision).

## Done (0.13.0 — Background tasks)

> App-readiness Wave N15b — the background-task toolkit (the higher-risk half of the N15 split; N15a = notifications precedes). `BGTaskScheduler` is `API_UNAVAILABLE(macos) API_UNAVAILABLE(watchos)` — background tasks are an iOS/tvOS/visionOS surface. `NebulaBackgroundTaskScheduler` (a `Sendable` port) carries the five scheduling requirements (`register(_:)` / `submit(_:)` / `cancel(_:)` / `cancelAll()` / `pendingRequests()`); the launch-handler surface is **not** on the port — it lives on the config. `NebulaBGTaskScheduler` (a `final class : NebulaBackgroundTaskScheduler, Sendable`, **no `NSObject`** — there is no `@objc` protocol to back) is the façade over `BGTaskScheduler.shared`; the singleton is non-`Sendable` AND shared → fetched **locally per method, never stored** (the singleton cannot be `sending`-consumed); `register(forTaskWithIdentifier:using:launchHandler:)` takes `using: nil` explicit. `NebulaBackgroundTask` is the Sendable launch-time handle (identifier + kind + two `@Sendable` closures that capture the Sendable façade + identifier, **never** the `BGTask`, and reach it through the façade's `Mutex`). `NebulaBackgroundTaskConfiguration` is the 7th config (`Sendable`, NOT `Equatable`); `NebulaBackgroundTaskConfig` is the `Mutex` accessor. `NebulaBackgroundTaskError` is an open-struct (`NebulaFailure`, the `NebulaKeychainError` precedent) — `coarseKind` (scheduling → `.cocoa`, rest → `.unknown`) + `toNebulaError(kind:)` — **no new `NebulaError.Kind` case**; factory statics carry `underlying: NebulaError.Box?` to preserve the SDK `NSError`. **NEW GATING PRECEDENT — `@available(<platform>, unavailable)` declaration gate (form c):** `BackgroundTasks.framework` is physically present on all 5 SDKs, so `#if canImport(BackgroundTasks)` is `true` on macOS/watchOS (gates nothing) and the `BGTaskScheduler.shared` reference then fails; the compile-safe gate is the type-level `@available(macOS, unavailable) @available(watchOS, unavailable)` declaration on the Nebula façade (the `NebulaLogStoreExporter` precedent — declaring a Nebula symbol unavailable suppresses body type-checking on mac/watch); `@available(macOS 26, unavailable)` is a syntax error. The 4-form gating taxonomy is consolidated: (1) declaration gate [N15b]; (2) `#if !os(<platform>)` compile gate [N15a]; (3) `#if canImport(<framework>)` absent-framework gate [reserved]; (4) `#if swift(>=6.4)` + `if #available` above-floor gate [N15b `submit`]. **SENDABILITY — `@unchecked Sendable` reference-type box:** storing the non-`Sendable` `BGTask` (system-delivered in the `@Sendable` launch callback) directly in a `Mutex<[String: BGTask]>` fails whole-module Swift 6 with a region-isolation wall (`'inout sending' parameter '$0' cannot be task-isolated`); the `NebulaDefaults` `Mutex<non-Sendable>` precedent does not apply (`UserDefaults.standard` arrives at a public `sending` API the caller asserts; `BGTask` arrives in a launch closure). Resolved by a `final class @unchecked Sendable` `NebulaBGTaskBox` holding the `BGTask` as an immutable `let` (the `NebulaMemoryLogHandler` precedent — the binding forbids `@unchecked` on value types, not reference types). The box crosses isolation; the façade holds `Mutex<[String: NebulaBGTaskBox]>` (Sendable boxes → Sendable `Mutex`); the façade's `Sendable` is **derived** — `@unchecked` isolated to the single box. **`submit` dual-path, warning-clean under `.v26`:** iOS-27 async `submitTaskRequest(_:)` (`#if swift(>=6.4)` + `if #available(iOS 27, tvOS 27, visionOS 27, *)`) + iOS-26 deprecated sync `submit(_:)` fallback (deprecation fires only at target ≥ 27). **Testing note (larger than N15a):** every `BackgroundTasks` SDK type is `API_UNAVAILABLE(macos)` → `swift test` (macOS host) can't compile them; the façade + mapping round-trip are gated `#if !os(macOS) && !os(watchOS)` (compile on iOS/tvOS/visionOS via `xcodebuild`, dead on the macOS host); `BGTaskScheduler.shared` is non-functional in a headless test bundle → the façade is never instantiated in tests; the port seam (`FakeBackgroundTaskScheduler`) + type-level conformance + mapping round-trip prove the architecture. Ships as Nebula 0.13.0 (additive minor within Nebula 26). Tagging deferred to the owner's gate decision.

### Wave N15b — Background tasks — DONE (tag pending)
- [x] `Sources/Nebula/Architecture/BackgroundTasks/NebulaBackgroundTaskRequest.swift` — `NebulaBackgroundTaskKind` (`.appRefresh`/`.processing`, all-5) + `NebulaBackgroundTaskRequest` value type, `Sendable` derived.
- [x] `Sources/Nebula/Architecture/BackgroundTasks/NebulaBackgroundTask.swift` — Sendable launch-time handle (identifier + kind + `@Sendable` `finish`/`setExpiration` closures capturing the Sendable façade + id, NOT the `BGTask`; inner `@escaping` explicit on the stored `@Sendable` closure param).
- [x] `Sources/Nebula/Architecture/BackgroundTasks/NebulaBackgroundTaskScheduler.swift` — `Sendable` port (5 scheduling requirements; launch handler on the config, not the port).
- [x] `Sources/Nebula/Architecture/BackgroundTasks/NebulaBackgroundTaskConfiguration.swift` — 7th config (`Sendable`, NOT `Equatable`; `@Sendable` `launch` handler; `.withLaunch` returns the concrete type; `static let default`).
- [x] `Sources/Nebula/Architecture/BackgroundTasks/NebulaBackgroundTaskConfig.swift` — caseless `enum` + `Mutex<NebulaBackgroundTaskConfiguration>(.default)` + `get()`/`set(_:)`.
- [x] `Sources/Nebula/Architecture/BackgroundTasks/NebulaBackgroundTaskError.swift` — open-struct (`NebulaFailure, Equatable, Hashable`), nested `Kind` presets (mirror `BGTaskSchedulerErrorCode`), `coarseKind`/`toNebulaError(kind:)` (no new `NebulaError.Kind`); factory statics carry `underlying: NebulaError.Box?`.
- [x] `Sources/Nebula/Architecture/BackgroundTasks/NebulaBGTaskScheduler.swift` — `final class : NebulaBackgroundTaskScheduler, Sendable` façade (no `NSObject`), macOS/watchOS-unavailable (`@available(macOS, unavailable) @available(watchOS, unavailable)`); `Mutex<[String: NebulaBGTaskBox]>` live-task map; `submit` dual-path (`#if swift(>=6.4)` async + iOS-26 deprecated fallback); `register` `using: nil` explicit; `pendingRequests` maps to Sendable inside the SDK completion; `internal` mapping helpers (`makeBGRequest`/`makeNebulaRequest`/`mapSubmitError`); `NebulaBGTaskBox` `final class @unchecked Sendable` reference-type box (the `NebulaMemoryLogHandler` precedent).
- [x] `Tests/NebulaTests/ArchitectureBackgroundTaskTests.swift` — value types (Kind/Request), Sendable handle (complete/onExpiration invoke closures; equality by id+kind; sends across Task), config (default no-op/`withLaunch`/Sendable-not-Equatable/sends across Task), `NebulaBackgroundTaskConfig` accessor (`.serialized`), `FakeBackgroundTaskScheduler` port seam, `NebulaBackgroundTaskError` (factory statics/coarseKind/toNebulaError/underlying/equality/string-literal/sends across Task); `#if !os(macOS) && !os(watchOS)` façade conformance + `makeBGRequest`/`makeNebulaRequest` round-trip (appRefresh + processing conditions) — compile on iOS/tvOS/visionOS, dead on the macOS host.
- [x] DocC — `ArchitectureBackgroundTasks.md`; `Architecture.md` index (after `ArchitecturePermissions`); `Nebula.md` configuration contracts (Six → Seven + `NebulaBackgroundTaskConfiguration`); the stale `#if canImport(BackgroundTasks)` reference in `ArchitectureNotifications.md` corrected.
- [x] Vault — `nebula-background-tasks.md` (shipped — documents the declaration-gate precedent + the 4-form taxonomy + the `@unchecked` reference-type-box sendability correction + the dual-path submit + the headless-test constraint); `nebula-background-notifications.md` (status split — background-tasks shipped 0.13.0, gating-form correction); `Home.md` N15b link; hub `nebula-app-readiness-research.md` N15b row (✅ shipped 0.13.0) + wave line (N15 split complete).
- [x] Governance — `DECISIONS.md` N15b ADR (Accepted); `ARCHITECTURE.md` prose bullet + structure tree; this roadmap; `CHANGELOG.md` 0.13.0 entry. 811 Nebula tests / 170 suites green; zero warnings; release clean; all 5 platforms `xcodebuild` BUILD SUCCEEDED (clean `.build`); DocC `BUILD DOCUMENTATION SUCCEEDED` (zero new warnings).
- [ ] Tag `0.13.0` — **pending** (owner gate decision).

### Deferred (tracked below in "Later")
- `BGContinuedProcessingTask` (iOS-26-only — request subclass + `SubmissionStrategy` + `Resources` + `supportedResources` + `NSProgressReporting` + title/subtitle; single-platform subset gate) → N15c; `registerForRemoteNotifications` (APNs token registration — `UIApplication`/`NSApplication` app-lifecycle); the unified `NebulaPermissions` request port (AV/CL/PH/ATT adapters — app-level glue).

## Done (0.12.0 — Notifications + permission status)

> App-readiness Wave N15a — the notifications + permission-status toolkit (the lower-risk half of the N15 split; N15b = background tasks follows). `UserNotifications` is available on all 5 platforms at the `.v26` floor, so the surface ships all-5 with no framework-level gate. `NebulaNotificationCenter` (a `Sendable` port) carries the five scheduling/authorization requirements (`requestAuthorization(options:)` / `add(_:)` / `cancel(_:)` / `cancelAll()` / `pendingRequests()`); the delegate-handler surface is **not** on the port — it lives on the config. `NebulaUNNotificationCenter` (a `final class : NSObject, NebulaNotificationCenter, UNUserNotificationCenterDelegate, Sendable`) is the delegate-adapter façade — `UNUserNotificationCenterDelegate` is an `@objc` protocol backed by a `weak` Obj-C reference, so the façade subclasses `NSObject` (the `NebulaHTTPServer` precedent gains an `NSObject` base); the delegate methods are sync Obj-C callbacks → a `final class` + a `Mutex<NebulaNotificationsConfiguration>` (not an actor); `Sendable` **derived**, no `@unchecked`. `UNUserNotificationCenter.current()` is non-Sendable AND a shared singleton → fetched **locally per method, never stored** (the singleton cannot be `sending`-consumed); `delegate` is `weak` → auto-nils → no `deinit`. The config handlers are **synchronous-returning** (`willPresent` returns options, `didReceive` returns `Void`) to avoid capturing the non-`@Sendable` Obj-C completion inside a `@Sendable` closure. `NebulaNotificationsConfiguration` is the 6th config (`Sendable`, NOT `Equatable`); `NebulaNotificationsConfig` is the `Mutex` accessor. `NebulaNotificationsError` is an open-struct (`NebulaFailure`, the `NebulaKeychainError` precedent) — `coarseKind` (scheduling → `.cocoa`, rest → `.unknown`) + `toNebulaError(kind:)` — **no new `NebulaError.Kind` case**. `NebulaPermissionStatus` (an 8-case `Sendable`/`CaseIterable` union-superset enum) bridges `UNAuthorizationStatus` (`.ephemeral` is `#if os(iOS)` — App Clips); the unified `NebulaPermissions` request port is deferred (app-level glue). **NEW GATING PRECEDENT — `#if !os(<platform>)` for SDK symbols `API_UNAVAILABLE`:** the `didReceive` callback, `UNNotificationResponse`, and every user-facing `UNNotificationContent` property are `API_UNAVAILABLE(tvos)`; empirically verified via `swiftc -typecheck` across all 5 SDKs that `@available(tvOS, unavailable)` on the override fails ("cannot override … which has been marked unavailable") and `if #available(..., *)` fails ("'title' is unavailable in tvOS" — the `*` wildcard does NOT exclude tvOS for an `unavailable` symbol). The ONLY compile-safe mechanism is `#if !os(tvOS)` — distinct from the `@available(<platform>, unavailable)` declaration gate and the `#if canImport(<framework>)` whole-file gate (N15b). **Testing note:** `UNUserNotificationCenter.current()` traps in a headless test bundle (signal 6, no app context) → the façade is never instantiated in tests; the mapping helpers are `internal` for a round-trip suite with constructible SDK types. Ships as Nebula 0.12.0 (additive minor within Nebula 26). Tagging deferred to the owner's gate decision.

### Wave N15a — Notifications + PermissionStatus — DONE (tag pending)
- [x] `Sources/Nebula/Architecture/Notifications/NebulaNotificationCenter.swift` — `Sendable` port (5 scheduling/authorization requirements; delegate handlers live on the config, not the port).
- [x] `Sources/Nebula/Architecture/Notifications/NebulaNotificationRequest.swift` — value types (`NebulaNotificationContent`/`Trigger`[`.timeInterval`/`.calendar`]/`Request`/`Response`/`PresentationOptions`/`AuthorizationOptions`), all `Sendable` derived, all-5; OptionSet raw values mirror `UN*` (verified against headers).
- [x] `Sources/Nebula/Architecture/Notifications/NebulaNotificationsConfiguration.swift` — 6th config (`Sendable`, NOT `Equatable`; synchronous-returning `@Sendable` `willPresent`/`didReceive` handlers; `.with*` return the concrete type; `static let default`).
- [x] `Sources/Nebula/Architecture/Notifications/NebulaNotificationsConfig.swift` — caseless `enum` + `Mutex<NebulaNotificationsConfiguration>(.default)` + `get()`/`set(_:)`.
- [x] `Sources/Nebula/Architecture/Notifications/NebulaNotificationsError.swift` — open-struct (`NebulaFailure, Equatable, Hashable`), nested `Kind` presets, `coarseKind`/`toNebulaError(kind:)` (no new `NebulaError.Kind`).
- [x] `Sources/Nebula/Architecture/Notifications/NebulaUNNotificationCenter.swift` — `final class : NSObject, NebulaNotificationCenter, UNUserNotificationCenterDelegate, Sendable` delegate-adapter façade; `Mutex<Config>`; `.current()` local not stored; `#if !os(tvOS)` on `didReceive` override + content mapping (the gating precedent); `internal` mapping helpers for testability.
- [x] `Sources/Nebula/Architecture/Permissions/NebulaPermissionStatus.swift` — 8-case `Sendable`/`Equatable`/`Hashable`/`CaseIterable`/`CustomStringConvertible` union-superset enum + `init?(UNAuthorizationStatus:)` (`.ephemeral` `#if os(iOS)`).
- [x] `Tests/NebulaTests/ArchitecturePermissionStatusTests.swift` (7) + `Tests/NebulaTests/ArchitectureNotificationsTests.swift` (29 tests / 12 suites) — value types, OptionSets, config (handlers/`with*`/Sendable-not-Equatable/`Mutex` accessor `.serialized`), `FakeNotificationCenter` port seam, `NebulaNotificationsError`, `NebulaUNNotificationCenter SDK mapping` round-trip (no `.current()`), type-level `facadeIsAPortConformer`.
- [x] DocC — `ArchitectureNotifications.md` + `ArchitecturePermissions.md`; `Architecture.md` index (after `ArchitectureFeatureFlags`); `Nebula.md` configuration contracts (Five → Six + `NebulaNotificationsConfiguration`).
- [x] Vault — `nebula-notifications.md` (shipped — documents the `#if !os(tvOS)` precedent, the `.current()`-local/Sendable-façade/synchronous-handler/headless-trap decisions); `nebula-background-notifications.md` (status split — notifications shipped, background-tasks → N15b); `Home.md` N15a link; hub `nebula-app-readiness-research.md` N15 row split (N15a ✅ 0.12.0 + N15b pending) + wave line.
- [x] Governance — `DECISIONS.md` N15a ADR (Accepted); `ARCHITECTURE.md` prose bullet + structure tree; this roadmap; `CHANGELOG.md` 0.12.0 entry. 788 Nebula tests / 163 suites green; zero warnings; release clean; all 5 platforms `xcodebuild` BUILD SUCCEEDED (clean `.build`); DocC `BUILD DOCUMENTATION SUCCEEDED` (zero new warnings).
- [ ] Tag `0.12.0` — **pending** (owner gate decision).

### Deferred (tracked below in "Later")
- `.location` notification trigger (needs `import CoreLocation` + a 3-platform gate); `setBadgeCount` (watchOS unavailable); `setNotificationCategories`/`getDelivered*`/`removeDelivered*` (tvOS unavailable); `openSettingsFor` delegate (watchOS + tvOS unavailable); the unified `NebulaPermissions` request port (AV/CL/PH/ATT adapters — app-level glue) + the AV/CL/PH/ATT status bridges; `registerForRemoteNotifications` (APNs token registration — `UIApplication`/`NSApplication` app-lifecycle).
- **N15b — `NebulaBackgroundTask`** (background-task scheduling — `#if canImport(BackgroundTasks)` whole-file gate + iOS-27 async `submitTaskRequest(_:)` + open-struct error, 0.13.0).
- The rest of the app-readiness sequence (N16 MetricKit → N17 network hardening → N18 StoreKit + A1–A3 Aurora migration + N11b runnable composition example) per the hub `nebula-app-readiness-research.md`.

## Done (0.11.0 — Feature flags)

> App-readiness Wave N14 — the feature-flag toolkit. There is no Apple-native remote-config or feature-flag API (zero `FeatureFlag`/`RemoteConfig`/`RolloutConfig` hits in `Foundation.swiftmodule`, Xcode 27 Beta 3), and `dependencies: []` forbids Firebase/LaunchDarkly — so a remote flag is necessarily a **port** the app conforms. Nebula ships the Foundation-tier pattern, not a backend. `NebulaFlagValue` (a `Sendable`/`Equatable`/`Hashable` value enum: `.bool`/`.string`/`.int`/`.double`/`.json(Data)`, `CustomStringConvertible` derived; no `Codable` yet) is the storage representation. `NebulaFeatureFlags` (a `Sendable` port) has ONE requirement `value(forKey:) -> NebulaFlagValue?` plus a default extension of typed accessors (`bool`/`string`/`int`/`double`/`number`/`json`) every conformer inherits — the seam model mirrors `NebulaPreferences`; `int`/`double` return only their own case (no silent coercion), `number` coerces either to `Double`, `json` decodes a `.json(_:)` blob via per-call `JSONDecoder()` (decoupled from `NebulaJSONDecoder`), and only `json` throws (flag errors surface as `DecodingError` — **no new `NebulaError.Kind` case**). `NebulaLocalFeatureFlags` (a `final class` wrapping `Mutex<[String: NebulaFlagValue]>`) is the in-memory façade — `Sendable` derived, no `@unchecked`; the `final class` absorbs the `~Copyable` `Mutex`; the store is a `Sendable` value type, so the init takes it by value (**no `sending`**, unlike `NebulaDefaults`'s `UserDefaults`); `setValue(nil)` removes. `NebulaRemoteFeatureFlags` refines the port with `refresh() async throws` (the conformer serves the last-fetched cache; the backend is app-supplied; deviates from the research's non-throwing `refresh() async` — honest about fetch failure). `NebulaCompositeFeatureFlags` (a `Sendable` struct + `[any NebulaFeatureFlags]` + `withSource(_:)`, first-non-nil resolution — mirrors `NebulaHTTPInterceptorChain`) is a generic resolver; the app wires `[localOverrides, remote, builtInDefaults]` — the composite does not hardcode local/remote/defaults. It conforms to `NebulaFeatureFlags`, so the typed accessors flow through. **No process-wide accessor** — all `final class` store façades are constructed-and-passed at the composition root (the `*Config` `Mutex` family is reserved for configuration values). **Deferred:** `NebulaDefaults`-backed persistent overrides (research marks optional — follow-up; needs `NebulaFlagValue: Codable`); SwiftUI `@Environment` injection / an `@Observable` flag manager (Cosmos-only); the remote backend itself (app-supplied); rollout-% / audience targeting (backend-computed — Nebula evaluates locally on a fetched value). Ships as Nebula 0.11.0 (additive minor within Nebula 26). Tagging deferred to the owner's gate decision.

### Wave N14 — Feature flags — DONE (tag pending)
- [x] `Sources/Nebula/Architecture/FeatureFlags/NebulaFlagValue.swift` — `Sendable`/`Equatable`/`Hashable`/`CustomStringConvertible` value enum (`.bool`/`.string`/`.int`/`.double`/`.json(Data)`, derived); `.int`/`.double` distinct (no `Codable` yet).
- [x] `Sources/Nebula/Architecture/FeatureFlags/NebulaFeatureFlags.swift` — `Sendable` port, one `value(forKey:) -> NebulaFlagValue?` requirement + default extension `bool`/`string`/`int`/`double`/`number`/`json` (`json` throws, per-call `JSONDecoder()`; `int`/`double` no coercion, `number` coerces either).
- [x] `Sources/Nebula/Architecture/FeatureFlags/NebulaLocalFeatureFlags.swift` — `final class` + `Mutex<[String: NebulaFlagValue]>`; `init(_ flags:)` (no `sending` — Dict is Sendable); `value`/`setValue` (nil removes)/`removeValue`/`removeAll`; derived `Sendable`, no `@unchecked`.
- [x] `Sources/Nebula/Architecture/FeatureFlags/NebulaRemoteFeatureFlags.swift` — `: NebulaFeatureFlags` + `refresh() async throws` (app-supplied backend).
- [x] `Sources/Nebula/Architecture/FeatureFlags/NebulaCompositeFeatureFlags.swift` — `Sendable` struct + `sources: [any NebulaFeatureFlags]` + `withSource(_:)` + first-non-nil `value(forKey:)`; conforms to the port (typed accessors flow through).
- [x] `Tests/NebulaTests/ArchitectureFeatureFlagsTests.swift` — 31 tests / 6 suites (FlagValue, façade, façade concurrency incl. 50-task, port seam on a custom conformer, remote port, composite incl. `[local, remote, defaults]` local-overrides-win).
- [x] `Nebula.docc/ArchitectureFeatureFlags.md` (mirror of `ArchitecturePreferences.md`); `Architecture.md` — `- <doc:ArchitectureFeatureFlags>` after `- <doc:ArchitectureKeychain>` (NOT in `Nebula.md` `### Configuration contracts`).
- [x] Vault — `nebula-feature-flags.md` (shipped); `nebula-usererror-environment-featureflags.md` feature-flags dimension → shipped; `Home.md` N14 shipped link; hub `nebula-app-readiness-research.md` N14 row + wave line → shipped.
- [x] Governance — `DECISIONS.md` N14 ADR (Accepted); `ARCHITECTURE.md` prose bullet + structure tree; this roadmap; `CHANGELOG.md` 0.11.0 entry. 752 Nebula tests / 151 suites + 12 Aurora / 3 suites green; zero warnings; DocC clean.
- [ ] Tag `0.11.0` — **pending** (owner gate decision).

### Deferred (tracked below in "Later")
- `NebulaDefaults`-backed persistent overrides (needs `NebulaFlagValue: Codable`); SwiftUI `@Environment` injection / `@Observable` flag manager (Cosmos-only); the remote backend itself (app-supplied); rollout-% / audience targeting (backend-computed).

## Done (0.10.0 — Environment value + reader)

> App-readiness Wave N13 — the environment value + reader pattern. Apple provides no Foundation `Environment` value type — the idiom is the Xcode `Configuration` build setting fed from `.xcconfig` + schemes and written into the app's `Info.plist` (a key, conventionally `Configuration`, set from `$(CONFIGURATION)`). Nebula ships the **value + reader**, not the wiring. `NebulaEnvironment` (a closed `String`-backed enum: `.development`/`.staging`/`.production`; `default = .production` — safe-fail-to-production) round-trips that string. `fromBundle(_:key:)` reads `object(forInfoDictionaryKey:)` (default key `"Configuration"`, default `bundle .main`), casts `Any? → String?` **before** crossing isolation (the `infoDictionary` values are `Any` and not Sendable), and resolves to `.default` on an absent/unknown/non-`String` value (never `nil`); it is a pure function over the Sendable `Bundle` — no `Mutex`. `NebulaEnvironmentConfiguration` (the fifth cross-cutting config contract) carries the resolved environment + per-environment `baseURLs` + string `overrides`; `baseURL(for:)` returns `nil` for an unregistered environment (Nebula ships no built-in URLs). `NebulaEnvironmentConfig` is the `Mutex` accessor alongside explicit-parameter DI (the two-path rule). `NebulaEnvironmentConfiguration` is `Sendable`-not-`Equatable` (family posture). **Deferred (app-tier):** `.xcconfig`/scheme/`Info.plist` wiring; a `ProcessInfo`-based reader (documented alternative — the `Info.plist`-keyed reader is the only one shipped); a reader façade (a value builder suffices). Naming `Configuration`/`Config` split mirrors the four-family convention (avoids the `NebulaEnvironmentConfigConfig` stutter). Ships as Nebula 0.10.0 (additive minor within Nebula 26). Tagging deferred to the owner's gate decision.

### Wave N13 — Environment value + reader — DONE (tag pending)
- [x] `Sources/Nebula/Environment/NebulaEnvironment.swift` — closed `String`-backed enum (`.development`/`.staging`/`.production`; `Sendable`/`Equatable`/`Hashable`/`CaseIterable`/`CustomStringConvertible`); `static let default = .production`; `fromBundle(_:key:)` reader (pure function, `Any? → String?` cast before crossing isolation, safe-fail-to-production, never `nil`).
- [x] `Sources/Nebula/Environment/NebulaEnvironmentConfiguration.swift` — 5th config struct (`environment`/`baseURLs: [NebulaEnvironment: URL]`/`overrides: [String: String]`); `.withEnvironment`/`.withBaseURLs`/`.withOverrides` returning the concrete type; `baseURL(for:)` (nil if unregistered)/`value(for:)`; `static let default`; `Sendable`-not-`Equatable`.
- [x] `Sources/Nebula/Environment/NebulaEnvironmentConfig.swift` — caseless enum + `Mutex<NebulaEnvironmentConfiguration>(.default)` + `get()`/`set(_:)`; `let current` never `var`; no convenience methods.
- [x] `Tests/NebulaTests/EnvironmentTests.swift` — 23 tests / 4 suites (enum, reader, config value, accessor `@Suite(.serialized)`).
- [x] `Nebula.docc/Environment.md` — top-level article (sibling to `Standardize.md`/`Measure.md`, the `ArchitecturePreferences.md` prose model); `Nebula.md` — `- <doc:Environment>` after `- <doc:Standardize>` + `NebulaEnvironmentConfiguration` added to `### Configuration contracts` (five structs).
- [x] Vault — `nebula-environment.md` (shipped); `nebula-usererror-environment-featureflags.md` environment dimension → shipped; `Home.md` N13 shipped link; hub `nebula-app-readiness-research.md` N13 row + wave line → shipped.
- [x] Governance — `DECISIONS.md` N13 ADR (Accepted); `ARCHITECTURE.md` prose bullet + structure tree; this roadmap; `CHANGELOG.md` 0.10.0 entry. 721 Nebula tests / 145 suites + 12 Aurora / 3 suites green; zero warnings; DocC clean.
- [ ] Tag `0.10.0` — **pending** (owner gate decision).

### Deferred (tracked below in "Later")
- `.xcconfig`/scheme/`Info.plist` wiring (app-tier); a `ProcessInfo`-based reader (documented alternative, not shipped); a reader façade/interpolator (a value builder suffices).

## Done (0.9.0 — User-error bridge)

> App-readiness Wave N12 — the user-error bridge. A Foundation-tier value `NebulaUserError` (a `Sendable`/`Equatable`/`Hashable` struct: `message` + `recoveryActions: [RecoveryAction]` + `helpAnchor`) that the presentation layer (Cosmos / the app) renders, produced by a configuration-layer map from the closed `NebulaError` envelope — `NebulaError → NebulaUserError?`. **Mapping only — no new `NebulaError.Kind` cases.** `NebulaUserError` is the *output* of the map — the **opposite direction** from `NebulaFailure` (which bridges a layer error *into* the envelope), so it is a plain value, **not** a `NebulaFailure` (no `coarseKind`/`toNebulaError`). `RecoveryAction` (`.retry`/`.cancel`/`.dismiss`/`.custom(String)`) is Nebula-authored because Apple's `RecoverableError` is closure-based, not value-based. `NebulaErrorConfiguration.withUserMessageMap(_:)` installs a `@Sendable` map keyed by `(NebulaError.Kind, [String: String])` (the dictionary is `NebulaError.metadata`) returning an optional `NebulaUserError`; default `{ _, _ in nil }` (opt-in). `NebulaUserError.default(for:context:)` ships an English fallback per `Kind` with HIG-neutral tone (overridable; L10n via `String(localized:)` at the app layer). `userError(for:)` (config + `NebulaErrorConfig` accessor) is **not** gated on `isEnabled` (orthogonal to reporting). `NebulaErrorConfiguration` stays `Sendable`-not-`Equatable`. Ships as Nebula 0.9.0 (additive minor within Nebula 26). Tagging deferred to the owner's gate decision.

### Wave N12 — User-error bridge — DONE (tag pending)
- [x] `Sources/Nebula/Architecture/Errors/NebulaUserError.swift` — `RecoveryAction` (`Sendable`/`Equatable`/`Hashable`/`CustomStringConvertible` enum, 4 cases) + `NebulaUserError` (`Sendable`/`Equatable`/`Hashable` struct, 3 fields) + `NebulaUserError.default(for:context:)` English table. Not a `NebulaFailure`; no `Kind` coupling.
- [x] `Sources/Nebula/Errors/NebulaErrorConfiguration.swift` — `userMessageMap` stored property + `init` param (default `{ _, _ in nil }`) threaded through the existing `.with*` builders; `withUserMessageMap(_:)` builder; `userError(for:)` accessor (not gated on `isEnabled`).
- [x] `Sources/Nebula/Errors/NebulaErrorConfig.swift` — `userError(for:)` accessor convenience (mirrors `report(_:)`).
- [x] `Tests/NebulaTests/ArchitectureUserErrorTests.swift` — 21 tests / 5 suites.
- [x] `Nebula.docc/ArchitectureUserError.md` — guide-style article (the `ArchitectureErrors.md` model); `Architecture.md` — `- <doc:ArchitectureUserError>` after `- <doc:ArchitectureErrors>`. The "Error taxonomy" paragraph is unchanged.
- [x] Vault — `nebula-user-error.md` (shipped); `nebula-usererror-environment-featureflags.md` user-error dimension → shipped; `Home.md` N12 shipped link; hub `nebula-app-readiness-research.md` N12 row + wave line → shipped.
- [x] Governance — `DECISIONS.md` N12 ADR (Accepted); `ARCHITECTURE.md` prose bullet; this roadmap; `CHANGELOG.md` 0.9.0 entry. 698 Nebula tests / 141 suites + 12 Aurora / 3 suites green; zero warnings; DocC clean.
- [ ] Tag `0.9.0` — **pending** (owner gate decision).

### Deferred (tracked below in "Later")
- `NebulaError` adopting `RecoverableError` (closure-based — app/Cosmos opt-in; closures crossing isolation need `@Sendable` handlers); a `NebulaErrorPresenter` port (Cosmos-only — presentation is UI); `RecoveryURL` support (not public Apple API — refuted).

## Done (0.8.0 — Composition root recipe + DocC)

> App-readiness Wave N11 — the composition-root recipe, **docs-only in Nebula** (no new source types, no API change, no new tests). A DocC article `ArchitectureCompositionRoot.md` names what the existing "Dependency injection without a framework" paragraph leaves implicit: how to wire the full vertical (`viewmodel ← use case ← repository ← gateway ← cache`) at launch via explicit constructor injection of `Sendable` values, `NebulaRegistryConfig.set(…)` once, `.instrumented()` decorators, and a hand-off to the app's `@MainActor @Observable` viewmodel. The article carries the guardrail "do not extend `NebulaRegistry` toward a container" (scope-creep risk #3) and the `@MainActor`-factory runtime-isolation caveat (Factory#322). Ships as Nebula 0.8.0 (additive minor within Nebula 26). The runnable `@MainActor @Observable` vertical is the follow-up **N11b** in a sibling (Meridian) — forbidden in Foundation-only Nebula. Tagging deferred to the owner's gate decision.

### Wave N11 — Composition root recipe + DocC — DONE (tag pending)
- [x] `Nebula.docc/ArchitectureCompositionRoot.md` — guide-style article (the `ArchitecturePresentation.md` model: prose `###` subsections + a `## Topics` group of existing symbols, no new API). Sections: *Where the composition root lives* (at `@main`/`App.init`; `@MainActor` confined to the UI-owning root, services `nonisolated Sendable` cross via `await`; the runnable vertical is in the Meridian sibling); *Wiring order* (build `NebulaRegistryConfiguration` via `.withFactory(for:_:)`, `NebulaRegistryConfig.set(…)` once, resolve adapters, inject explicitly into the `NebulaUseCase` body, `.instrumented()`, hand off); *Why not a container* (the guardrail — no singleton/scoping/graph; the registry is a factory map; no `NebulaCompositionRoot`/`NebulaAppContainer` helper type); *The `@MainActor`-factory hazard* (keep registry factories `nonisolated @Sendable`). The `swift` wiring block stops at the use-case handoff — no SwiftUI/SwiftData symbols in Nebula's catalog.
- [x] `Nebula.docc/Architecture.md` — `- <doc:ArchitectureCompositionRoot>` inserted after `- <doc:ArchitectureRegistry>` (registry → composition root → testing cluster). The "Dependency injection without a framework" paragraph is unchanged.
- [x] No source / no tests — N11 is docs-only; the recipe's correctness is its fidelity to the existing `NebulaRegistry` / `NebulaUseCase` API. 677 Nebula tests / 136 suites green (unchanged); zero warnings.
- [x] Vault — `nebula-composition-root.md` marked `status: shipped` / `shipped: "0.8.0"`; `vault/Home.md` N11 shipped link; hub `nebula-app-readiness-research.md` N11 row → shipped + N11b follow-up.
- [x] Governance — `DECISIONS.md` N11 ADR (Accepted); `ARCHITECTURE.md` prose bullet (composition-root recipe article); this roadmap; `CHANGELOG.md` 0.8.0 entry.
- [ ] Tag `0.8.0` — **pending** (owner gate decision).

### Deferred (tracked below in "Later")
- **N11b** — runnable `NebulaCompositionExample` in a sibling (extend `MeridianExample` or a new `NebulaCompositionExample`): `Account: NebulaEntity` + `AccountRepository` (backed by `NebulaFakeRepository`, swappable for real `NebulaHTTPGateway` + `NebulaURLCache`) + `NebulaUseCase<WithdrawInput, Account>` + `@MainActor @Observable ProfileViewModel: NebulaViewModel` + `Router<AppRoute>`; compile gate (`swift run`) + a test proving the wiring is unit-testable without a simulator.

## Later (post-0.1.0)

- [ ] `NebulaLogStoreExporter`/`NebulaLogStoreEntry` (deferred from Wave B) — macOS-only `.system`/`.local()` gated `#if os(macOS)` + explicit per-platform `@available(<platform>, unavailable)` (NOT `@available(macOS 12, *)` alone); `entries` copies into Sendable `NebulaLogStoreEntry` since `OSLogEntry` is not Sendable. visionOS availability uncertain → conservatively gate unavailable.
- [ ] `NebulaLocked<Value>`/`NebulaFlag`/`NebulaOnce` (`~Copyable`/`Sendable` concurrency wrappers around `Mutex`/`Atomic` — deferred from Wave A; compile-validate `~Copyable`+`Sendable` synthesis against the exact Swift 6.4 toolchain before shipping)
- [ ] `NebulaClock` (ContinuousClock/SuspendingClock wrapper; measure/now/sleep — deferred from Wave F; measure currently uses `any Clock<Duration>` directly)
- [ ] `NebulaMeasureResult` distribution stats (min/max/mean/p50/p99) + `Clock.minimumResolution` field
- [ ] `NebulaStandards.FormatStyle` polymorphic entry / `AnyFormatStyle` (if helpers need to store "some format" generically)
- [ ] `NebulaSignposter` default-disable in release builds (`#if DEBUG` default or release-build disable)
- [ ] OS 26 signpost-metric APIs — `NebulaMetricOperation` enum (add/subtract/set/reset) gated `@available(iOS 26, *)` (`OSMetricOperation` is internal to the os overlay — cannot re-export)
- [ ] Async `NebulaLogStoreExporter.entries` variant (sync `AnySequence` copying into `NebulaLogStoreEntry` for v1)
- [ ] `NebulaJSONSerialization` helper for top-level fragments
- [ ] `URLDetector` wrapper (NSDataDetector `.link` already in String extensions; defer standalone)
- [ ] `ValidatedCodable` wrapper / Codable validation helpers
- [ ] Back-deployment via `FoundationPreview` (if the floor ever lowers)
- [ ] `nebulaFiltered(by:)` (Foundation `Predicate`-based filter) + `NebulaFrequency` frequency-counter (deferred from Wave D — not shipped in 0.1.0; `nebulaFiltered`/`NebulaFrequency` referenced in early ROADMAP drafts were removed before tagging)
- [ ] `CountedSet` full API (if `NebulaFrequency` struct-copy is insufficient)
- [ ] Lazy sequence types behind `nebulaLazy` property (lazy needs `@escaping @Sendable` projections only there)

## Open risks / verification TODOs (verified at blueprint time, NOT now)

- visionOS availability of `OSLogStore.Scope.system`/`.local()` — header does NOT list visionOS in `API_UNAVAILABLE`; conservatively gate unavailable; confirm at compile time on the visionOS SDK.
- `~Copyable`+`Sendable` wrapper conformance synthesis for `NebulaLocked`/`NebulaFlag`/`NebulaOnce` — validate against the exact Swift 6.4 toolchain before scaffolding (the design matches verified stdlib signatures but synthesized `Sendable` for a `~Copyable` struct holding a `~Copyable` Sendable `let` was not executed in the research pass).
- watchOS/visionOS `FormatStyle` case differences — flag for Wave F verification against the `.swiftinterface` (DiscreteFormatStyle is iOS 18/watchOS 11 — below floor, ungated; but platform-specific case availability still needs enumeration).
- `_Concurrency` Clock/Instant/ContinuousClock/SuspendingClock ship only as binary prebuilt `.swiftmodule` (no textual `.swiftinterface`) — availability via Apple docs + SE-0329 + typecheck probe, not a local grep. Confirm at compile time.
- `OSLog` is a clang umbrella module with no textual `.swiftinterface` — `os.Logger`/`OSLogStore`/`OSLogEntry`/`OSSignposter` availability (iOS 14–15/macOS 11–12/tvOS 14–15/watchOS 7–8/visionOS 1, all below `.v26`) from Apple docs + WWDC; keep wrappers thin.
- CI Xcode pin: must be Xcode 26.4+ (Swift 6.3), not Xcode 26.0–26.3 (Swift 6.2, will NOT parse a 6.3 manifest).
- `WebFetch` has hallucinated availability tables repeatedly (UUID.random, percentEncodedQueryItems, `OutputFormatting.fragmentsAllowed`, `convertFromKebabCase`, `base64Encode`) — the `.swiftinterface` is authoritative; never rely on WebFetch for availability.

### Refuted specs / corrected citations (recorded in vault `08-riscos/`)

- `OSLogStore`/`OSLogEntry`/`getEntries` are NOT in `os.swiftmodule` (0 matches) — they live in the OSLog clang module + a separate `OSLog.swiftmodule` Swift overlay (getEntries at L11-14). The original `os.swiftmodule L11-14 getEntries` citation is wrong.
- `@available(macOS 12, *)` alone does NOT make a symbol macOS-only — the `*` fallback enables all platforms. Use `#if os(macOS)` or explicit per-platform `@available(<platform>, unavailable)`.
- `NebulaErrorConfiguration: Equatable` does NOT compile (`@Sendable` closure not `Equatable`) — corrected to Sendable only (mirrors `CosmosErrorConfiguration`).
- `underlying: NebulaError?` does NOT compile (struct recursion illegal) — corrected to `underlying: Box?` via `final class Box: Sendable, Hashable`.
- `Calendar.gregorian` does NOT exist as a static var → use `Calendar(identifier: .gregorian)`.
- `Date.RelativeFormatStyle` has NO `.offset`/`.timer`/parameterless `static var relative` — only `static func relative(presentation:unitsStyle:)`; timer-style display is `Duration.TimeFormatStyle.Pattern.hourMinuteSecond`. `RelativeFormatStyle` uses stored `var` properties + init, NOT a fluent chain.
- `Decimal` does NOT conform to `FloatingPoint`; `Decimal.FormatStyle` is its own struct. `Decimal` has NO value-level `rounded(_:)` — implement via `NSDecimalRound`.
- `ListFormatter` has NO `ItemStyle` enum (erroneous assumption in the brief).
- `UUID.random(using:)` (NOT parameterless `random()`) is `@available(iOS 26.4, *)` — ABOVE floor; `UUID()` remains the default random v4 generator.
- `JSONEncoder.OutputFormatting` has ONLY `prettyPrinted`/`sortedKeys`/`withoutEscapingSlashes` — NO `fragmentsAllowed` (that's `JSONSerialization.WritingOptions`); `ReadingOptions` uses `.allowFragments` (singular). NO `convertFromKebabCase`, NO `base64Encode`.
- Foundation has NO native hex encode/decode (grep confirms).
- `ListFormatStyle` + `.list` accessor are `@available(macOS 12, iOS 15, tvOS 15, watchOS 8)` — NOT macOS 13/iOS 16/watchOS 9 as originally claimed.
- `Duration` has NO top-level `Duration.Unit` — the unit type is `Duration.UnitsFormatStyle.Unit`, init takes a `Set`, NOT an `Array`.
- `Measurement<U>.FormatStyle` is constructed with `width`/`usage`/`numberFormatStyle`/`locale` — NO `unit` parameter.
- `DateComponents.ISO8601FormatStyle` + `.iso8601` static is ALSO OS 26 (research missed this) — gate alongside `DateComponents.formatted(_:)`.
- `swift-tools-version: 6.3` requires Xcode 26.4+ (Swift 6.3 first shipped in Xcode 26.4, March 2026); Xcode 26.0–26.3 shipped Swift 6.2.
- `Atomic.load(ordering:)` takes `AtomicLoadOrdering` (only `.relaxed`/`.acquiring`/`.sequentiallyConsistent`) — `.acquiringAndReleasing` is INVALID for `load` (lives in `AtomicUpdateOrdering`). NO single `Ordering` enum — three frozen structs.
- `Mutex.withLock` uses `sending` (SE-0430), NOT `transferring` (the earlier SE-0433 spelling was revised).